# new_JIMI L5 Sidecar — server/.env 模板

> **读者**：A 端负责人（L5 自动执行）  
> **用法**：复制为 `new_JIMI/HAJIMI_UI/server/.env` 并填入真实值。  
> **端口**：Sidecar 默认 **8011**（与 canonical 8010 并存）。  
> **注意**：B 端 `env_sync` **不会**写入此文件；Omni/LLM 需与 8010 手动对齐（共用同一 `OMNIPARSER_URL`）。

```ini
# ── LLM API（OpenAI 兼容）──
LLM_API_KEY=sk-your-key-here
LLM_MODEL=gpt-4o
LLM_BASE_URL=https://api.openai.com/v1
LLM_TIMEOUT=60

# ── 服务认证 ──
HAJIMI_DEMO_KEY=hajimi-demo-2026
HAJIMI_HOST=127.0.0.1
HAJIMI_PORT=8011

# ── OmniParser（与 8010 canonical 共用同一实例）──
# GPU API 模式：SSH 隧道 :9800
OMNIPARSER_URL=http://127.0.0.1:9800
OMNIPARSER_TIMEOUT=120
OMNIPARSER_RETRY=0
# 本地 CPU 模式：:8002，OMNIPARSER_TIMEOUT=360

USE_REAL_LLM=true
```

**首装**：

```powershell
cd new_JIMI\HAJIMI_UI
scripts\setup_server_env.bat
copy server\.env.example server\.env
# 编辑 server\.env
set HAJIMI_PORT=8011
scripts\start_server.bat
```

**验收**：`curl http://127.0.0.1:8011/api/demo/health/live`

B 端一键：`HAJIMI_UI\scripts\start_l5_sidecar.bat`（薄封装，cd 到本目录并 call `start_server.bat`）。
