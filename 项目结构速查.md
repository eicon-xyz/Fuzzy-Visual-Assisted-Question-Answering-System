# HAJIMI 项目结构速查

> V2.2 | 2026-07-07 | 以 `启动本地.bat` 为基准

## 启动方式

```bash
E:\Fuzzy-Visual-Assisted-Question-Answering-System\启动本地.bat
```

内部调用链：`启动本地.bat` → `HAJIMI_UI/scripts/start_local_vision.bat`（local_vision 模式，无 :9800）

## 目录结构

```
E:\Fuzzy-Visual-Assisted-Question-Answering-System\
│
├── 启动本地.bat              ← 最终启动入口
│
├── HAJIMI_UI/                 ← B端 + A端（主开发目录）
│   ├── main.py                ← B端 PyQt5 桌面应用入口
│   ├── config.py              ← B端配置（API地址、超时、UI参数）
│   ├── core/                  ← B端核心模块
│   │   ├── api_client.py      ← 对A端的HTTP调用
│   │   ├── bc_signals.py      ← B-C信号总线
│   │   ├── defaults.py        ← 全局默认值（端口/URL/Key）
│   │   ├── execute_worker.py  ← V2.2 自动执行引擎
│   │   └── ...
│   ├── ui/                    ← B端 PyQt5 界面
│   │   ├── main_widget.py     ← 主窗口（含C端集成）
│   │   ├── app_controller.py  ← 业务控制器
│   │   └── native/            ← Native UI 组件
│   ├── server/                ← A端 FastAPI 后端
│   │   ├── main.py            ← 入口（注册全部7路由）
│   │   ├── config.py          ← A端配置（LLM/OmniParser）
│   │   ├── .env               ← 环境变量（API Key等）
│   │   ├── .venv/             ← Python虚拟环境
│   │   ├── routes/            ← 全部路由文件
│   │   │   ├── demo.py        ← /api/demo/* (12端点)
│   │   │   ├── admin.py       ← /api/admin/* (11端点)
│   │   │   ├── audit.py       ← /api/audit/* (2端点)
│   │   │   ├── auth.py        ← /api/auth/login
│   │   │   ├── config_client.py ← /api/config/pull
│   │   │   ├── flow.py        ← /api/admin/flow/* (3端点)
│   │   │   └── monitor.py     ← /api/admin/monitor/* (3端点)
│   │   ├── database/          ← SQLAlchemy ORM
│   │   │   ├── models.py      ← 7表模型定义
│   │   │   └── repository.py  ← 数据访问层
│   │   ├── services/          ← AI服务
│   │   │   ├── llm/           ← LLM客户端(8供应商)
│   │   │   ├── planning/      ← 规划/评估/风险评分
│   │   │   ├── agent/         ← 编排器
│   │   │   └── omniparser_client.py ← OmniParser客户端
│   │   └── data/              ← SQLite数据库文件
│   │       └── hajimi.db
│   └── scripts/               ← bat启动脚本
│       ├── start_local_vision.bat ← local_vision 启动（无 :9800）
│       └── verify_bc_signals.py ← B-C信号验证
│
├── client/                    ← C端 Python模块
│   ├── voice/                 ← ASR + TTS
│   ├── audit/                 ← 审计代理(SQLite+脱敏)
│   ├── config/                ← 配置轮询(ETag)
│   ├── integration/           ← B-C集成控制器
│   ├── bc_adapter.py          ← B端3行接入适配器
│   └── voice_setup.py         ← 语音环境验证
│
├── web-admin/                 ← C端 Web管理面板(Vue3)
│   ├── src/
│   │   ├── api/admin.js       ← 19个Admin API方法+Mock
│   │   ├── views/             ← 6个页面
│   │   └── main.js            ← 入口(autoDetectServer)
│   └── vite.config.js         ← proxy→127.0.0.1:8010
│
├── 项目文档/                   ← 全部项目文档
│   ├── 详细设计/              ← 4份详细设计docx
│   ├── 测试文档/              ← 测试方案+报告+7个测试脚本
│   ├── 答辩PPT/               ← 验收答辩PPT
│   └── HAJIMI-API接口文档.html
│
└── 工作进度/                   ← 每日进度报告+测试指南+启动指南+工作计划
```

## 端口与通信

| 端口 | 服务 | 启动方 | 地址 |
|------|------|--------|------|
| `8010` | A端 FastAPI | `启动本地.bat` 自动 | 监听 `0.0.0.0:8010`，客户端连 `127.0.0.1:8010` |
| `8011` | L5 Sidecar | `启动本地.bat` 自动 | B端内部管理 |
| `9800` | OmniParser GPU | SSH隧道手动建立 | `ssh -L 9800:127.0.0.1:9800 student@...` |
| `5173` | Web管理面板 | `cd web-admin && npm run dev` | `http://localhost:5173` |

## C端快速启动

```bash
# A端自动启（通过启动本地.bat）
E:\Fuzzy-Visual-Assisted-Question-Answering-System\启动本地.bat

# Web面板
cd web-admin && npm run dev

# 测试
python 项目文档/测试文档/demo_connectivity.py
```
