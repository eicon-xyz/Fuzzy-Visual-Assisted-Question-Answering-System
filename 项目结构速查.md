# HAJIMI 项目结构速查

> V2.3 | 2026-09 | L5 自动执行（唯一模式）| 基准：`安装全栈.bat` + `启动全栈.bat`

## 启动方式

```bash
E:\Fuzzy-Visual-Assisted-Question-Answering-System\安装全栈.bat   # 首次：建 2 个 venv + 初始化 server_A/server/.env
E:\Fuzzy-Visual-Assisted-Question-Answering-System\启动全栈.bat   # 日常一键启动
```

内部调用链：`启动全栈.bat` → `HAJIMI_UI/scripts/start_release_fullstack.bat`（拉起 L5 Sidecar :8011 + B端两个窗口）
开发备用：`启动本地.bat` → `HAJIMI_UI/scripts/start_local_vision.bat`（同样 L5-only）；停止：`stop_all.bat`（只停 :8011）；验收：`验收.bat`（`verify_all.py --require-l5`）

## 目录结构

```
E:\Fuzzy-Visual-Assisted-Question-Answering-System\
│
├── 安装全栈.bat / 启动全栈.bat / 启动本地.bat / 验收.bat / stop_all.bat / 打包.bat
│
├── server_A/                   ← L5 Sidecar（唯一后端，FastAPI :8011）
│   ├── scripts/                ← Sidecar 启动/修复脚本
│   ├── data/                   ← SQLite数据库文件
│   │   └── hajimi.db
│   └── server/                 ← Sidecar 服务端代码
│       ├── main.py             ← 入口（注册全部7路由）
│       ├── config.py           ← Sidecar 配置（LLM_*；OMNIPARSER_ENABLED=false / ROUTING_MODE=l5）
│       ├── .env                ← 模型 key 唯一存放处（DEEPSEEK_API_KEY / LLM_*）
│       ├── .venv/              ← Sidecar 虚拟环境（安装全栈.bat 创建）
│       ├── routes/             ← 全部路由文件
│       │   ├── demo.py         ← /api/demo/*（execute/stream/cancel/health）
│       │   ├── admin.py        ← /api/admin/* + /users/*
│       │   ├── audit.py        ← /api/audit/*
│       │   ├── auth.py         ← /api/auth/login
│       │   ├── config_client.py ← /api/config/pull
│       │   ├── flow.py         ← /api/admin/flow/*
│       │   └── monitor.py      ← /api/admin/monitor/*
│       ├── database/           ← SQLAlchemy ORM
│       │   ├── models.py       ← 7表模型定义
│       │   └── repository.py   ← 数据访问层
│       └── services/           ← AI服务
│           ├── llm/            ← LLM客户端(多供应商)
│           ├── planning/       ← 规划/评估/风险评分
│           ├── agent/          ← 编排器
│           ├── executor/       ← L5 执行引擎（uia_bridge UIA绑定 + clicker + safety）
│           └── browser/        ← Playwright DOM 自动化
│
├── HAJIMI_UI/                 ← B端 PyQt5 桌面应用（主开发目录）
│   ├── main.py                ← B端入口
│   ├── config.py              ← B端配置（L5 API地址、超时、UI参数）
│   ├── requirements.txt       ← 依赖仅 PyQt5
│   ├── .venv/                 ← B端虚拟环境（安装全栈.bat 创建）
│   ├── core/                  ← B端核心模块（L5 子集）
│   │   ├── api_client.py      ← 对 Sidecar 的 HTTP 调用（execute_task/cancel_task/health）
│   │   ├── execute_worker.py  ← L5 提交 + SSE 进度线程
│   │   ├── l5_sidecar_launcher.py ← 拉起/守护 Sidecar(:8011)
│   │   ├── l5_query_normalize.py  ← 提交前红线归一化
│   │   ├── sidecar_modules.py ← 从 server_A 动态加载红线规则
│   │   ├── env_sync.py        ← sync_l5_sidecar_env → server_A/server/.env
│   │   ├── service_manager.py ← 进程管理（仅 :8011）
│   │   ├── user_settings.py   ← 用户设置持久化
│   │   ├── bc_signals.py      ← B-C信号总线
│   │   └── defaults.py        ← 全局默认值（DEFAULT_L5_PORT=8011 等）
│   ├── ui/                    ← B端 PyQt5 界面
│   │   ├── main_widget.py     ← 主窗口（操作指引/步骤列表/提醒通知/系统设置，含C端集成）
│   │   ├── app_controller.py  ← 业务控制器（L5）
│   │   └── native/            ← Native UI 组件（含 l5_timeline / l5_step_row）
│   └── scripts/               ← bat启动脚本
│       ├── start_release_fullstack.bat ← 全栈启动（Sidecar :8011 + B端）
│       ├── start_local_vision.bat ← 本地开发启动（L5-only）
│       ├── start_l5_sidecar.bat   ← 单独启动 Sidecar
│       ├── apply_l5_settings.py   ← 写 OMNIPARSER_ENABLED=false / ROUTING_MODE=l5
│       └── verify_bc_signals.py ← B-C信号验证
│
├── client/                    ← C端 Python模块（server_url 默认指 :8011）
│   ├── voice/                 ← ASR + TTS
│   ├── audit/                 ← 审计代理(SQLite+脱敏)
│   ├── config/                ← 配置轮询(ETag)
│   ├── integration/           ← B-C集成控制器
│   ├── bc_adapter.py          ← B端3行接入适配器
│   └── voice_setup.py         ← 语音环境验证
│
├── web-admin/                 ← C端 Web管理面板(Vue3)
│   ├── src/
│   │   ├── api/admin.js       ← Admin API方法
│   │   ├── views/             ← 页面
│   │   └── main.js            ← 入口(autoDetectServer)
│   └── vite.config.js         ← proxy→127.0.0.1:8011（Sidecar 挂载 admin/audit/auth/flow/monitor/users）
│
├── 项目文档/                   ← 全部项目文档
│   ├── 详细设计/              ← 4份详细设计docx
│   ├── 测试文档/              ← 测试方案+报告+7个测试脚本
│   ├── 答辩PPT/               ← 验收答辩PPT
│   └── HAJIMI-API接口文档.html
│
└── 工作进度/                   ← 每日进度报告+测试指南+启动指南+工作计划
```

## 端口与通信

| 端口 | 服务 | 启动方 | 地址 |
|------|------|--------|------|
| `8011` | L5 Sidecar（server_A，唯一后端） | `启动全栈.bat` 自动 | 监听 `127.0.0.1:8011`，B端/C端/web-admin 均连此端口 |
| `5173` | Web管理面板 | `cd web-admin && npm run dev` | `http://localhost:5173`（proxy → :8011） |

> 旧 A端 `:8010`、OmniParser GPU 隧道 `:9800` 已随 L4 指引模式整体移除。

## C端快速启动

```bash
# Sidecar + B端（自动）
E:\Fuzzy-Visual-Assisted-Question-Answering-System\启动全栈.bat

# Web面板
cd web-admin && npm run dev

# 测试
python 项目文档/测试文档/demo_connectivity.py
```
