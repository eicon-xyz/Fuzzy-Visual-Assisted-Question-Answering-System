# HAJIMI B-end UI dependencies (PyQt5 native client, L5 auto-execute mode)
# Backend = server_A L5 Sidecar (own venv; see server_A/server/requirements.txt)
# C-end voice (client/) has its own requirements (client/requirements.txt)

PyQt5>=5.15

# PyQt5 wheel includes QtSvg (used by ui/native/luxury/icons.py).
# If ImportError on PyQt5.QtSvg, reinstall: pip install --force-reinstall PyQt5
