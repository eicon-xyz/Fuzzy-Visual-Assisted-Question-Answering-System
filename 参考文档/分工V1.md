# HAJIMI 团队分工 V1

> 基于[设计文档V1.md](设计文档V1.md)，经工作量评判后调整，将语音交互（ASR/TTS）从 B 移交至 C。

---

## 调整后分工总览

| 成员 | 定位 | 核心职责 |
|------|------|----------|
| **A** | 后端 / AI 核心 | 理解与规划层 + Server 后端 |
| **B** | 前端 / 桌面应用 | 感知层 + 执行层（视觉）+ GUI |
| **C** | 集成 / 管理端 / 语音 | 审计代理 + 管理面板 + 语音交互 |

---

## 成员 A — 后端 / AI 核心

| 模块 | 具体任务 | 难度 | 工作量 |
|------|----------|------|--------|
| **意图理解 (INTENT)** | 结构化解析、指代消解、置信度检测与主动澄清逻辑 | ⭐⭐⭐⭐⭐ | 高 |
| **蓝图规划 (PLANNER)** | 蓝图生成/锁定、指纹比对、状态机（挂起/恢复/回退） | ⭐⭐⭐⭐⭐ | 高 |
| **LLM/搜索 API 封装** | GPT-4V / Qwen-VL 适配、流式输出、Agent 联网搜索 | ⭐⭐⭐⭐ | 中高 |
| **数据库设计** | 5 张表（事务/步骤/异常/反馈）Schema 设计与 Migration | ⭐⭐⭐ | 中 |
| **Server 核心 API** | FastAPI 框架搭建、各接口实现 | ⭐⭐⭐ | 中 |

> **特征**：深而窄，AI 逻辑核心，技术栈集中在 Python + LLM + FastAPI

---

## 成员 B — 前端 / 桌面应用

| 模块 | 具体任务 | 难度 | 工作量 |
|------|----------|------|--------|
| **屏幕捕获 (CAP)** | `mss`/`PIL` 截图、高 DPI 适配、窗口指纹计算 | ⭐⭐⭐ | 中 |
| **UI 解析器 (PARSER)** | OmniParser V2 部署、GPU 显存管理、备选方案 PaddleOCR+GroundingDINO | ⭐⭐⭐⭐⭐ | 高 |
| **SoM 标注 (SOM)** | 标注图生成（边界框+ID）、元素映射表构建 | ⭐⭐⭐⭐ | 中高 |
| **桌面挂件 (主 UI)** | PyQt5 悬浮窗：毛玻璃效果、对话气泡、步骤列表、状态指示器、控制栏 | ⭐⭐⭐⭐ | **非常高** |
| **屏幕覆盖层 (ANNO)** | 全屏透明窗口：红色箭头、虚线高亮框、编号标签、鼠标穿透 | ⭐⭐⭐⭐ | 高 |
| **文字指引 (TEXT)** | 步骤列表格式化、当前步骤高亮 | ⭐⭐ | 低 |

> **特征**：广而精（减至 3 个技术域：GUI + ML 部署 + 图像处理），PyQt5 仍是最大单项

---

## 成员 C — 集成 / 管理端 / 语音

| 模块 | 具体任务 | 难度 | 工作量 |
|------|----------|------|--------|
| **ASR 语音识别** | `SpeechRecognition` 集成、Google/百度 API（在线）+ Vosk（离线）、麦克风按钮交互 | ⭐⭐⭐ | 中 |
| **TTS 语音合成** | `pyttsx3`（离线）+ 云端 TTS API、语速调节、开关控制 | ⭐⭐ | 中低 |
| **语音控制逻辑** | 语音播报队列管理、ASR→意图理解层对接、与 B 的 GUI 控制栏联动接口 | ⭐⭐⭐ | 中 |
| **审计代理** | 本地 SQLite 缓存、隐私脱敏、批量 POST 上报、重试机制 | ⭐⭐⭐ | 中 |
| **配置拉取** | `/api/config/pull` 定时轮询、本地配置更新 | ⭐⭐ | 低 |
| **Web 管理面板** | 失败归因看板（图表）、配置热部署、健康监控、数据脱敏导出 | ⭐⭐⭐ | 高 |
| **联调测试 + 文档** | 全流程集成测试、演示材料、项目报告 | ⭐⭐⭐ | 中 |

> **特征**：面广但各项独立，语音模块与审计/面板无耦合，可并行推进

---

## 调整前后对比

| 维度 | A（不变） | B（减负） | C（增负） |
|------|-----------|-----------|-----------|
| **技术域数量** | 2（AI + 后端） | ~~5~~ → **3**（GUI + ML + 图像） | ~~3~~ → **4**（语音 + 审计 + Web + 集成） |
| **最大单项工作量** | 蓝图状态机 | PyQt5 桌面挂件 | Web 管理面板 |
| **不确定性风险** | LLM 调优 | OmniParser 资源 | 低（均为成熟方案） |
| **整体评级** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐（从 ⭐⭐⭐⭐⭐ 降） | ⭐⭐⭐（从 ⭐⭐ 升） |
| **相对排名** | 次重 | **仍最重** | 最轻 |

---

## B 与 C 的接口约定

语音模块移交后需明确对接点：

| 接口点 | B 提供 | C 调用 |
|--------|--------|--------|
| **麦克风按钮** | PyQt5 控制栏预留按钮 + 点击信号 | C 绑定信号 → 启动 ASR 录音 |
| **TTS 播报触发** | 文字指引生成完毕后 emit 信号（含文本内容） | C 接收信号 → 入队 TTS 播报 |
| **语音开关/语速** | 控制栏 toggle/slider 控件 + 状态变量 | C 读取状态 → 控制语音行为 |

> 双方只需约定 **2 个信号 + 2 个状态变量**，耦合极低。C 可先独立完成语音模块并用命令行测试，联调时接入 B 的 GUI 即可。

---

## 注意事项

1. **B 仍是工作量最大者**：PyQt5 桌面挂件 + OmniParser 仍是两大重头戏。如果后续发现 B 仍然超载，可进一步将 **SoM 标注生成**移给 A（A 已掌握图像→LLM 链路，SoM 是 LLM 输入的前置环节，逻辑上归属理解与规划层）。

2. **C 的 Web 管理面板优先级可降**：前两周开发路线中，管理面板在 D7-8 才启动。语音模块工作量可控（预估 2-3 天），不会挤压管理面板的开发窗口。

3. **三人在 D10-14 联调阶段均需全员参与**，C 作为集成负责人牵头，A/B 配合排查各自模块问题。
