# HAJIMI 第四天工作进度 — 角色 C（集成 / 语音 / 管理端）

**姓名**：涂浚稷  
**学号**：20230353  
**日期**：2026年7月2日  
**角色**：C（集成 / 管理端 / 语音）

---

## 一、完成任务

### 1. Admin API 服务层 ✅

**文件**：`web-admin/src/api/admin.js`（372 行）

封装全部 19 个 `/api/admin/*` 端点调用方法，包含：
- **总览**：`fetchOverview`、`fetchTrend`、`fetchFeedback`、`fetchTopTasks`、`fetchRedline`
- **失败归因**：`fetchFailuresStats`、`fetchFailuresList`、`fetchFailureDetail`
- **数据流**：`fetchFlowTopology`、`fetchFlowMetrics`、`fetchFlowVersions`
- **健康监控**：`fetchMonitorHealth`、`fetchAlerts`、`markAlertRead`、`markAllAlertsRead`
- **系统配置**：`fetchConfigCurrent`、`deployConfig`、`fetchDeployLogs`

内置 `USE_MOCK` 开关：A 端未就位时自动使用完整 Mock 数据，A 端就位后一行 `setUseMock(false)` 即可全部切换为真实 API。

### 2. FlowMonitor — 桑基数据流图 ✅

**文件**：`web-admin/src/views/FlowMonitor.vue`

- 力导向图替换为 **桑基图**（ECharts Sankey），直观展示客户端→网关→数据库→LLM 的流量走向
- 高负载链路红色标注（`status=high_load`）
- QPS + 成功率双轴图（含 99% 告警阈值线）
- 客户端版本环形饼图
- 全部图表通过 `fetchFlowTopology/Metrics/Versions` 加载数据

### 3. SystemConfig — 10 项表单 + 部署日志 ✅

**文件**：`web-admin/src/views/SystemConfig.vue`

- 4 个分区（`el-divider`）：AI 推理、任务控制、语音、路由规则
- 10 项表单项：置信度阈值、LLM 端点、LLM 模型、Token 限制、模板相似度阈值、最大蓝图步骤数、配置拉取间隔、审计批量大小、离线 TTS 引擎、路由规则 JSON
- JSON 编辑器：格式化 + 校验按钮
- 热部署二次确认弹窗
- 底部部署操作日志表（操作人/操作类型/版本/影响客户端/时间）

### 4. HealthMonitor — 状态灯 + 告警管理 + CSV 导出 ✅

**文件**：`web-admin/src/views/HealthMonitor.vue`

- 资源卡片带图标和背景色（CPU/内存/磁盘/运行时间）
- 组件健康状态**指示灯**（绿色光晕=正常，黄色光晕=降级）
- 告警管理：单条已读 / 全部已读 / 未读红色角标
- **CSV 导出**：Blob 生成，BOM UTF-8 编码（Excel 直接打开中文不乱码）
- 通过 `fetchMonitorHealth`、`fetchAlerts` 加载数据

### 5. Dashboard API 对接 ✅

**文件**：`web-admin/src/views/Dashboard.vue`

- 新增 `fetchOverview` 导入，KPI 卡片数据从 API 服务层拉取
- Mock 降级自动生效（A 端离线时仍使用静态数据渲染）

---

## 二、新增/修改文件

```
web-admin/src/
├── api/
│   └── admin.js               # 新增：19 个 API 方法 + Mock 数据
└── views/
    ├── Dashboard.vue           # 修改：对接 API 服务层
    ├── FlowMonitor.vue         # 重写：力导向图 → 桑基图
    ├── SystemConfig.vue        # 重写：10 表单 + 4 分区 + 部署日志
    └── HealthMonitor.vue       # 重写：状态灯 + 告警管理 + CSV 导出
```

---

## 三、验证结果

| 测试 | 项目数 | 结果 |
|------|--------|------|
| `day4_check.py` | 54 | 54/54 ✅ |
| `day3_check.py`（回归） | 48 | 48/48 ✅ |
| Admin API 方法 | 19 | 全部 ✅ |
| Web 页面功能 | 18 | 全部 ✅ |

---

## 四、Day 4 管理员功能对照（六天计划）

| 计划功能 | 实现状态 |
|----------|----------|
| 桑基图/流向图 | ✅ Sankey 图 |
| 接口 QPS+成功率双轴图 | ✅ |
| 客户端版本饼图 | ✅ |
| 10 项配置表单 | ✅ 4 分区 |
| JSON 编辑器 + 格式化/校验 | ✅ |
| 热部署二次确认弹窗 | ✅ |
| 资源卡片 | ✅ 带图标 |
| 组件状态指示灯 | ✅ 光晕效果 |
| 告警列表 | ✅ |
| JWT 刷新 Token 机制 | ✅ axios 拦截器 |
| 告警标记已读/全部已读 | ✅ |
| CSV 导出 | ✅ BOM UTF-8 |
| 配置热部署操作日志 | ✅ |
| 管理面板全部图表对接 API | ✅ 19 个端点 |

---

## 五、遗留问题

1. **A 端 `/api/admin/*` 端点未部署**：全部 19 个管理端点返回 404，C 端通过 `USE_MOCK` 开关 + 完整 Mock 数据保证页面正常渲染
2. **A 端就位后切换步骤**：在 `admin.js` 中执行 `setUseMock(false)` 即可全站切换真实数据
3. **B-C 真实 PyQt5 信号联调**：`bc_adapter.py` 已就绪，等待 B 接入

---

## 六、下一步计划（Day 5）

- 全流程集成测试
- Bug 修复冲刺
- 性能基准测试
- 管理面板性能验证
