# HAJIMI 第三天工作进度 — 角色 C（集成 / 语音 / 管理端）

**姓名**：涂浚稷  
**学号**：20230353  
**日期**：2026年7月1日  
**角色**：C（集成 / 管理端 / 语音）

---

## 一、完成任务

### 1. B-C 集成联调仿真测试 ✅

**文件**：`client/bc_integration_test.py`

由于 B 的 PyQt5 GUI 尚未联调，创建独立于 PyQt5 的信号模拟测试框架：
- 实现 `SimulatedSignal` 类模拟 PyQt5 `pyqtSignal` 的 connect/emit 机制
- 实现 `SimulatedBSignals` 完整模拟 B-C 接口契约全部 9 个信号
- 7 个测试用例覆盖完整交互流程：信号绑定 → ASR 管线 → TTS 管线 → 审计提交 → 健康检测 → 配置变更 → 完整交互序列
- 模拟场景：用户按下麦克风 → ASR 录音 → 转写结果 → TTS 播报 → 审计提交

### 2. B-C PyQt5 集成适配器 ✅

**文件**：`client/bc_adapter.py`

B 端只需 3 行代码即可接入全部 C 模块：
```python
from client.bc_adapter import BCAdapter
self.bc = BCAdapter(server_url="http://localhost:8010")
self.bc.wire(self)  # self = AppController
```

提供 4 个公开方法：`bc.speak()` / `bc.audit()` / `bc.health()` / `bc.unwire()`

### 3. 审计代理端到端测试 ✅

**文件**：`client/audit_e2e_test.py`

启动内置 Mock HTTP 服务器（端口 18900），测试 5 个场景：
- 正常批量上报（3 条 → POST → 确认 → 清空本地队列）
- 服务端 500 错误 → 重试机制（记录保留在队列等待重试）
- 隐私脱敏完整覆盖（密码/Key/路径/手机/邮箱/身份证 6 类）
- 请求体格式验证（A-C 契约 §3.1 全部必填字段逐项检查）
- 单独反馈端点 `/api/audit/feedback`

### 4. A 端真实接口集成测试 ✅

**文件**：`client/real_api_test.py`

同步 A 端最新变更后完成真实接口联调准备：
- 端口更新：全部 C 模块默认 URL 从 `localhost:8000` → `localhost:8010`
- `/health` 新增字段适配：`detector_backend`、`omniparser_ready`
- 离线降级测试：A 端离线时所有模块正常降级不崩溃
- 启动 A 端后重跑即可完成真实联调

**已更新的文件**：`audit/audit_agent.py`、`config/config_poller.py`、`integration/controller.py`、`web-admin/vite.config.js`、`CLAUDE.md`

### 5. Web 失败归因页增强 ✅

**文件**：`web-admin/src/views/Failures.vue`

- 详情滑出面板（el-drawer，480px）：包含任务 ID、失败类型、指纹比对
- LLM 输入/输出快照（el-collapse 折叠区）：请求参数 + 响应蓝图 + 错误详情
- 指纹比对详情：期望 vs 实际 App/Verb，不匹配字段红色标注
- 柱状图点击筛选：点击柱状图按失败类型自动过滤列表

### 6. 接口契约补齐 ✅

- 补齐 `POST /api/audit/feedback` 单独反馈端点（`send_feedback()` 方法）
- 审计上报请求体验证：`{client_id, batch}` 格式 + 11 个必填字段全部对齐 A-C 契约

---

## 二、新增文件

```
client/
├── bc_adapter.py              # B-C PyQt5 真实集成适配器
├── bc_integration_test.py     # B-C 集成联调仿真测试（7 个用例）
├── audit_e2e_test.py          # 审计代理端到端测试（5 个场景，含 Mock 服务器）
├── real_api_test.py           # A 端真实接口集成测试（端口 8010）
└── day3_check.py              # Day 3 验证脚本（48 项检查）

web-admin/src/views/
└── Failures.vue               # 增强：详情滑出面板 + LLM 快照 + 图表筛选联动
```

---

## 三、项目文档整理

- 根目录文档归位至 `项目文档/`：`设计文档V2.md`、`分工V1.md`、`用户需求分类.md`
- A 端 CHANGELOG 同步至 `项目文档/CHANGELOG-A端.md`
- 项目结构说明同步至 `项目文档/项目结构.md`
- 清理重复文档 8 个：`HAJIMI_UI/docs/` 全部 6 个文件、`HAJIMI_UI/b-c-api-contract.md`、`HAJIMI_UI/api-contract-demo.yaml`
- 清理废弃文件 5 个：`新建 文本文档.txt`、`device list.txt`、旧版 HTML 原型 3 个
- `项目文档/` 现统一存放 20 个 markdown 文件
- 根目录仅保留 `CLAUDE.md` + `README.md`

### 8. A 端启动排障 ✅

- 安装缺失依赖：`fastapi`、`uvicorn`、`opencv-python-headless`、`Pillow`、`numpy`、`replicate`
- 创建 `HAJIMI_UI/server/.env` 配置文件
- 诊断并解决 `bat` 脚本启动失败：缺少 `server\.venv` + 依赖未安装
- 提供直接启动命令替代 `bat` 脚本：
  ```bash
  cd HAJIMI_UI
  python -m uvicorn server.main:app --host 127.0.0.1 --port 8010
  ```
- 验证 A 端 health 端点返回正常（`detector_backend=replicate_omniparser`）
- 解决端口占用（WinError 10048）：查 PID → kill → 重启

### 9. 测试指南文档 ✅

**文件**：`工作进度/测试指南.md`

编写完整的角色 C 每日测试指南，包含：
- 环境准备（一次性依赖安装 + .env 配置）
- A 端启动步骤与验证
- Day 1 测试（语音 + 设备列表）
- Day 2 测试（模块检查 + Web 管理面板 6 页）
- Day 3 测试（快速验证 + B-C 仿真 + 审计 E2E + 真实接口）
- 一键全测命令
- 常见问题表（含端口冲突处理完整步骤）

---

## 四、验证结果

| 测试 | 项目数 | 结果 |
|------|--------|------|
| `day3_check.py` | 48 | 48/48 ✅ |
| `bc_integration_test.py` | 36 | 36/36 ✅ |
| `audit_e2e_test.py` | 25 | 25/25 ✅ |
| `real_api_test.py` | 16 | 16/16 ✅（A 端在线，管理端点待 Day 4-5） |
| `web-admin` 构建 | vite build | ✅ |
| A-C 接口契约对齐 | 23 端点 | ✅ |
| B-C 接口契约对齐 | 9 信号 | ✅ |
| A 端 health | `/api/demo/health` | ✅ |
| 项目文档整理 | 20 .md 统一存放 | ✅ |

---

## 五、遗留问题

1. **B-C 真实 PyQt5 信号联调**：`bc_adapter.py` 已就绪，等待 B 在 `app_controller.py` 中调用 `BCAdapter.wire(self)`
2. **A 端管理端点未部署**：`/api/audit/*`、`/api/config/*` 返回 404，为 A 端 Day 4-5 计划任务，C 端降级处理已验证正常
3. **Web 管理面板数据**：图表仍使用静态 Mock，待 A 端 `/api/admin/*` 就位后切换

---

## 六、下一步计划（Day 4）

- B↔C 真实 PyQt5 信号联调（`bc_adapter.py` 接入 B 的 AppController）
- A 端全部 23 个端点真实联调验证
- Web 管理面板对接 A 的真实 `/api/admin/*` 数据
- 配置热部署 + 健康监控端到端验证
