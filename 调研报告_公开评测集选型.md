# 调研报告：公开评测集选型（HAJIMI 桌面/浏览器 Agent）

> 版本：v1.0 · 2026-09 · 调研方式：一手来源核查（GitHub repo/README/评测器源码、arXiv 原文摘要页、官方项目页、HF 数据集卡），关键数字均注明 URL；抄不到者标「未核实」。
> **审校记录（审查代理，同日）**：① WindowsWorld 真实性经独立检索双重确认（[ACL 2026 Findings](https://aclanthology.org/2026.findings-acl.750/)、[arXiv:2604.27776](https://huggingface.co/papers/2604.27776)、[repo](https://github.com/HITsz-TMG/WindowsWorld)）；② 「M2AT 查无此物」经独立 web_search 复验零命中——审校方原有「应该存在」的先验被检索轨迹否定，以一手为准；若日后出现同名物视为新项目；③ §2.1 「`--som-origin a11y` + 独立 `--a11y-backend uia` 两开关」的记法修正采纳。
> 一句话结论：**WindowsAgentArena 是唯一与 HAJIMI 技术栈（Windows + UIA + 机器判据）真正对口、可直接移植任务的公开集；浏览器通道借 WebArena-Verified 的判分协议；OSWorld/OSWorld 2.0 是框架与协议参照而非任务来源；M2AT、WindowsBench 两个名字查无此物；中文桌面评测是空白——我们的自研集（叠加 WindowsWorld 的中文指令素材）即是填补。**

---

## 1. TL;DR 选型结论表

| 档位 | 集合 | 一行理由 |
|---|---|---|
| **核心采用（转写后跑）** | [WindowsAgentArena](https://github.com/microsoft/WindowsAgentArena) | 154 条 Windows 真任务、MIT 许可、OSWorld 式**确定性机器判据**（getter+metric 源码可查）、含 13 条 infeasible 负向样本；观测通道原生支持 UIA（`--som-origin a11y --a11y-backend uia`），与我们控件绑定栈同构。任务 JSON→seed.json 字段一一对应，估计抽 60 条转写 ≈6–8 人日 |
| **核心采用（只借判分协议）** | [WebArena-Verified](https://github.com/ServiceNow/webarena-verified)（ServiceNow/Mila，NeurIPS'25 SEA Workshop） | 全量审计 WebArena 812 任务、**明确移除 LLM-judge 与子串匹配**、后端状态校验、结构化 JSON 状态码、模板宏平均+95%CI+失败分型——正是我们 browser_* 通道 oracle 与报表该抄的形态 |
| **可选采用（任务素材）** | [OSWorld examples_windows](https://github.com/xlang-ai/OSWorld/tree/main/evaluation_examples/examples_windows)（49 条）+ [test_infeasible.json](https://raw.githubusercontent.com/xlang-ai/OSWorld/main/evaluation_examples/test_infeasible.json)（9 条）；[WindowsWorld](https://github.com/HITsz-TMG/WindowsWorld)（181 条中英双语，**只借任务不借判据**）；[macOSWorld](https://arxiv.org/abs/2506.04135)（202 条五语含中文，存档） | 与 WAA 同格式可批量转换；WindowsWorld 是唯一**自带中文指令**的 Windows 桌面集（但判分为 qwen3-vl-plus VLM 截图判分，触我们红线，仅借任务文本与 L4「应拒绝」任务设计） |
| **借协议不借任务** | OSWorld-Verified（修订/并行/AWS 复现协议）、[OSWorld 2.0](https://github.com/xlang-ai/OSWorld-V2)（release manifest 版本钉死、防泄漏 gated 分发、长程任务范式）、[UniClawBench](https://github.com/HKU-MMLab/UniClawBench)（user-simulator 追问协议→我们 ask_user 评测参考）、[AgentDojo](https://github.com/ethz-spylab/agentdojo) / [InjecAgent](https://arxiv.org/abs/2403.02691)（注入评测范式，红线层未来用） | 任务本体是 Ubuntu/Linux/工具 API 模拟或 LLM-judge 判分，跑不了也不该照跑；协议思想可直接搬 |
| **不适配 / 查无此物** | M2AT（查无此物）、WindowsBench（查无此物）、OSWorld 主体 369 条（Ubuntu 域+qemu）、VisualWebArena / Mind2Web / WebVoyager / AssistantBench / BrowseComp(-Plus) / MiniWoB++ / WorkArena / UFO²（非任务集）/ ScreenSpot 系（存而不用） | 各一句原因见 §4 清单 |

---

## 2. 候选逐个详表

字段说明：「单机可跑」= 能否在我们的一台 Windows 物理机/WSL2 轻量 VM 上跑起来；「转换工作量」= 接入我们 seed.json（instruction/seeds/setup_ps1/cleanup_ps1/oracle/expect_status）的估计。

### 2.1 WindowsAgentArena（WAA）★ 核心采用

- **地址**：<https://github.com/microsoft/WindowsAgentArena>（项目页 <https://microsoft.github.io/WindowsAgentArena/>，论文 [arXiv:2409.08264](https://arxiv.org/abs/2409.08264)）
- **许可证**：MIT（[API](https://api.github.com/repos/microsoft/WindowsAgentArena) 与 README 徽章一致）
- **维护状态**：最后 push 2026-04-13（GitHub API `pushed_at`），未 archived；分支含 BYOA 文档、macOS host 支持等，半活跃
- **任务量**：**154 条**（一手清点：`src/win-arena-container/client/evaluation_examples_windows/examples/` 恰 154 个 JSON；另有 `examples_noctxt/` 同数 154 条无 setup 上下文的"硬模式"变体，README 2024-11-10 更新说明 `diff_lvl="hard"`）。类别分布：vs_code 24、libreoffice_calc 24、vlc 21、file_explorer 19、chrome 19、libreoffice_writer 19、msedge 13、settings 5、clock 4、windows_calc 3、paint 3、notepad 2。论文原文口径「150+」，Navi 基线 **19.5%** vs 人类 **74.5%**（[arXiv abs 页](https://arxiv.org/abs/2409.08264)）
- **执行环境要求（重点核实——不必 Azure！）**：README 有独立的「**💻 Local deployment (WSL or Linux)**」章节：Docker（Windows 上推荐 Docker Desktop + WSL2）→ `docker pull windowsarena/winarena:latest` → 下载 Win11 企业版评估 ISO（90 天试用，英文）→ `./scripts/run-local.sh --prepare-image true` 自动制备 30GB golden image（QEMU VM，~20 分钟）→ `./run-local.sh` 开跑，`http://localhost:8006` 看屏幕。默认 8GB RAM/8 核，可 `--ram-size 4G --cpu-cores 4` 降配；**KVM 加速强烈建议**（`--use-kvm false` 可关但"性能问题不推荐"，FAQ 原话），Azure ML 只是并行化选项（154 任务 ~20 分钟/40 VM 的那个卖点）。对我们一台 Windows 物理机：**可行路径 = WSL2+Docker Desktop+KVM（本机 CPU 支持虚拟化即可），或干脆只借它的任务与判据、在裸机上用我们的 setup_ps1 重建初始环境（任务初始文件全在公开 repo，可直接镜像，见下）**
- **观测通道对口度**：极高。配置表原话：`--som-origin a11y --a11y-backend uia` = "slower, more accurate accessibility tree"（注意：**是 `--som-origin a11y` + 独立开关 `--a11y-backend win32|uia` 两个参数**，不是 `--som-origin uia`——修正任务书里的记法）。项目页明确 Navi 的 SoM 生成含「**UIA tree parsing** + DOM tree parsing（browser only）」双轨——与我们「UIA 控件 + Playwright DOM」双通道几乎同构；VM 内评测服务源码 `src/win-arena-container/vm/setup/server/uiautomation_utils.py` 直接 `comtypes GetModule('UIAutomationCore.dll')` 操作原生 UIA，`main.py` 注释明确「chrome evals 必须用 uia 后端」
- **判据可否机器化**：可。任务 JSON = `instruction / config / evaluator{func, result, expected} / postconfig`；`func` 分布（一手统计 154 条）：exact_match 46、compare_table 24、**infeasible 13**、check_json_settings 10、is_extension_installed 7、compare_text_file 5、compare_docx_files 5……getter 以 `vm_file`(65)、`vlc_config`(10)、`vm_command_line`(7)、文件/目录/书签/历史/设置类为主——**绝大多数是纯文件系统/注册表/配置/URL 断言，与我们 oracle 十类直接对应**；例外：paint 的 `compare_images`/`is_red_circle_present_on_canvas`（像素级，OpenCV 非 VLM，仍属机器判分）与个别依赖浏览器内部状态的 getter（需按我们 Playwright 通道重写）。postconfig 里偶见「打开目标文件激活窗口再 ctrl+s」的取数前置动作，移植时需改造
- **初始环境可复现性**：任务 setup 的文件下载 URL 全部指向公开 GitHub `https://raw.githubusercontent.com/rogeriobonatti/winarenafiles/...`（实测样本 200 ✓），可离线镜像进我们的裸机资源库——**裸机转写路线成立的关键**
- **语言**：英文指令（个别含中文 UI 目标）。转中文需人工润色（LLM 直译可用但校准要人）
- **负向样本**：13 条 `INF-*` infeasible 任务（实测样本：要求 LibreOffice 实时协同编辑，附社区帖"做不到"出处），`expect_status: infeasible` 语义现成对应我们 report_infeasible 红线评测
- **BYOA**：官方支持自定义 agent（`src/win-arena-container/client/mm_agents/<name>/agent.py` 实现 `predict()/reset()`）——若未来想在它 VM 里直跑 HAJIMI 引擎有正规入口
- **转换工作量（一句话）**：写一个 `waa2seed.py`（字段机械映射 + func 白名单）+ 中文转写 + 双向校准，**首批 60 条机器判据可映射任务约 6–8 人日**（脚本 1 日、逐条 setup/cleanup 改写与校准约 5–7 日）
- **风险注记**：① Win11 eval ISO 90 天过期（golden image 会到期，需重建或改正式镜像许可——未核实具体宽限）；② LibreOffice ≠ MS Office，涉及 `compare_docx_files` 的判据要适配我们裸机上实际装的字处理器；③ 若走「它的 VM + BYOA」路线，等于把 HAJIMI 引擎塞进别人 harness，红线判定层会受限——**不推荐主路线**，主路线选「任务移植到裸机」

### 2.2 OSWorld / OSWorld-Verified / OSWorld 2.0

- **地址**：<https://github.com/xlang-ai/OSWorld>（[arXiv:2404.07972](https://arxiv.org/abs/2404.07972)，NeurIPS 2024）；2.0 独立 repo <https://github.com/xlang-ai/OSWorld-V2>
- **许可证**：均 Apache-2.0（GitHub API）
- **维护状态**：OSWorld 最后 push 2026-08-30（活跃）；OSWorld-V2 最后 push 2026-09-11（很活跃，创建于 2025-10-09）
- **官方 Windows 支持到什么程度（一手）**：主 benchmark = **369 条任务全在 Ubuntu 域**（一手清点 `evaluation_examples/examples/`：chrome 46 / calc 47 / impress 47 / multi_apps 101 / gimp 26 / writer 23 / os 24 / vlc 17 / thunderbird 15 / vs_code 23）。Windows 域 = `examples_windows/` **仅 49 条**（excel 11 / word 9 / ppt 7 / multi_app 22，MS Office 向）+ `test_infeasible.json` 9 条（Ubuntu 应用）。README 环境参数 `os_type: Ubuntu or Windows`，VM 支持 VMware/VirtualBox（裸机）与 **Docker(需 KVM 的 Linux 服务器)**/AWS/Azure——**与我们"一台 Windows 物理机 + 裸机评测"方案的冲突在于：它跑任务需要再起一个 guest VM 并维持桌面内 Python 控制服务，判据 getter 也是 Ubuntu/VM 语义（`/home/user` 路径系）；但 49 条 Windows 子集 + 9 条 infeasible 的任务 JSON（与 WAA 同族）可像 2.1 那样转写到裸机**
- **OSWorld-Verified（2025-07-28 起，官方自身的大修订）**：修社区报告问题、AWS 并行 ≤1 小时、"让 benchmark 信号更有效"（README 更新日志 + [官方博客](https://xlang.ai/blog/osworld-verified)）；官网 <https://os-world.github.io/> 的榜单已换 Verified 口径。**对 HAJIMI 的价值是协议**（任务修订流程、并行复现、报告口径），不是 369 条 Ubuntu 任务
- **OSWorld 2.0 是否已放出可下载（一手）**：**是，但 gated**。108 条长程任务（官方 release manifest `task_hash_manifest.task_count: 108`，<https://github.com/xlang-ai/OSWorld-V2/blob/main/benchmark_releases/osworld-v2-2026.08.08.json>）；任务实现是 **Python task class，放在 HuggingFace gated 数据集 `xlangai/osworld_v2_tasks`**（登录点接受即自动通过），资产在 `xlangai/osworld_v2_assets_gated`；镜像仅 Ubuntu（manifest `provider_images` 只有 ubuntu：qcow2 ~14GB / AWS AMI）；部分任务需 mock 站点与**自托管 GitLab**（README 明说）。防泄漏设计（任务/判据不落公开网页）值得抄，**任务本体与 Windows/中文均不匹配 → 仅协议参照**
- **转换工作量（一句话）**：Windows 49 条同 WAA 转法 ≈2–3 人日；2.0 不建议转

### 2.3 M2AT —— **查无此物**

- 检索轨迹：GitHub 仓库搜索 `M2AT`（全部命中为无关：M2ATNet 图像增强、M2ATS 空管数据集等）；arXiv 标题精确检索无结果；[simular-ai/Agent-S](https://github.com/simular-ai/Agent-S)（Apache-2.0，12.2k⭐，活跃）repo 内仅 `evaluation_sets/test_all.json` 等指向 OSWorld/WAA 的引用，README 与全文无 "M2AT"；Agent S2 论文（[arXiv:2504.00906](https://arxiv.org/abs/2504.00906)）全文 HTML grep 无 "M2AT"，评测基准=OSWorld+WAA+AndroidWorld；Simular 官方博客（Agent S 破人类基线新闻稿）亦无此名
- **结论**：不存在名为 M2AT 的公开桌面多步任务 benchmark。最可能的混淆源：① Agent-S 系列论文对 OSWorld/WAA 子集的评测口径；② 浏览器多跳数据集 [MMInA](https://arxiv.org/abs/2501.01776)（名字形似，未展开核实）；③ WindowsWorld 的 "multi-app multi-step" 描述。**后人勿再按此名调研**。判据问题（VLM judge？）不适用

### 2.4 UFO² / UFO³（microsoft/UFO）

- **地址**：<https://github.com/microsoft/UFO>；许可证 MIT；9.7k⭐；最后 push 2026-09-11（活跃，现为「UFO³ Galaxy」多设备编排框架）
- **它跑 WAA 之外有没有自己发布任务集（一手）**：**没有**。`ufo/README.md` 评测节明确仅两项：WAA 154 任务 + "OSWorld (Windows)" 49 跨应用任务，且集成放在独立 fork `nice-mee/WindowsAgentArena`（含 `2020-qqtcg/osworld` 分支）；v1 论文（[arXiv:2402.07939](https://arxiv.org/abs/2402.07939)）为"9 款常用 Windows 应用 + 用例研究"，未发布可下载任务集
- **但有一个直接可用的情报**：其 [WAA 部署文档](https://microsoft.github.io/UFO/ufo2/evaluation/benchmark/windows_agent_arena/) 注明 **`test_full.json` = "UIA 可用的全部用例"清单，`test_all.json` = 全部（含 UIA 不可用）**，并给出 WSL 下建镜像→golden image→`run-local.sh --mode dev --agent UFO` 全流程——**我们做 WAA 移植时可以直接拿它的 fork 里的 UIA 子集清单当白名单起点**（清单本体在 nice-mee fork，未逐条核对 → 存疑清单）
- **定位**：同类实现/外部基线来源（它的 WAA 成绩可做数字对标），**不是任务集**。UFO² 在 WAA 上的具体分数未在本次会话核实 → 存疑清单

### 2.5 「WindowsBench」及其他 Windows GUI benchmark 搜索轮

- **「WindowsBench」：查无此物**（GitHub 仓库搜索仅命中 2016 年硬件跑分脚本；无同名 agent benchmark）。易混真实项：
  - **WindowsWorld（HITsz-TMG/WindowsWorld，ACL 2026 Findings，[arXiv:2604.27776](https://arxiv.org/abs/2604.27776)）**：<https://github.com/HITsz-TMG/WindowsWorld>，Apache-2.0，21⭐，2026-04 创建、最后 push 2026-05-11。一手核实：**181 条任务、17 款 Windows 应用、4 难度级（L1–L4 = 39/80/50/12）、77.9% 多应用任务、平均 4.97 个中间检查点**；`benchmark.json`（531KB，公开在 repo 根，实测已下载）每条含 `instruction` + **`instruction_cn`（原生中文指令！）** + persona + pre_conditions + files_to_create + evaluation_metrics（success_criterion/intermediate_checks 纯文本）；环境 = **Windows 宿主机 + VMware Workstation Pro（vmrun）**，Win10/11/Server 支持。**判分方式（读 `hf_run.py` 源码）= 把轨迹截图+动作喂给 qwen3-vl-plus 的 VLM judge（中文评审 prompt）**→ 触我们"禁 LLM judge/截图判分"红线；**唯一例外：L4 的 12 条 = 判"agent 是否主动 FAIL/拒绝"的不可行任务（机制机器化）**。定位：**借任务文本（尤其中文指令语料、多应用 persona 场景设计）与 L4 设计，绝不借判据**；转换时判据要在我们 oracle 体系里重写
  - **macOSWorld**（[arXiv:2506.04135](https://arxiv.org/abs/2506.04135)）：202 条 macOS 任务，**任务指令与系统界面提供 5 种语言（英/中/阿/日/俄）**，含专门的安全（欺骗攻击）子集。macOS 域，与我们执行环境不匹配 → 存档参照（多语言任务模板 + 安全子集设计）
  - 其余搜索命中的 "Windows" 系（WindowsAgentArena 复刻/分支、UI-Venus/OS-Atlas 类 grounding 集）不重复合入

### 2.6 WebArena / VisualWebArena / WebArena-Verified（browser_* 通道相关）

| 项 | WebArena | VisualWebArena | **WebArena-Verified** |
|---|---|---|---|
| 地址 | [web-arena-x/webarena](https://github.com/web-arena-x/webarena) | [web-arena-x/visualwebarena](https://github.com/web-arena-x/visualwebarena) | [ServiceNow/webarena-verified](https://github.com/ServiceNow/webarena-verified) |
| 论文/出处 | ICLR'24 | ACL'24 | NeurIPS'25 SEA Workshop（[页面](https://neurips.cc/virtual/2025/loc/san-diego/124576)，作者 Elhattami/Thakkar/Chapados/Pal = ServiceNow Research/Mila） |
| 许可证 | Apache-2.0 | 未核实（同族推测 Apache-2.0） | Apache-2.0 |
| 维护 | 最后 push 2025-11-26 | 2024-11-09（停滞） | 2026-03-08，活跃（PyPI/uvx/docker 分发） |
| 任务量 | **812**（README 原文） | **910**（README 原文） | 812 全量审计修订 + **Hard 子集 258**（repo README 2026 口径；workshop 摘要写 137——以 repo 为准，差异进存疑清单） |
| 环境 | **自托管 docker**：shopping/shopping_admin(CMS)/reddit 论坛/gitlab/map/wikipedia 一站一镜像（2026-02 起有官方"小 92%"优化镜像、map 单容器），需 KVM 的 docker 主机；无外网限制可跑（站点全本地） | 同上 + classifieds 等 | 复用 WA 容器栈 + `env-ctrl` 状态管理 |
| 判据 | program_html/url_match/must_include 等机器断言 **+ 部分 fuzzy LLM 判分** | 含图像相似度/视觉判分需求 | **确定性判分：移除 LLM-judge 与子串匹配，类型感知归一化比较、状态变更任务校验后端（网络轨迹/数据库）状态、JSON schema+显式 status code、模板宏平均+95%CI+失败分型** |
| 纯文本可达 | 大体可达（官方支持 a11y-tree 文本观测；"多少条纯文本可达"未核实） | **不可**：任务设计即多模态 | 同 WebArena |
| 对 HAJIMI | 任务与站点数据强绑定（判据引用其 gitlab/shopping 内部状态），**整体搬进我们 Playwright 通道 = 自托管整套 LAMP 栈，成本高**；可选"借协议+少量移植" | 不适配（模型无视觉） | **核心采用（协议档）**：oracle 比较器设计、status code、CI/失败分型直接指导我们 browser_* 任务 oracle 与 `report.py` 报表 |

- **转换工作量（一句话）**：不搬 812 任务；**把 Verified 的 comparator 思路落进我们 oracle_eval.py 的 browser 类判据 ≈2–3 人日**；若未来想跑 Verified 本体，需 Linux+KVM 机器 + `uvx webarena-verified` + 自托管站点（单机 WSL2 理论可行，重）

### 2.7 浏览器类其余候选（各一句判定）

| 候选 | 判定 | 依据（一手位置） |
|---|---|---|
| [Mind2Web](https://github.com/OSU-NLP-Group/Mind2Web)（MIT，1,026⭐，pushed 2025-11） | **不适配**：离线静态标注数据集（2,000+ 任务/137 站点/31 域，评 element/step 选择匹配），非可执行环境，判分是集合匹配非状态 oracle；真实站点已漂移 | README 原文 + arXiv |
| [Multimodal-Mind2Web](https://huggingface.co/datasets/osunlp/Multimodal-Mind2Web)（HF，即 MM-Mind2Web） | **存而不用**：Mind2Web 加截图/坐标标注的视觉版 → 归入 2.8 grounding 档（具体规模数字未核实） | HF 数据集卡 |
| [WebVoyager](https://github.com/MinorJerry/WebVoyager)（Apache-2.0，1,124⭐，2024-03 停滞） | **不适配**：643 任务打在 15 个**真实在线网站**（时序敏感需手工改时间），判分 = **GPT-4V 截图打分**——在线漂移+VLM judge 双踩红线 | README 原文 |
| [AssistantBench](https://github.com/oriyor/assistantbench)（许可 NOASSERTION）+ [HF 数据集](https://huggingface.co/datasets/AssistantBench/AssistantBench) | **不适配**：214 条开放式**答案匹配**型网络研究任务（评"找答案"非"执行操作"），依赖开放网页，英文 | [arXiv:2407.15711](https://arxiv.org/abs/2407.15711) 摘要（214 tasks） |
| [BrowseComp](https://github.com/openai/simple-evals)（MIT，4,630⭐，2026-04） | **不适配**：1,266 道**短答案**难题检索（[arXiv:2504.12516](https://arxiv.org/abs/2504.12516)），QA 判分，评 deep-research 非操作执行 | repo README + 摘要 |
| [BrowseComp-Plus](https://github.com/texttron/BrowseComp-Plus)（MIT，356⭐，2026-05） | **不适配**：固定 ~100K 语料的检索问答变体（离线语料是其卖点），**判分 Qwen3-32B LLM judge** | README 原文 |
| [MiniWoB++](https://github.com/Farama-Foundation/MiniWoB-plusplus)（MIT，400⭐，2026-08 维护中） | **不适配**：100+ 合成网页小任务（RL 玩具级、模板英文指令、Chrome 专用），与真实桌面/浏览器工作流差距过大；程序化 reward 是它唯一可敬处 | README 原文 |
| [WorkArena / WorkArena++](https://github.com/ServiceNow/workarena)（许可 NOASSERTION，271⭐，2026-04） | **不适配**：33 原子任务×19,912 实例 / ++682 组合任务，全部打在 **ServiceNow 企业 SaaS 实例**上（实例经 HF gated 发放）；判据虽程序化（BrowserGym 断言），但依赖商业平台，与单机桌面场景无关 | README 原文 |

### 2.8 Grounding 类（ScreenSpot / ScreenSpot-Pro / MM-Mind2Web）——存而不用

- [ScreenSpot](https://github.com/njucckevin/SeeClick)（SeeClick 附属，Apache-2.0，494⭐）：网页/桌面/移动三域真实 GUI 元素定位集（具体条数本次未核实）。[arXiv:2401.10935](https://arxiv.org/abs/2401.10935)
- [ScreenSpot-Pro](https://github.com/likaixin2000/ScreenSpot-Pro-GUI-Grounding)（MIT，396⭐，2026-06；[HF likaixin/ScreenSpot-Pro](https://huggingface.co/datasets/likaixin/ScreenSpot-Pro)）：**高分辨率专业桌面**（23 应用 × 5 行业 × 3 OS），专家标注；已成年 grounding 事实标准（Omniparser v2/Qwen2.5-VL/UI-TARS 引用，见 README）。条数具体值未核实
- **若未来接视觉专家（P2-2.3 坐标逃生舱/截图 OCR）**：评「指令→bbox 命中率」；与 HAJIMI 端到端指标是两层（组件 vs 任务），届时以单测形式挂 `models/` 下跑，**现在只需存档这三件套的名字+链接，不做任何接入**。当前 deepseek-chat 无视觉，任何 grounding 分数都无法归因进 All-Pass 体系

### 2.9 中文桌面评测集：**空白**（认真搜过）

检索轮：GitHub `chinese GUI agent benchmark` / `desktop agent benchmark windows chinese`（total_count=0）/ 中文词「GUI评测」「桌面 评测集 Windows 中文」/ 知乎-资讯向中文报道（结果全是国产 agent **打榜** OSWorld/WAA 的新闻：实在 Agent、Mano、UI-Mate 等，无自建公开桌面评测集发布）。
最接近的三个（都不构成"中文桌面 benchmark"）：① **WindowsWorld** 的 `instruction_cn` 双语列（中国团队做的 Windows 桌面集，但判分 VLM judge）；② **macOSWorld** 的中文任务列（macOS 域）；③ **UniClawBench** 200 条中文任务（Linux docker 内 GUI+工具工作流、隐藏 LLM supervisor 判分，[arXiv:2607.08768](https://arxiv.org/abs/2607.08768)，HKU MMLab，MIT，39⭐，2026-07 活跃）。
**结论（可放心写进宣传）**：中文、Windows、机器判据三条件交集的公开评测集目前为空白；HAJIMI 自研 20 条 + 本方案移植 WAA 中文化子集即填补该生态位，无外部集可直接引用对标。

### 2.10 负向/infeasible 与「该问用户」样本盘点

| 来源 | 现成量 | 形态 | 可用性 |
|---|---|---|---|
| WAA | **13 条** `INF-*` | `evaluator.func="infeasible"`，agent 声明不可行即满分（机器判） | 直接转写（含任务文件出处链接），喂我们 report_infeasible 红线评测 |
| OSWorld | **9 条** `test_infeasible.json` | 同族机制（Ubuntu 应用） | 指令可中文化移植，环境步骤需改造 |
| WindowsWorld | **12 条 L4** | 判"末动作是否 FAIL"（L4 不跑 judge，见 `hf_run.py` 源码） | 机制太粗（不判理由），借任务不借判分 |
| macOSWorld | 专门安全子集（欺骗攻击） | 数量未核实 | 存档思想 |
| **「该问用户/ask_user」** | **公开桌面 benchmark 中未发现现成任务样本**（多轮追问类最接近 = UniClawBench 的 public user simulator 协议：supervisor 判 continue 后由模拟器生成追问——是评测协议不是数据集） | — | **红线评测的 ask_user 任务仍需自研**；UniClawBench 三角色（executor/hidden supervisor/user-simulator）设计值得借鉴 |

### 2.11 安全/注入类 benchmark（红线层一句话适配度）

- [InjecAgent](https://arxiv.org/abs/2403.02691)（[uiuc-kang-lab/InjecAgent](https://github.com/uiuc-kang-lab/InjecAgent)，168⭐，2024 停滞）：1,054 例间接提示注入（17 用户工具×62 攻击工具，direct-harm/data-exfil 两类意图）——**工具层注入的题库范式可直接搬**进我们"文件内容/剪贴板/网页文本里藏指令→期望拒绝/上报"的红线任务；环境本体（模拟工具 API）不跑
- [AgentDojo](https://github.com/ethz-spylab/agentdojo)（MIT，819⭐，2026-06 维护）：动态沙箱（邮件/文件/银行等场景）评注入攻击效用-代价曲线——**场景构造思路（合法任务+注入内容+既完成率又拒攻击的双指标）适合我们红线层的指标设计**
- [AgentHarm](https://arxiv.org/abs/2410.09024)（UK AISI；GitHub 官方 repo 未检索到——404，数据在 [HF ai-safety-institute/AgentHarm](https://huggingface.co/datasets/ai-safety-institute/AgentHarm)）：110 条（增广 440）恶意 agent 任务，评"该不该拒绝"+"越狱后能力保持"——**"合规拒绝"题库思想可参考，桌面环境不匹配**
- 总评：**都属"未来红线层"候选，现阶段零接入**；三家共同点=无 Windows UIA 环境，注入载体多为工具返回/网页/文件，恰好提示我们：红线样本要埋进 setup_ps1 产出的文件/剪贴板/本地页面里

---

## 3. 推荐的落地组合

**主 = WindowsAgentArena 抽 60–80 条转写中文版进 seed.json（估 6–8 人日）**
**+ 次 = 借 WebArena-Verified 判分协议升级 oracle_eval/report（估 2–3 人日）与 OSWorld(-2.0) 的版本钉死/修订协议**
**+ 再借 = WAA 13 条 + OSWorld 9 条 infeasible 负向样本（≈1 人日并入红线评测批）**
**+ 仅存档待 P2 = ScreenSpot(-Pro)/MM-Mind2Web（视觉专家）+ macOSWorld/UniClawBench（多语言与追问协议）+ WindowsWorld 中文指令语料（judge 弃用）**

外部可比性话术（写论文/答辩用）：移植任务保留原 task id 可溯源，A/B 基线回测在转写版上进行；同时报「我们在 WAA 原版 VM 上 BYOA 跑的 Navi 对标数字」——**可选，不阻塞主计划**。

### 转换第一步：具体操作命令草案

```bash
# 0) 拉 WAA 任务源（浅克 + 稀疏只取任务与评测器源码，<10MB 级）
git clone --depth 1 --filter=blob:none --sparse \
  https://github.com/microsoft/WindowsAgentArena.git waa
cd waa
git sparse-checkout set \
  src/win-arena-container/client/evaluation_examples_windows \
  src/win-arena-container/client/desktop_env/evaluators

# 1) 镜像任务初始文件（下载步骤全指向公开 winarenafiles，已实测可达）
grep -rhoE 'https://raw\.githubusercontent\.com/rogeriobonatti/winarenafiles/[^"]+' \
  src/win-arena-container/client/evaluation_examples_windows/examples/*.json \
  | sort -u > /tmp/waa_files.txt
aria2c -i /tmp/waa_files.txt -d ../eval_vendor/waa/ -x4 || xargs -n1 curl -sLO < /tmp/waa_files.txt

# 2) 生成候选清单：只保留 oracle 函数在白名单内、可映射我们十类判据的任务
#    （白名单按 2.1 的一手统计挑选）
python tools/waa2seed.py \
  --in src/win-arena-container/client/evaluation_examples_windows/examples \
  --func-whitelist exact_match,compare_text_file,vm_file_exists_in_vm_folder,check_json_settings,infeasible,... \
  --category-blacklist libreoffice_* \
  --out ../eval_vendor/waa/candidates.json
# 首批预估：notepad2+calc3+explorer19+settings5+clock4+paint3+vscode24 中可映射者≈45–60 条（含 13 负向）

# 3) waa2seed.py 的映射规则（半天工作量）：
#    instruction        → instruction（英文保留 + 中文转写列，人工过一遍量词/路径）
#    config.download    → setup_ps1: Invoke-WebRequest 本地镜像 + Copy-Item 到位
#    config.launch/open → requires + 指令内「打开记事本」类步骤交 agent 自己做
#    evaluator{func,result,expected} → oracle.all：
#      vm_file+exact_match(内容)      → file_content_equals / _contains
#      vm_file_exists_in_vm_folder    → file_exists / file_glob_min_count
#      settings/注册表类 getter       → registry_value（读 getter 源码逐个核对）
#      func=infeasible                → expect_status: infeasible + oracle: {}（红线通道判）
#    seeds: 我们补 {seed} 占位；双向校准协议不变（真做→PASS / 故意做错→FAIL）
```

随后按《评测方案》双向校准流程逐条 `calibrated:true`，进入 A/B 回测池。

---

## 4. 明确的不适配清单（防后人重复调研）

| 名称 | 一句原因 |
|---|---|
| **M2AT** | 查无此物（GitHub/arXiv/Simular 全部一手材料零命中，见 §2.3；勿再搜） |
| **WindowsBench** | 查无此物（无同名 benchmark；搜索常混向 WindowsWorld / WindowsAgentArena） |
| OSWorld 主集 369 条 | Ubuntu 域（GIMP/VLC/LibreOffice/Thunderbird 路径系判据），Windows 仅 49 条且需 guest VM + VM 内控制服务，与单机裸机方案冲突 |
| OSWorld 2.0 | Ubuntu 域 + gated Python 任务类 + 自托管 GitLab/mock 站，重环境长程 QA 流，非我们平台（协议参照见 §3） |
| VisualWebArena | 任务与判分即多模态（截图/图像相似），纯文本模型不可达 |
| Mind2Web（含 i/MT/Online 变体） | 离线轨迹静态打分，不是可执行环境，无状态 oracle |
| WebVoyager | 真实在线站点（时序漂移）+ GPT-4V 截图判分，双踩红线 |
| AssistantBench | 答案匹配型开放网络研究任务，评检索不评操作 |
| BrowseComp / BrowseComp-Plus（含 BrowseComp-ZH 思路） | 短答案 deep-research QA；Plus 固定语料但 LLM judge——与桌面执行无关 |
| MiniWoB++ | 合成网页玩具小任务（RL 传统），无真实应用与 OS 状态 |
| WorkArena / WorkArena++ / BrowserGym | 全部任务绑定 ServiceNow 企业 SaaS 实例（gated 发放），商业平台依赖 |
| UFO² / UFO³ / Agent-S | 是 agent 框架/同类实现不是公开任务集（UFO 官方评测复用 WAA+OSWorld-Windows，见 §2.4）；价值=同类基线数字与架构对标 |
| ScreenSpot / ScreenSpot-Pro / MM-Mind2Web | 组件级 grounding 点击精度（bbox 命中），非端到端任务评测；当前无视觉模型可评——**存档待 P2-2.3，不是现在接入** |
| InjecAgent / AgentDojo / AgentHarm | 注入载体为工具 API/邮件/网页模拟，无 Windows UIA 环境；现阶段零接入，仅红线层未来借题库与双指标设计 |

---

## 5. 未核实/存疑清单

1. **WAA 侧**：`--gpu-enabled` 之外的 CPU 最低实配（README 未给数字）；Win11 eval ISO 90 天到期对 golden image 的实际影响（社区常见、本次未见官方说明）；官方最新 leaderboard 各 agent 数字（未拉取）；UFO fork `test_full.json`（UIA 可用清单）的具体任务数（在 `nice-mee/WindowsAgentArena`，未逐条核对）
2. **WebArena-Verified**：Hard 子集 137（NeurIPS 摘要）→ 258（2026 repo README）演变原因未考证；"纯文本可达任务占比"无官方统计（自行分析 getter 依赖前的结论：未核实）
3. **UFO² 在 WAA 的分数**（网传 62%+ 级）：未在 UFO 仓库 README 与本次抓取中见到硬数字 → 未核实
4. **ScreenSpot(-Pro) 精确条数**（1,200?/1,000+/800?）：数据集卡与摘要只给维度（23 应用×5 行业×3 OS），数字未核实；MM-Mind2Web 规模同理
5. **VisualWebArena 许可证**（API 未拉到，同族推测 Apache-2.0）；**WorkArena/AssistantBench** GitHub API 报 NOASSERTION，LICENSE 原文未读
6. **macOSWorld 安全子集条数**、**UniClawBench GUI 任务的运行平台细节**（Zotero/Obsidian 任务见 Linux 桌面容器迹象，未全量核实）
7. **OSWorld Windows guest 在 VMware-on-Windows 宿主的现行官方支持度**（README 有 VMware/VirtualBox 分支但主要示例 Docker/Linux 优先；未实机验证）
8. **AgentHarm 的 GitHub 官方仓库地址**（API 探测 404，HF 数据集存在；可能改放 HF 或私有）
9. 本次 GitHub API 偶发 SSL 中断，个别仓库 star/pushed 数字为抓取时刻（2026-09-13）快照，会漂移

---

### 附：本调研引用的主要一手来源

- WAA：repo <https://github.com/microsoft/WindowsAgentArena>（README、`evaluation_examples_windows/examples/*`、`desktop_env/evaluators/*`、`vm/setup/server/uiautomation_utils.py`、`docs/Develop-Tasks.md`）、[arXiv:2409.08264](https://arxiv.org/abs/2409.08264)、项目页 <https://microsoft.github.io/WindowsAgentArena/>
- OSWorld：<https://github.com/xlang-ai/OSWorld>（README、`evaluation_examples/{test_all,test_infeasible}.json`、`examples_windows/`）、[arXiv:2404.07972](https://arxiv.org/abs/2404.07972)、[官方博客 OSWorld-Verified](https://xlang.ai/blog/osworld-verified)
- OSWorld 2.0：<https://github.com/xlang-ai/OSWorld-V2>（`benchmark_releases/*.json`、README）、HF `xlangai/osworld_v2_tasks`
- UFO：<https://github.com/microsoft/UFO>（README、`ufo/README.md` 评测节、[WAA 文档](https://microsoft.github.io/UFO/ufo2/evaluation/benchmark/windows_agent_arena/)）、[arXiv:2402.07939](https://arxiv.org/abs/2402.07939)、[arXiv:2504.14603](https://arxiv.org/abs/2504.14603)
- WindowsWorld：<https://github.com/HITsz-TMG/WindowsWorld>（README、`benchmark.json`、`hf_run.py` 的 `evaluate()`）、[arXiv:2604.27776](https://arxiv.org/abs/2604.27776)
- WebArena：<https://github.com/web-arena-x/webarena>；VWA：<https://github.com/web-arena-x/visualwebarena>；WAV：<https://github.com/ServiceNow/webarena-verified> + [NeurIPS'25 页面](https://neurips.cc/virtual/2025/loc/san-diego/124576)
- Agent-S：<https://github.com/simular-ai/Agent-S> + [arXiv:2504.00906](https://arxiv.org/abs/2504.00906)；Simular 博客 <https://www.simular.ai/articles/simulars-computer-use-agent-outperforms-humans>
- 其余：各表内逐条链接
