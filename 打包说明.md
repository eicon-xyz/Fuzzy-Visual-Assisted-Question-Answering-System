# HAJIMI L5 源码交付包 — 打包说明

> 方案一：全栈源码 zip + 双 venv 安装脚本  
> 支持 **L5 自动执行**（B 端 + L5 Sidecar，唯一模式；旧 A 端 :8010 与 OmniParser 已移除）

---

## 1. 目录要求

解压后须保持如下结构（`HAJIMI_UI` 与 `server_A` **同级**）：

```
HAJIMI-release/
├── 安装全栈.bat
├── 启动全栈.bat
├── 验收.bat
├── 打包说明.md
├── HAJIMI_UI/          # B 端 PyQt5 桌面应用
├── server_A/           # L5 Sidecar :8011（必须，唯一后端）
├── client/             # C 端：语音 ASR/TTS + 审计代理 + B-C 桥接
├── web-admin/          # Vue3 管理面板源码（需 npm install）
├── models/             # Vosk 离线中文语音模型
└── launchers/          # 各启动快捷方式
```

---

## 2. 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 x64 |
| Python | 3.12+（安装时加入 PATH） |
| 内存 | 建议 8 GB+（L5 依赖含 PyTorch） |
| 网络 | 首次安装需 pip 下载依赖 |
| LLM | 文本对话 API Key（L5 必需；默认 DeepSeek `deepseek-chat`） |
| 浏览器 | Edge 或 Chrome（L5 网页自动化） |

---

## 3. 首次安装

1. 解压 zip 到任意目录（路径尽量不含中文空格）
2. 双击 **`安装全栈.bat`**
   - 自动创建两个 venv：B 端（`HAJIMI_UI\.venv`，仅 PyQt5）、L5 Sidecar（`server_A\server\.venv`，8011）
   - L5 venv 安装较慢（约 10–30 分钟），请耐心等待
3. 编辑 **`server_A\server\.env`**（模型 key 唯一存放处），填入：
   ```ini
   DEEPSEEK_API_KEY=你的密钥
   DEEPSEEK_MODEL=deepseek-chat
   ```
   （或改用 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` 指向任意 OpenAI 兼容文本模型）
4. 安装脚本会自动写入 L5 设置（`OMNIPARSER_ENABLED=false`、`ROUTING_MODE=l5`）；也可先跳过第 3 步，启动后在 B 端设置页填 key，保存时会自动同步进该 `.env` 并重启 Sidecar

---

## 4. 启动

双击 **`启动全栈.bat`**（内部 `HAJIMI_UI\scripts\start_release_fullstack.bat`），将依次打开两个控制台窗口：

| 窗口标题 | 端口 | 作用 |
|----------|------|------|
| HAJIMI-L5-Sidecar | 8011 | L5 规划 + 自动执行（UIA 控件绑定 + Playwright DOM） |
| HAJIMI-B-end | — | PyQt5 桌面 UI |

模式说明：**L5 自动执行是唯一模式**，无需切换路由（旧 `auto/fast/balanced/precision` routing 与 UI 部署模式切换已随 L4 移除）；B 端「系统设置」保留「L5 自动执行」开关与桌面授权、快捷键（H 批准 / P 暂停 / J 停止）。

---

## 5. 验收

先 **`启动全栈.bat`**，再双击 **`验收.bat`**（内部调用 `verify_all.bat --require-l5`）。

通过标准：

- `check_ui_env` — B/Sidecar 环境就绪
- `verify_bc_signals` / `verify_settings_fragment` / `verify_theme_apply` — B 端回归
- `verify_l5` — 8011 `/execute`、`/stream`、`/cancel`、`/health` 端点

手动 E2E：

1. B 端输入指令 → L5 知情确认 → 观察步骤时间线与本机自动操作（UIA 点击/输入、Playwright 网页）
2. 停止：`stop_all.bat`（只停 :8011）

---

## 6. 常见问题

| 现象 | 处理 |
|------|------|
| `找不到 server_A` | 确认 `server_A` 与 `HAJIMI_UI` 同级解压 |
| L5 venv 验证失败 | `cd server_A` → `scripts\repair_l5_venv.bat` |
| `fastapi._compat` 报错 | 同上，重建 L5 venv |
| L5 不执行 | 确认 HAJIMI-L5-Sidecar（:8011）窗口正常；`curl http://127.0.0.1:8011/api/demo/health` |
| 无 LLM Key | 编辑 `server_A\server\.env` 后重启 Sidecar（或 B 端设置页保存，自动同步+重启） |
| L5 网页任务失败 | 安装 Edge/Chrome，或 `playwright install chromium` |
| 端口占用 | `HAJIMI_UI\scripts\stop_all.bat`（只停 :8011） |

---

## 7. 制作 zip（开发者）

在仓库根目录：

```bat
打包.bat
```

输出：`dist\HAJIMI-release-YYYYMMDD.zip`

排除项：`.venv/`（2 处）、`**/.env`（含密钥，保留 `.env.example` 模板）、`__pycache__/`、`node_modules/`

---

## 8. 与开发环境的区别

本交付包**仅新增**根目录启动脚本与 `HAJIMI_UI\scripts\start_release_fullstack.bat`，**不修改**现有业务代码。

与 `启动本地.bat`（开发用，内部 `start_local_vision.bat`，日志落盘）的差异：

- 两者都只启动 **L5 Sidecar（:8011）+ B 端**（唯一模式）
- `启动全栈.bat` **必须**等待 L5 Sidecar `:8011` 健康检查通过再拉起 B 端（交付保证）
- 不存在 Mock 回退路径（Mock 后端已随 L4 移除）

---

## 9. C 端（语音 ASR/TTS + 审计代理）

C 端提供语音交互和离线审计上报。源码在 `client/` 目录，Vosk 中文语音模型在 `models/vosk-model-small-cn-0.22/`。

### 首次配置

```bash
pip install -r client/requirements.txt
```

### 验证语音模块

```bash
python client/voice_setup.py
```

### B-C 信号联调

需 B 端 UI 已启动：

```bash
python client/bc_adapter.py
```

### 审计队列

本地 SQLite 队列存储在 `%LOCALAPPDATA%/HAJIMI/audit_queue.db`，首次运行自动创建。离线时缓存，恢复网络后批量上报 L5 Sidecar `:8011`。

---

## 10. Web 管理面板

Vue3 + Element Plus + ECharts，源码在 `web-admin/`（不含 `node_modules`）。

### 首次启动

```bash
cd web-admin
npm install
npm run dev          # 开发模式 http://localhost:5173
```

或生产构建：

```bash
npm run build        # 输出 web-admin/dist/
```

### 注意

Admin 面板依赖 L5 Sidecar `:8011` 挂载的 `/api/admin/*`（admin/audit/auth/flow/monitor/users）端点，经 vite proxy 转发；个别报表数据仍为 Mock，待 Sidecar 对应端点补齐后切换。

---

## 11. 已知疏漏清单

以下为历史进度文档（`HAJIMI_UI/docs/ABC-完整调试距离与分工清单.md` 及各成员 DAY5/6 文档）摘录，**2026-09 L5 重构后已按现状清理**：原 P0「环境搭建 / execute_worker 改连 :8011 / l5_sidecar_launcher 自动拉起 / C 端 server_url 统一（现为 :8011）」等项已完成并移除；L4/OmniParser/澄清弹窗（`/clarify`）相关条目随 L4 指引模式一并删除。

### P1 — 影响演示

| 模块 | 疏漏 | 责任人 |
|------|------|--------|
| Web 面板 | 个别报表仍为 Mock 数据，切换到 Sidecar `:8011` 真实 admin 端点 | C |
| 审计补传 | 离线队列恢复后自动补传 | C |

### P2 — 下个迭代

| 模块 | 疏漏 | 责任人 |
|------|------|--------|
| PyInstaller | 单文件 exe（含 Vosk 模型路径） | B |
| L2 快路径 | 离线模板 < 3s 验收 | A |
| 监控 | Admin `flow.py`/`monitor.py` 切换到真实数据 | A |
| macOS | PyQt 透明窗口兼容（已知限制） | B |

### P3 — 长期优化

| 模块 | 疏漏 |
|------|------|
| 日志持久化 | structlog + 文件轮转 + task_id 追踪 |
| PostgreSQL | SQLite → PostgreSQL 生产迁移（Alembic 已备） |
| 流式输出 | LLM streaming SSE 响应（`client.py` 已预留接口） |

### 数据存储位置汇总

| 数据 | 路径 | 管理方 |
|------|------|--------|
| 主数据库（7 表） | `server_A/data/hajimi.db`（Sidecar 首次启动建表） | A |
| C 端审计队列 | `%LOCALAPPDATA%/HAJIMI/audit_queue.db` | C |
| 用户设置 | `%LOCALAPPDATA%/HAJIMI/user_settings.json` | B |
