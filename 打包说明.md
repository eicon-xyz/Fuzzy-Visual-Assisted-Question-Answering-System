# HAJIMI L4+L5 源码交付包 — 打包说明

> 方案一：全栈源码 zip + 三 venv 安装脚本  
> 支持 **L4 Vision 指引** 与 **L5 自动执行**（非 Mock 演示）

---

## 1. 目录要求

解压后须保持如下结构（`HAJIMI_UI` 与 `server_A` **同级**）：

```
HAJIMI-release/
├── 安装全栈.bat
├── 启动全栈.bat
├── 验收.bat
├── 打包说明.md
├── HAJIMI_UI/          # B 端 + A 端 :8010
├── server_A/           # L5 Sidecar :8011（必须）
├── client/             # C 端：语音 ASR/TTS + 审计代理 + B-C 桥接
├── web-admin/          # Vue3 管理面板源码（需 npm install）
├── models/             # Vosk 离线中文语音模型
└── launchers/          # 各启动快捷方式
```

---

## 2. 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 x64 |
| Python | 3.12+（安装时加入 PATH） |
| 内存 | 建议 8 GB+（L5 依赖含 PyTorch） |
| 网络 | 首次安装需 pip 下载依赖 |
| LLM | Vision 多模态 API Key（L4/L5 必需） |
| 浏览器 | Edge 或 Chrome（L5 网页自动化可选） |
| OmniParser | 可选；precision/inspect 及完整视觉检测需要 |

---

## 3. 首次安装

1. 解压 zip 到任意目录（路径尽量不含中文空格）
2. 双击 **`安装全栈.bat`**
   - 自动创建三个 venv：B 端、A 端 8010、L5 8011
   - L5 venv 安装较慢（约 10–30 分钟），请耐心等待
3. 编辑 **`HAJIMI_UI\server\.env`**，填入：
   ```ini
   LLM_API_KEY=你的密钥
   LLM_BASE_URL=https://api.siliconflow.cn/v1
   LLM_MODEL=Qwen/Qwen2.5-VL-72B-Instruct
   ```
   （模型名按你的供应商调整，须支持 Vision）
4. 安装脚本会自动将关键配置同步到 **`server_A\server\.env`**（端口改为 8011）

---

## 4. 启动

双击 **`启动全栈.bat`**，将依次打开四个控制台窗口：

| 窗口标题 | 端口 | 作用 |
|----------|------|------|
| HAJIMI-A-end | 8010 | L3/L4 规划、inspect |
| HAJIMI-L5-Sidecar | 8011 | L5 自动执行（pyautogui） |
| HAJIMI-B-end | — | PyQt5 桌面 UI |
| （可选）GPU 隧道 | 9800 | OmniParser 远程 GPU |

### 路由模式（在 UI 设置中切换）

| 模式 | 设置值 | 功能 |
|------|--------|------|
| L4 指引 | `fast` / `balanced` / `auto` | Vision LLM 生成步骤 + 屏幕标注 |
| L5 自动执行 | `l5` | 需开启「L5 自动执行」并授权 |

**注意**：不要使用 `HAJIMI_MOCK_ONLY=1`，否则 L5 会被拒绝。

---

## 5. OmniParser（三选一）

| 方式 | 配置 | 适用 |
|------|------|------|
| **GPU 隧道**（推荐） | `OMNIPARSER_URL=http://127.0.0.1:9800` + SSH 隧道 | 校园 GPU / 远程 OP |
| **本地 CPU** | `OMNIPARSER_URL=http://127.0.0.1:8002` + `scripts\setup_omniparser.bat` | 无 GPU，速度慢 |
| **纯 L4 Vision** | `DETECTOR_BACKEND=vision_llm` | 仅 L4，不依赖 OmniParser |

无 GPU 隧道时，`启动全栈.bat` 会警告但继续启动；L4 Vision 仍可用，precision/inspect 受限。

---

## 6. 验收

先 **`启动全栈.bat`**，再双击 **`验收.bat`**（内部调用 `verify_all.bat --require-a`）。

通过标准：

- `verify_l4` — A 端 L4 健康检查
- `verify_l5` — 8011 `/execute` 端点
- `verify_integration` — 8010 全链路

手动 E2E：

1. 设置 → `fast` → 输入任务 → 观察覆盖层标注（L4）
2. 设置 → `l5` → 开启自动执行 → 观察时间线与本机操作（L5）

---

## 7. 常见问题

| 问题 | 处理 |
|------|------|
| `找不到 server_A` | 确认 `server_A` 与 `HAJIMI_UI` 同级解压 |
| L5 venv 验证失败 | `cd server_A` → `scripts\repair_l5_venv.bat` |
| `fastapi._compat` 报错 | 同上，重建 L5 venv |
| A 端未连接 | 检查 8010 窗口；`HAJIMI_UI\scripts\start_server.bat` |
| L5 不执行 | 确认 8011 窗口正常；路由须为 `l5` |
| 无 LLM Key | 编辑 `server\.env` 后重启 A 端与 L5 |
| L5 网页任务失败 | 安装 Edge/Chrome，或 `playwright install chromium` |
| 端口占用 | `HAJIMI_UI\scripts\stop_all.bat` |

---

## 8. 制作 zip（开发者）

在仓库根目录：

```bat
打包.bat
```

输出：`dist\HAJIMI-release-YYYYMMDD.zip`

排除项：`.venv/`、`server/.env`（含密钥）、`__pycache__/`、`node_modules/`、`OmniParser/`

---

## 9. 与开发环境的区别

本交付包**仅新增**根目录启动脚本与 `HAJIMI_UI\scripts\start_release_fullstack.bat`，**不修改**现有业务代码。

与 `启动本地.bat` 的差异：

- `启动全栈.bat` 不强制要求 OmniParser `:9800` 隧道就绪（L4 可先跑）
- `启动全栈.bat` **必须**等待 L5 Sidecar `:8011` 就绪（L5 功能保证）
- 不使用 Mock 回退路径

---

## 10. C 端（语音 ASR/TTS + 审计代理）

C 端提供语音交互和离线审计上报。源码在 `client/` 目录，Vosk 中文语音模型在 `models/vosk-model-small-cn-0.22/`。

### 首次配置

```bash
pip install -r client/requirements.txt
```

### 验证语音模块

```bash
python client/voice_setup.py
```

### B-C 信号联调

需 B 端 UI 已启动：

```bash
python client/bc_adapter.py
```

### 审计队列

本地 SQLite 队列存储在 `%LOCALAPPDATA%/HAJIMI/audit_queue.db`，首次运行自动创建。离线时缓存，恢复网络后批量上报 A 端 `:8010`。

---

## 11. Web 管理面板

Vue3 + Element Plus + ECharts，源码在 `web-admin/`（不含 `node_modules`）。

### 首次启动

```bash
cd web-admin
npm install
npm run dev          # 开发模式 http://localhost:5173
```

或生产构建：

```bash
npm run build        # 输出 web-admin/dist/
```

### 注意

Admin 面板依赖 A 端 `:8010` 的 `/api/admin/*` 端点，部分数据当前为 Mock，等 A 端就位后切换真实数据源。

---

## 12. 已知疏漏清单

以下为 A/B 端实现中已知的未完成项（摘自 `HAJIMI_UI/docs/ABC-完整调试距离与分工清单.md` 及各成员 DAY5/6 进度文档）。

### P0 — 阻塞级

| 模块 | 疏漏 | 责任人 |
|------|------|--------|
| L5 Sidecar | `server_A/server/.venv` 环境搭建（`setup_server_env.bat`） | A |
| L5 Sidecar | `.env` 填入 LLM key + `OMNIPARSER_URL` | A |
| L5 Sidecar | `:8011` health 检查流 | A |
| B-L5 接线 | `execute_worker.py` 改连 `:8011` | B |
| B-L5 接线 | `l5_sidecar_launcher.py` 自动拉起 | B |
| B-C 联调 | PyQt5 信号联调（`bc_adapter.py` 已就绪，等 B 接入） | B+C |
| C 端 | `server_url` 统一为 8010 | C |
| C 端 | audit SQLite 路径改为 `%LOCALAPPDATA%/HAJIMI/` | C |

### P1 — 影响演示

| 模块 | 疏漏 | 责任人 |
|------|------|--------|
| 红线拦截 | B 端 UI 接入 A 端 `redline_service` | B+C |
| 澄清弹窗 | B 端接入 `/clarify` 低置信度交互 | B+C |
| Web 面板 | Mock 数据切换到真实 admin 端点 | C |
| 审计补传 | 离线队列恢复后自动补传 | C |

### P2 — 下个迭代

| 模块 | 疏漏 | 责任人 |
|------|------|--------|
| GroundingDINO | 开放词汇检测补 OmniParser 盲区（8-12h） | A |
| PyInstaller | 单文件 exe（含 Vosk/OmniParser 路径） | B |
| L2 快路径 | 离线模板 < 3s 验收 | A |
| 监控 | Admin `flow.py`/`monitor.py` 切换到真实数据 | A |
| macOS | PyQt 透明窗口兼容（已知限制） | B |

### P3 — 长期优化

| 模块 | 疏漏 |
|------|------|
| 日志持久化 | structlog + 文件轮转 + task_id 追踪 |
| PostgreSQL | SQLite → PostgreSQL 生产迁移（Alembic 已备） |
| 流式输出 | LLM streaming SSE 响应（`client.py` 已预留接口） |

### 数据存储位置汇总

| 数据 | 路径 | 管理方 |
|------|------|--------|
| 主数据库（7 表） | `HAJIMI_UI/data/hajimi.db`（A 端首次启动建表） | A |
| L5 数据库 | `server_A/data/hajimi.db`（独立实例） | A |
| C 端审计队列 | `%LOCALAPPDATA%/HAJIMI/audit_queue.db` | C |
| 用户设置 | `%LOCALAPPDATA%/HAJIMI/user_settings.json` | B |
