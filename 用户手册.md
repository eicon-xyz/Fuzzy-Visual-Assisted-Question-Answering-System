# HAJIMI 用户手册

> 适用版本：v1.0 | 2026-07-09

---

## 一、系统简介

HAJIMI 是一款 AI 驱动的桌面智能助手。用户用自然语言描述任务，HAJIMI 截屏 → 多模态 LLM 理解屏幕 → 生成操作指引（L4）或自动执行（L5），在桌面上叠加可视化标注。

**两种核心模式：**

| 模式 | 路由值 | 能力 |
|------|--------|------|
| **L4 智能指引** | `fast` / `balanced` / `auto` | 分析屏幕 → 生成步骤 → 覆盖层标注箭头和高亮框 |
| **L5 自动执行** | `l5` | 需要时自动点击、输入、按键、启动软件 |

---

## 二、界面概览

启动后显示 PyQt5 桌面窗口，分为三个区域：

```
┌─────────────────────────────────────────┐
│  顶部控制栏    [模式切换] [设置] [最小化] [关闭]  │
├─────────────────────────────────────────┤
│                                         │
│  对话区域                                │
│  用户输入 → AI 助手回复                    │
│  步骤展示 / 执行时间线                     │
│                                         │
├─────────────────────────────────────────┤
│  底部输入框    [发送] [麦克风(预留)]         │
└─────────────────────────────────────────┘
```

### 控制栏功能

| 控件 | 功能 |
|------|------|
| 模式切换 | 切换 `fast`（L4）/ `l5`（L5）/ `auto` / `balanced` / `precision` |
| 设置 | 部署模式、LLM 配置、L4 参数、语音、主题、字体 |
| 最小化 | 收为系统托盘图标（Compact 模式） |
| 托盘 | 右键菜单：打开主面板 / 退出 |

### 覆盖层窗口

在 L4 模式下，AI 回复后桌面会自动弹出**透明覆盖层**，显示：

- 编号标签（对应步骤列表）
- 高亮框（标记操作目标位置）
- 箭头指示

覆盖层**鼠标穿透**（不影响桌面操作），右键或按 ESC 关闭。

---

## 三、L4 Vision 智能指引模式

### 适用场景

- 不知道某个功能在哪里
- 需要分步操作指引
- 不想让 AI 自动操作

### 使用步骤

1. 在控制栏确认路由模式为 **`fast`**（或 `balanced` / `auto`）

2. 在输入框输入任务，例如：
   - 「怎么导出 PDF」
   - 「如何设置 Wi-Fi 密码」
   - 「帮我找到打印机设置」

3. AI 截屏分析后，在对话区展示步骤列表，并在桌面叠加**可视化标注**

4. 按照标注位置依次操作；如果需要重新定位，点击「重新检测」

### 路由模式对比

| 模式 | 速度 | 适用 |
|------|------|------|
| `fast` | 快 | 简单操作指引 |
| `balanced` | 中等 | 一般任务 |
| `auto` | 自适应 | 不确定复杂度时用 |
| `precision` | 慢（需 OmniParser） | 精确元素定位 |

---

## 四、L5 自动执行模式

### 适用场景

- 重复性操作（打开软件、填写表单）
- 需要跨窗口、跨应用操作
- 愿意授权 AI 控制鼠标键盘

### 使用步骤

1. 在控制栏切换路由模式为 **`l5`**

2. 在设置中开启 **「L5 自动执行」**

3. 最小化 HAJIMI 窗口（露出桌面和要操作的区域）

4. 输入任务，例如：
   - 「打开记事本写入 hello world」
   - 「打开计算器」
   - 「在浏览器打开百度」

5. 在弹出的授权弹窗中确认

6. 观察自动操作时间线：
   - 左侧：规划步骤列表
   - 右侧：执行时间线（日志、截屏缩略图、完成/失败状态）

### L5 桌面标注开关

设置 → L5 自动执行 → 「L5 桌面标注」可开关执行过程中的桌面高亮。

### L5 工具级监控

启用额外 SSE 事件（显示每次工具调用的详细参数和结果）：

```bat
set HAJIMI_L5_TOOL_SSE=1
```

并重启 8011 Sidecar。

### L5 支持的自动化操作

| 操作 | 需要 OmniParser | 示例 |
|------|-----------------|------|
| 启动应用 (`launch_app`) | 否 | 打开记事本、计算器 |
| 点击 (`click`) | 是 | 点击桌面图标 |
| 双击 (`double_click`) | 是 | 打开桌面文件 |
| 输入文本 (`type_text`) | 否 | 填充表单 |
| 按键 (`press_key`) | 否 | 回车、保存 |
| 截图 (`screenshot`) | 否 | 获取当前屏幕 |
| 滚动 (`scroll`) | 是 | 浏览网页 |
| 浏览器操作 (`browser_*`) | 否 | 打开网页、点击网页元素 |

### 中止执行

点击 L5 时间线中的 **「停止」** 按钮，或按 Ctrl+C 在 8011 窗口。

> 紧急停止：将鼠标**快速移到屏幕左上角**（pyautogui FAILSAFE 机制）。

---

## 五、设置面板

点击控制栏齿轮图标进入设置页，分为三个 Tab：

### 5.1 后端 & LLM

| 设置项 | 说明 |
|--------|------|
| 部署模式 | `gpu_api`（默认）/ `local` / `intranet` |
| A 端地址 | 默认 `http://127.0.0.1:8010` |
| LLM API Key | Vision 多模态模型密钥 |
| LLM Base URL | API 地址 |
| LLM Model | 模型名称 |
| Demo Key | API 访问密钥（默认 `hajimi-demo-2026`） |

### 5.2 路由 & L4

| 设置项 | 说明 | 建议 |
|--------|------|------|
| 路由模式 | `fast`/`balanced`/`auto`/`precision`/`l5` | L4 用 `fast`，L5 用 `l5` |
| L4 Planner 模型 | 规划步骤的 LLM | 默认即可 |
| L4 Locator 模型 | 定位元素的 LLM | 默认即可 |
| 严格定位 | 是否强制要求精确坐标 | 建议开启 |

### 5.3 外观

| 设置项 | 选项 |
|--------|------|
| UI 主题 | `current` / `variant_luxury` |
| 字体 | 系统字体 |
| 界面模式 | Medium（完整面板）/ Compact（浮动条） |

---

## 六、语音功能（C 端，可选）

### 初始化

```bash
pip install -r client/requirements.txt
python client/voice_setup.py
```

### 使用

- B 端 UI 控制栏预留麦克风按钮
- 点击后启动语音识别（ASR），识别结果填入输入框
- AI 回复后触发语音合成播报（TTS）

> B-C 语音信号联调需要 B 端接入 `bc_adapter.py`，当前处于集成阶段。

---

## 七、典型操作示例

### 示例 1：L4 指引「怎么设置 Wi-Fi」

1. 路由模式选 `fast`
2. 输入「怎么设置 Wi-Fi」
3. 等待覆盖层弹出箭头和高亮框
4. 按照编号步骤点击

### 示例 2：L5 自动打开记事本

1. 路由模式选 `l5`，开启自动执行
2. 最小化 HAJIMI
3. 输入「打开记事本」
4. 确认授权 → 观察笔记本自动弹出

### 示例 3：浏览器搜索

1. 确保 Edge/Chrome 已安装
2. 路由模式 `l5`
3. 输入「在浏览器打开百度搜索今天天气」
4. 确认授权

---

## 八、状态栏提示

| 提示 | 含义 | 处理 |
|------|------|------|
| 「A 端已连接 (L4 Vision)」 | 正常，可进行 L4 操作 | — |
| 「A 端未连接」 | :8010 未启动 | 运行 `启动全栈.bat` |
| 「OmniParser 未连接」 | :9800 不可达 | 建立 GPU 隧道或切换 L4 Vision |
| 「L5 不可用」 | :8011 未启动或路由非 `l5` | 检查 8011 窗口；切换路由 |
| 「UI 演示模式」 | Mock Only 开启 | 关闭 `HAJIMI_MOCK_ONLY` 环境变量 |

---

## 九、键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| Enter | 发送消息 |
| ESC | 关闭覆盖层 |
| 鼠标甩到屏幕左上角 | 紧急停止 L5 自动执行 |

---

## 十、常见问题

| 问题 | 处理 |
|------|------|
| 覆盖层标注位置不对 | 点击「重新检测」；确认屏幕缩放 100% |
| L5 点不到/点错 | 确认 OmniParser 连接正常；路由 `l5` |
| 语音识别没反应 | `python client/voice_setup.py` 验证；检查麦克风 |
| 中文输入后无响应 | 检查 LLM API Key 是否配置 |
| 窗口最小化后找不回 | 右键系统托盘图标 → 打开主面板 |
| 主题切换后字体异常 | 重启 HAJIMI |
| L5 网页任务失败 | 安装/更新 Edge 或 Chrome |
