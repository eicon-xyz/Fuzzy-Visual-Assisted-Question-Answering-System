# HAJIMI 用户手册

> 适用版本：v2.0 | 2026-09（L5 自动执行，唯一模式；L4 指引模式已移除）

---

## 一、系统简介

HAJIMI 是一款 AI 驱动的桌面智能助手。用户用自然语言描述任务，HAJIMI 的 L5 引擎理解意图并生成操作计划，随后通过 **Windows UIA 控件绑定 + Playwright DOM** 在桌面上自动点击、输入、按键、启动软件，并以时间线实时回显执行进度。

> 旧 L4 指引模式（截屏 + 红框/箭头覆盖层标注）、检验（inspect）模式与部署模式切换已整体移除，系统只有 L5 自动执行一种模式。

---

## 二、界面概览

启动后显示 PyQt5 桌面窗口，左侧导航四个页面：

```
┌─────────────────────────────────────────┐
│  顶部控制栏        [最小化] [关闭]         │
├────┬────────────────────────────────────┤
│ 导 │  操作指引：聊天对话，输入任务指令        │
│ 航 │  步骤列表：L5 规划步骤 + 执行时间线      │
│    │  提醒通知：系统与任务提醒               │
│    │  系统设置：模型 key、L5 选项、外观、语音 │
├────┴────────────────────────────────────┤
│  底部输入框    [发送] [麦克风(C端语音)]      │
└─────────────────────────────────────────┘
```

### 控制栏 / 托盘

| 控件 | 功能 |
|------|------|
| 最小化 | 收为系统托盘图标（Compact 浮动条模式） |
| 托盘 | 右键菜单：打开主面板 / 退出（退出时按配置停止 L5 Sidecar） |

> 原「模式切换」路由选择与「覆盖层标注窗口」已随 L4 移除；执行过程不再弹出透明标注层，进度全部在「步骤列表」时间线中展示（可选开启桌面高亮标注，见设置）。

---

## 三、L5 自动执行

### 适用场景

- 重复性操作（打开软件、填写表单）
- 跨窗口、跨应用操作
- 愿意授权 AI 控制鼠标键盘

### 使用步骤

1. 首次使用在「系统设置」中开启 **「L5 自动执行」** 并确认知情说明

2. 在「操作指引」页输入任务，例如：
   - 「打开记事本写入 hello world」
   - 「打开计算器」
   - 「在浏览器打开百度」

3. 弹出 **L5 知情确认** 对话框，确认后指令提交给 L5 Sidecar（提交前 B 端会做红线归一化，危险请求直接拒绝）

4. 最小化 HAJIMI 窗口（露出桌面和要操作的区域），观察「步骤列表」页：
   - 规划步骤列表（StepCard）
   - 执行时间线（SSE 实时事件：step_start / step_done / step_blocked / step_failed / task_done，含每步截图与日志）

### 执行控制快捷键（全局）

| 快捷键 | 功能 |
|--------|------|
| `H` | 批准（继续/授权当前步骤） |
| `P` | 暂停 |
| `J` | 停止（取消任务） |

（默认 H/J/P，可在设置的 L5 卡片中查看；快捷键在 HAJIMI 未聚焦时也可用。）

### L5 桌面标注开关

设置 → L5 自动执行 → 「L5 桌面标注」：Agent 点击时在屏幕显示高亮，可开关。

### L5 工具级监控

启用额外 SSE 事件（显示每次工具调用的详细参数和结果）：

```bat
set HAJIMI_L5_TOOL_SSE=1
```

并重启 8011 Sidecar。

### L5 支持的自动化操作

| 操作 | 定位方式 | 示例 |
|------|----------|------|
| 启动应用 (`launch_app`) | 系统启动 | 打开记事本、计算器 |
| 点击 / 双击 (`click`/`double_click`) | UIA 控件绑定（必要时坐标兜底） | 点击按钮、打开桌面文件 |
| 输入文本 (`type_text`) | UIA SetValue / 键盘 | 填充表单 |
| 按键 (`press_key`) | 键盘 | 回车、保存 |
| 截图 (`screenshot`) | 屏幕捕获（仅用于回显） | 查看当前屏幕 |
| 滚动 (`scroll`) | 鼠标滚轮 | 浏览网页 |
| 浏览器操作 (`browser_*`) | Playwright DOM 选择器 | 打开网页、点击网页元素 |

### 中止执行

按 `J` 停止，或点击时间线中的 **「停止」** 按钮；也可在 8011 Sidecar 窗口按 Ctrl+C。

> 紧急停止：将鼠标**快速移到屏幕左上角**（pyautogui FAILSAFE 机制）。

---

## 四、设置面板

进入「系统设置」页，按卡片分组：

### 4.1 模型（问答模型 & L5）

| 设置项 | 说明 |
|--------|------|
| Demo Key | API 访问密钥（默认 `hajimi-demo-2026`） |
| 问答模型 API Key | L5 规划/对话使用的文本模型密钥 |
| LLM Base URL / 模型名 | 默认 DeepSeek 官方 `deepseek-chat` |
| L5 自动执行选项 | 开启/关闭自动执行授权 |

> 保存后会写入 L5 Sidecar 的 `server_A\server\.env`（模型 key 唯一存放处）并自动重启 Sidecar。原「部署模式」「A 端地址 (:8010)」「OmniParser 配置」项已删除。

### 4.2 L5 自动执行

| 设置项 | 说明 | 建议 |
|--------|------|------|
| L5 桌面标注 | 执行时屏幕高亮 | 演示时开启 |
| 快捷键（批准/暂停/停止） | 默认 H / P / J | 保持默认 |

### 4.3 外观 / 语音

| 设置项 | 选项 |
|--------|------|
| UI 主题 | `current` / `variant_luxury` |
| 字体 | 系统字体 |
| 界面模式 | Medium（完整面板）/ Compact（浮动条） |
| 语音（C 端） | ASR/TTS 引擎、麦克风、 Vosk 模型路径 |

---

## 五、语音功能（C 端，可选）

### 初始化

```bash
pip install -r client/requirements.txt
python client/voice_setup.py
```

### 使用

- C 端语音服务默认连接 L5 Sidecar `http://127.0.0.1:8011`（`bc_adapter.py` / `config_poller.py`）
- 语音识别（ASR）结果可转入 B 端输入框；AI 回复后触发语音合成播报（TTS）

> B-C 语音信号经 B 端 `bc_signals` 总线接入，详见 `HAJIMI_UI/docs/语音集成操作手册.md`。

---

## 六、典型操作示例

### 示例 1：自动打开记事本

1. 「操作指引」输入「打开记事本写入 hello world」
2. 确认 L5 知情确认弹窗
3. 切到「步骤列表」观察时间线：启动应用 → 输入文本 → task_done

### 示例 2：浏览器搜索

1. 确保 Edge/Chrome 已安装
2. 输入「在浏览器打开百度搜索今天天气」
3. 确认授权 → Playwright 自动打开网页并操作

### 示例 3：中途停止

1. 任务执行中按 `J` → 任务取消，时间线标记停止原因

---

## 七、状态栏提示

| 提示 | 含义 | 处理 |
|------|------|------|
| 「L5 Sidecar 已连接」 | :8011 正常，可提交任务 | — |
| 「后端未连接」 | :8011 未启动 | 运行 `启动全栈.bat`，或等 B 端自动拉起 |
| 「L5 不可用 / 未授权」 | 设置中未开启 L5 自动执行 | 到系统设置开启并确认知情 |
| 「模型未配置」 | Sidecar 无可用 API Key | 设置页填 key（写入 `server_A\server\.env`）|

> 旧「A 端已连接 (L4 Vision)」「OmniParser 未连接 (:9800)」「UI 演示模式 (Mock)」提示已随对应功能移除。

---

## 八、键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| Enter | 发送消息 |
| H | L5：批准 |
| P | L5：暂停 |
| J | L5：停止 |
| 鼠标甩到屏幕左上角 | 紧急停止 L5 自动执行（FAILSAFE） |

---

## 九、常见问题

| 问题 | 处理 |
|------|------|
| L5 点不到/点错 | 目标程序需支持 Windows 无障碍（UIA）；必要时开启「L5 桌面标注」观察实际点位；个别软件需以相同权限运行 HAJIMI/Sidecar |
| 提交被红线拒绝 | 涉及隐私、系统高危或物理设备操作的指令会被拒绝，属正常安全行为 |
| 语音识别没反应 | `python client/voice_setup.py` 验证；检查麦克风 |
| 中文输入后无响应 | 检查模型 API Key 是否配置（设置页或 `server_A\server\.env`） |
| 窗口最小化后找不回 | 右键系统托盘图标 → 打开主面板 |
| 主题切换后字体异常 | 重启 HAJIMI |
| L5 网页任务失败 | 安装/更新 Edge 或 Chrome |
| 步骤列表一直转圈 | 查看 HAJIMI-L5-Sidecar 窗口日志；确认 `curl http://127.0.0.1:8011/api/demo/health` 正常 |
