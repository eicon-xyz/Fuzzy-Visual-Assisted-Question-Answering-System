# HAJIMI 启动指南

> 最后更新：2026-07-03

---

## 前置条件

```bash
cd E:\Fuzzy-Visual-Assisted-Question-Answering-System
```

以下包已安装（缺失则执行对应命令）：

| 组件 | 验证 | 安装命令 |
|------|------|----------|
| A 端依赖 | `python -c "import fastapi,uvicorn"` | `pip install fastapi uvicorn pydantic python-dotenv httpx sqlalchemy psutil` |
| Web 面板 | `dir web-admin\node_modules` 存在 | `cd web-admin && npm install` |
| Vosk 模型 | `dir models\vosk-model-small-cn-0.22\am` 存在 | 无（ASR 自动降级在线引擎） |

---

## 启动步骤

### 第一步：A 端（端口 8000）

```bash
python -m uvicorn server.main:app --host 127.0.0.1 --port 8000
```

等待输出 `Application startup complete`（约 5 秒，首次启动创建 7 个数据库表）。

**验证**：浏览器打开 `http://localhost:8000/docs` 或：

```bash
curl http://localhost:8000/api/demo/health
# → {"status":"ok","version":"1.0.0",...}
```

### 第二步：Web 管理面板（端口 5173）

```bash
cd web-admin
npm run dev
```

浏览器打开 `http://localhost:5173`，输入任意密码登录。

### 第三步：C 端测试（可选）

```bash
python client/real_api_test.py
```

---

## 端口一览

| 端口 | 用途 | 启动命令 |
|------|------|----------|
| `8000` | A 端 Server | `python -m uvicorn server.main:app --host 127.0.0.1 --port 8000` |
| `5173` | Web 面板 | `cd web-admin && npm run dev` |
| `8002` | OmniParser | B 端脚本管理（C 端不需要） |
| `8010` | B 端嵌入式 A | B 端脚本管理（C 端不需要） |

---

## 停止

关闭终端窗口，或：

```bash
# 查端口
cmd //c "netstat -ano | findstr :8000 | findstr LISTENING"
# 杀进程（替换 PID）
cmd //c "taskkill /F /PID <PID>"
```

---

## 常见问题

**Q: `[WinError 10048]` 端口被占用**

```bash
cmd //c "netstat -ano | findstr :8000 | findstr LISTENING"
# 记下最后一列 PID，然后：
cmd //c "taskkill /F /PID <PID>"
```

**Q: A 端启动报 `No module named 'xxx'`**

```bash
pip install fastapi uvicorn pydantic python-dotenv httpx sqlalchemy psutil
```

**Q: 数据库在哪？**

首次启动自动在项目根目录创建 `hajimi.db`（SQLite）。服务重启后数据保留。

**Q: 没有 DeepSeek API Key 能用吗？**

能。A 端启动不依赖 LLM Key。`/process` 等需要 LLM 调用的端点会返回 Mock 降级数据。

**Q: 要启动 B 端桌面应用怎么办？**

B 端需要额外的 conda 环境（`omni`、`hajimi_ui`），配置较复杂，见 `参考文档/环境配置问题记录.md`。C 端集成测试不需要 B 端。
