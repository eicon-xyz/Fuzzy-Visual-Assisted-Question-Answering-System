# HAJIMI 启动指南

> 最后更新：2026-07-06（new_JIMI 重构版）

---

## 前置条件

```bash
cd E:\Fuzzy-Visual-Assisted-Question-Answering-System
```

| 组件 | 验证 | 安装命令 |
|------|------|----------|
| A 端依赖 | `python -c "import fastapi,uvicorn,sqlalchemy"` | `pip install fastapi uvicorn sqlalchemy pydantic python-dotenv httpx psutil` |
| Web 面板 | `dir web-admin\node_modules` 存在 | `cd web-admin && npm install` |
| Vosk 模型 | `dir models\vosk-model-small-cn-0.22\am` 存在 | 无（ASR 自动降级 Google 在线引擎） |

---

## 项目结构

```
E:\Fuzzy-Visual-Assisted-Question-Answering-System\
├── new_JIMI/HAJIMI_UI/       ← 新后端（A 端 + B 端）
│   ├── server/                ← A 端 Server（端口 8010）
│   │   ├── database/          ← SQLAlchemy ORM（7 表）
│   │   ├── routes/            ← 26 端点（demo/admin/audit/auth/flow/monitor/config_client）
│   │   └── data/hajimi.db     ← SQLite 数据库文件
│   ├── core/                  ← B 端核心模块
│   ├── ui/                    ← B 端 PyQt5 UI
│   └── config.py              ← B 端配置
├── client/                    ← C 端客户端
│   ├── voice/                 ← ASR + TTS
│   ├── audit/                 ← 审计代理
│   ├── config/                ← 配置轮询
│   └── integration/           ← B-C 集成
└── web-admin/                 ← C 端 Web 管理面板（端口 5173）
```

---

## 启动步骤

### 第一步：A 端（端口 8010）

```bash
cd E:\Fuzzy-Visual-Assisted-Question-Answering-System\new_JIMI\HAJIMI_UI
python -m uvicorn server.main:app --host 127.0.0.1 --port 8010
```

等待 `Application startup complete`（约 5 秒）。数据库自动创建在 `new_JIMI/HAJIMI_UI/data/hajimi.db`。

验证：

```bash
curl http://localhost:8010/api/demo/health
# → {"status":"ok","version":"1.0.0",...}
```

### 第二步：注入测试数据（仅首次）

```bash
cd E:\Fuzzy-Visual-Assisted-Question-Answering-System
python client/seed_data.py
```

### 第三步：Web 管理面板（端口 5173）

```bash
cd E:\Fuzzy-Visual-Assisted-Question-Answering-System\web-admin
npm run dev
# 或 npm run fast（构建后预览，速度更快）
```

浏览器 `http://localhost:5173`，输入任意密码登录。

### 第四步：C 端测试（可选）

```bash
cd E:\Fuzzy-Visual-Assisted-Question-Answering-System
python client/real_api_test.py
```

---

## 端口一览

| 端口 | 用途 | 启动命令 |
|------|------|----------|
| `8010` | A 端 Server（new_JIMI） | `cd new_JIMI/HAJIMI_UI && python -m uvicorn server.main:app --host 127.0.0.1 --port 8010` |
| `5173` | Web 面板 | `cd web-admin && npm run dev` |
| `8002` | OmniParser | `cd HAJIMI_UI && scripts\start_omniparser.bat` |
| `9800` | OmniParser（SSH 隧道） | SSH 端口转发 |

---

## 停止

```bash
# 查端口
cmd //c "netstat -ano | findstr :8010 | findstr LISTENING"
# 杀进程
cmd //c "taskkill /F /PID <PID>"
```

---

## 常见问题

| 问题 | 解决 |
|------|------|
| 端口被占用 [WinError 10048] | `cmd //c "for /f \"tokens=5\" %a in ('netstat -ano ^| findstr :8010 ^| findstr LISTENING') do taskkill /F /PID %a"` |
| `No module named 'xxx'` | `pip install fastapi uvicorn sqlalchemy pydantic python-dotenv httpx psutil` |
| 数据库在哪？ | `new_JIMI/HAJIMI_UI/data/hajimi.db`（SQLite，首次启动自动创建） |
| Web 面板是真实数据吗？ | `cd web-admin && npm run dev` 后打开 → 数据来自 A 端 `/api/admin/*` 真实查询 |
| 没有 API Key 能用吗？ | 能。A 端启动不依赖 LLM Key，核心流程返回 Mock 降级。 |
