# HAJIMI 启动指南

> 基准：`启动本地.bat` | V2.2 | 2026-07-07

## 一键启动（推荐）

```bash
E:\Fuzzy-Visual-Assisted-Question-Answering-System\启动本地.bat
```

内部自动：检查环境 → 启动 OmniParser 隧道 → 启动 A端(8010) → 启动 L5 Sidecar(8011)

## 分步启动（调试/开发用）

### 1. SSH隧道（OmniParser GPU）

```bash
ssh -L 9800:127.0.0.1:9800 student@10.246.2.7 -p 12202
```

验证：
```bash
curl http://127.0.0.1:9800/health
```

### 2. A端（端口 8010）

```bash
cd HAJIMI_UI
server\.venv\Scripts\python.exe -m uvicorn server.main:app --host 0.0.0.0 --port 8010
```

或（无 venv 时）：
```bash
python -m uvicorn server.main:app --host 0.0.0.0 --port 8010
```

验证：
```bash
curl http://127.0.0.1:8010/api/demo/health
```

### 3. Web管理面板（端口 5173）

```bash
cd web-admin
npm run dev
```

浏览器：`http://localhost:5173`

### 4. C端测试

```bash
python 项目文档/测试文档/demo_connectivity.py
```

## 目录速查

| 组件 | 位置 |
|------|------|
| A端 Server | `HAJIMI_UI/server/` |
| B端 桌面 | `HAJIMI_UI/main.py` |
| C端 Voice/Audit/Config | `client/` |
| C端 Web面板 | `web-admin/` |
| 数据库 | `HAJIMI_UI/server/data/hajimi.db` |
| 项目文档 | `项目文档/` |
| 进度报告 | `工作进度/` |
| 结构速查 | `项目结构速查.md` |

## 常见问题

| 问题 | 解决 |
|------|------|
| 端口被占用 | `cmd //c "for /f \"tokens=5\" %a in ('netstat -ano ^| findstr :8010 ^| findstr LISTENING') do taskkill /F /PID %a"` |
| server\.venv 不存在 | `cd HAJIMI_UI && python -m venv server\.venv && server\.venv\Scripts\pip install fastapi uvicorn sqlalchemy httpx psutil` |
| OmniParser 不可达 | 检查 SSH 隧道是否建立：`curl http://127.0.0.1:9800/health` |
