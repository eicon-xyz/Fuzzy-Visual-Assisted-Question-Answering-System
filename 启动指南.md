# HAJIMI 启动指南

> 基准：`启动全栈.bat` | V2.4 | 2026-09（L5 自动执行，唯一模式）

## 安装（首次）

```bash
E:\Fuzzy-Visual-Assisted-Question-Answering-System\安装全栈.bat
```

内部自动：创建 2 个 venv —— B端 `HAJIMI_UI\.venv`（依赖仅 PyQt5）+ L5 Sidecar `server_A\server\.venv`（含 torch，首次约 10–30 分钟），安装依赖并从 `.env.example` 生成 `server_A\server\.env`。

## 一键启动（推荐，当前默认）

```bash
E:\Fuzzy-Visual-Assisted-Question-Answering-System\启动全栈.bat
```

内部自动（`HAJIMI_UI\scripts\start_release_fullstack.bat`）：写入 L5 模式设置（`scripts\apply_l5_settings.py` → `OMNIPARSER_ENABLED=false`、`ROUTING_MODE=l5`）→ 启动 **L5 Sidecar 窗口（:8011）** → 等健康检查通过 → 启动 **B端窗口**。共两个窗口。

开发/调试也可用 `启动本地.bat`（内部 `HAJIMI_UI\scripts\start_local_vision.bat`），同样只启动 L5 Sidecar + B端，日志写 `HAJIMI_UI\scripts\local_vision_run.log`。

**L5 自动执行模式说明**（旧 L4 指引模式、OmniParser、A端 :8010 已整体移除）：
- 执行层：**Windows UIA 绑定**（`uia_bridge.py`，Invoke/SetValue 精确操作）+ Playwright DOM（网页）
- `OMNIPARSER_ENABLED=false`：一切 OmniParser 探测/调用被跳过；唯一后端 = server_A Sidecar
- 文本 LLM：`deepseek-chat`（官方 DeepSeek，已验证）
- 模型 key 唯一存放处：`server_A\server\.env`（`DEEPSEEK_API_KEY` / `LLM_*`）；B端设置页保存后由 `core/env_sync.py` 同步写入该文件并重启 Sidecar
- 提交流程：输入指令 → L5 知情确认 → `/api/demo/execute` → SSE `/api/demo/stream` 实时进度（快捷键 H 批准 / P 暂停 / J 停止）

> 旧 :9800 GPU 隧道、校园 A端内网联调、联调启动.bat 均已随 L4 移除；如未来需要可从 git 历史取回。

## 停止

```bash
stop_all.bat    # 只停 L5 Sidecar（:8011）
```

## 验收

```bash
验收.bat        # 内部 scripts\verify_all.py --require-l5，确认 :8011 L5 端点就绪
```

## 分步启动（调试/开发用）

### 1. L5 Sidecar（端口 8011）

```bash
HAJIMI_UI\scripts\start_l5_sidecar.bat
```

或手动：
```bash
cd server_A
server\.venv\Scripts\python.exe -m uvicorn server.main:app --host 127.0.0.1 --port 8011
```

验证：
```bash
curl http://127.0.0.1:8011/api/demo/health
```

### 2. B端

```bash
cd HAJIMI_UI
.venv\Scripts\python.exe main.py
```

（B端启动时若检测 Sidecar 未运行，会自动拉起 `start_l5_sidecar.bat`。）

### 3. Web管理面板（端口 5173，可选）

```bash
cd web-admin
npm run dev
```

浏览器：`http://localhost:5173`（API 已改指 Sidecar :8011）

### 4. C端测试

```bash
python 项目文档/测试文档/demo_connectivity.py
```

## 目录速查

| 组件 | 位置 |
|------|------|
| L5 Sidecar（唯一后端） | `server_A/server/`（:8011） |
| B端 桌面 | `HAJIMI_UI/main.py` |
| C端 Voice/Audit/Config | `client/`（server_url 默认指 :8011） |
| C端 Web面板 | `web-admin/` |
| 数据库 | `server_A/data/hajimi.db` |
| 模型 key | `server_A/server/.env` |
| 项目文档 | `项目文档/` |
| 进度报告 | `工作进度/` |
| 结构速查 | `项目结构速查.md` |

## 常见问题

| 问题 | 解决 |
|------|------|
| 端口被占用 | `cmd //c "for /f \"tokens=5\" %a in ('netstat -ano ^| findstr :8011 ^| findstr LISTENING') do taskkill /F /PID %a"`，或直接跑 `stop_all.bat` |
| `server_A\server\.venv` 不存在 | 重跑 `安装全栈.bat`（或 `HAJIMI_UI\scripts\ensure_l5_sidecar_env.bat`） |
| B端提示后端未启动 | 看 HAJIMI-L5-Sidecar 窗口报错；手动 `HAJIMI_UI\scripts\start_l5_sidecar.bat` 后用 `/api/demo/health` 验证 |
| L5 提交报未配置 key | 在 `server_A\server\.env` 填 `DEEPSEEK_API_KEY` / `LLM_API_KEY`（或 B端设置页保存，会自动同步并重启 Sidecar） |
| 验收失败 | 先 `启动全栈.bat`，再 `验收.bat`；检查 Sidecar 窗口输出 |
