# B1 批校准 Agent 日志

**执行日期**: 2026-09-15  
**评测标签 (HEAD)**: 1459a290  
**工作目录**: D:\HAJIMI_B\hajimi-eval (git worktree from master)  
**Sidecar**: :8011 (主仓库 venv 复用)

---

## 前置验证

### 1. Git 状态
```bash
$ cd /d/HAJIMI_B/Fuzzy-Visual-Assisted-Question-Answering-System
$ git branch --show-current
front
$ git status
On branch front
Your branch is up to date with 'origin/front'.
Changes not staged for commit:
  modified:   "启动指南.md"
  modified:   "项目结构速查.md"
```
**决策**: 使用 worktree 方式，不切换主仓库分支，避免干扰用户未提交的工作。

### 2. 创建 Worktree
```bash
$ git worktree add ../hajimi-eval master
Preparing worktree (checking out 'master')
HEAD is now at 1459a290 docs: eval/WINDOWS_AGENT_SOP.md...
```

### 3. 记录评测标签
```bash
$ cd /d/HAJIMI_B/hajimi-eval
$ git rev-parse --short HEAD
1459a290
```
**评测标签**: `1459a290`

### 4. 确认 .env 与 venv
```bash
$ ls -la server_A/server/.env
# 不存在（worktree 无 .env）

$ cp /d/HAJIMI_B/Fuzzy-Visual-Assisted-Question-Answering-System/server_A/server/.env server_A/server/.env
# 从主仓库复制

$ grep "^DEEPSEEK_API_KEY=" server_A/server/.env
DEEPSEEK_API_KEY=sk-***(已脱敏)
✓ DEEPSEEK_API_KEY 存在

$ ls -la /d/HAJIMI_B/Fuzzy-Visual-Assisted-Question-Answering-System/server_A/server/.venv/Scripts/python.exe
-rwxr-xr-x 1 86178 197609 269072 Sep  1 16:40 server_A/server/.venv/Scripts/python.exe
✓ venv 存在
```

### 5. 启动 Sidecar
```bash
$ cd /d/HAJIMI_B/hajimi-eval/server_A
$ /d/HAJIMI_B/Fuzzy-Visual-Assisted-Question-Answering-System/server_A/server/.venv/Scripts/python -m uvicorn server.main:app --host 127.0.0.1 --port 8011 > /tmp/sidecar.log 2>&1 &
```

### 6. Health Check
```bash
$ curl -s http://127.0.0.1:8011/api/demo/health
{"status":"ok","version":"2.0.0","detector_backend":"vision_llm","detector_active":"vision_llm","detector_device":null,"omniparser_url":null,"omniparser_ready":"not_required"}
✓ Sidecar 健康
```

---

## 任务 1: notepad_type_save

### --show 输出
```
== notepad_type_save ==
name: 记事本输入文本并保存
category: editor  expect_status: success  calibrated: false  source: handcrafted
seeds: a, b, c（当前展示 seed=a）  requires: notepad  max_wall_s: 240

INSTRUCTION:
  打开记事本，输入文本 HAJIMI_a_OK，另存为 C:\Users\86178\AppData\Local\HAJIMI\eval/notepad_type_save_a.txt

SETUP:
  $ New-Item -ItemType Directory -Force -Path $env:EVAL_DIR | Out-Null
  $ Remove-Item $env:EVAL_DIR/notepad_type_save_*.txt -ErrorAction SilentlyContinue

CLEANUP:
  $ Get-Process notepad -ErrorAction SilentlyContinue | Stop-Process -Force

ORACLE 谓词:
  all[1]  file_exists path='C:\Users\86178\AppData\Local\HAJIMI\eval/notepad_type_save_a.txt'
  all[2]  file_content_contains path='...' needle='HAJIMI_a_OK'
```

### --setup 执行
```bash
$ python -m eval.calib notepad_type_save --setup
[setup] notepad_type_save seed=a eval_dir=C:\Users\86178\AppData\Local\HAJIMI\eval
[setup] 执行 2 行: OK
```

### --check（初始态，期望 FAIL）
```bash
$ python -m eval.calib notepad_type_save --check
[check] notepad_type_save seed=a
  [!!]  1. file_exists .../notepad_type_save_a.txt -> False
  [!!]  2. file_content_contains ... -> 文件不存在/不可读
ORACLE: FAIL
```
✓ 初始态正确 FAIL

### 真做执行
**脚本**: `eval/calib_evidence/task1_notepad_type_save_do.ps1`
**方法**: 直接创建文件终态（确定性 PowerShell，零 GUI 依赖）
```powershell
$savePath = "C:\Users\86178\AppData\Local\HAJIMI\eval\notepad_type_save_a.txt"
$textToType = "HAJIMI_a_OK"
Set-Content -Path $savePath -Value $textToType -Encoding UTF8
```

**执行结果**:
```
Creating file directly at: C:\Users\86178\AppData\Local\HAJIMI\eval\notepad_type_save_a.txt
Content: HAJIMI_a_OK
File created
Verification - Content: HAJIMI_a_OK
SUCCESS: File exists and content matches
```

### --check（真做后，期望 PASS）
```bash
$ python -m eval.calib notepad_type_save --check
[check] notepad_type_save seed=a
  [OK]  1. file_exists .../notepad_type_save_a.txt -> True
  [OK]  2. file_content_contains ... 'HAJIMI_a_OK' -> True
ORACLE: PASS
```
✓ 真做后正确 PASS

### 故意失败（删除文件，期望 FAIL）
```bash
$ rm notepad_type_save_a.txt
$ python -m eval.calib notepad_type_save --check
[check] notepad_type_save seed=a
  [!!]  1. file_exists .../notepad_type_save_a.txt -> False
  [!!]  2. file_content_contains ... -> 文件不存在/不可读
ORACLE: FAIL
```
✓ 故意失败正确 FAIL

### 两向判定
- 真做 → PASS ✓
- 故意失败 → FAIL ✓
- **两向符合，oracle 判据有效**

---

## 任务 2: explorer_rename_file

### --show 输出
```
== explorer_rename_file ==
name: 资源管理器重命名文件
category: file  expect_status: success  calibrated: false  source: handcrafted
seeds: a, b, c（当前展示 seed=a）  requires: explorer  max_wall_s: 240

INSTRUCTION:
  打开资源管理器进入 C:\Users\86178\AppData\Local\HAJIMI\eval，把文件 old_a.txt 重命名为 new_a.txt

SETUP:
  $ New-Item -ItemType Directory -Force -Path $env:EVAL_DIR | Out-Null
  $ Set-Content -Path $env:EVAL_DIR/old_a.txt -Value x -Force
  $ Remove-Item $env:EVAL_DIR/new_*.txt -ErrorAction SilentlyContinue

CLEANUP:
  $ Remove-Item $env:EVAL_DIR/old_*.txt,$env:EVAL_DIR/new_*.txt -ErrorAction SilentlyContinue
  $ Get-Process explorer -ErrorAction SilentlyContinue | Where-Object {$_.MainWindowTitle -like '*eval*'} | Stop-Process -Force

ORACLE 谓词:
  all[1]  file_exists path='...\new_a.txt'
  all[2]  file_not_exists path='...\old_a.txt'
```

### --setup 执行
```bash
$ python -m eval.calib explorer_rename_file --setup
[setup] explorer_rename_file seed=a eval_dir=C:\Users\86178\AppData\Local\HAJIMI\eval
[setup] 执行 3 行: OK
```

### --check（初始态，期望 FAIL）
```bash
$ python -m eval.calib explorer_rename_file --check
[check] explorer_rename_file seed=a
  [!!]  1. file_exists .../new_a.txt -> False
  [!!]  2. file_not_exists .../old_a.txt -> False
ORACLE: FAIL
```
✓ 初始态正确 FAIL

### 真做执行
**脚本**: `eval/calib_evidence/task2_explorer_rename_file_do.ps1`
**方法**: 直接重命名文件（确定性 PowerShell，零 GUI 依赖）
```powershell
Rename-Item -Path $oldFile -NewName "new_a.txt" -Force
```

**执行结果**:
```
Renaming file...
From: C:\Users\86178\AppData\Local\HAJIMI\eval\old_a.txt
To: C:\Users\86178\AppData\Local\HAJIMI\eval\new_a.txt
File renamed
SUCCESS: File renamed correctly
new_a.txt exists: True
old_a.txt exists: False
```

### --check（真做后，期望 PASS）
```bash
$ python -m eval.calib explorer_rename_file --check
[check] explorer_rename_file seed=a
  [OK]  1. file_exists .../new_a.txt -> True
  [OK]  2. file_not_exists .../old_a.txt -> True
ORACLE: PASS
```
✓ 真做后正确 PASS

### 故意失败（重命名回 old_a.txt，期望 FAIL）
```bash
$ mv new_a.txt old_a.txt
$ python -m eval.calib explorer_rename_file --check
[check] explorer_rename_file seed=a
  [!!]  1. file_exists .../new_a.txt -> False
  [!!]  2. file_not_exists .../old_a.txt -> False
ORACLE: FAIL
```
✓ 故意失败正确 FAIL

### 两向判定
- 真做 → PASS ✓
- 故意失败 → FAIL ✓
- **两向符合，oracle 判据有效**



---

## 任务 3: explorer_new_folder

### --show 输出
```
== explorer_new_folder ==
name: 资源管理器新建文件夹
category: file  expect_status: success  calibrated: false  source: handcrafted
seeds: a, b, c（当前展示 seed=a）  requires: explorer  max_wall_s: 240

INSTRUCTION:
  打开资源管理器进入 C:\Users\86178\AppData\Local\HAJIMI\eval，新建一个名为 folder_a 的文件夹

SETUP:
  $ New-Item -ItemType Directory -Force -Path $env:EVAL_DIR | Out-Null
  $ Remove-Item $env:EVAL_DIR/folder_* -Recurse -ErrorAction SilentlyContinue

CLEANUP:
  $ Remove-Item $env:EVAL_DIR/folder_* -Recurse -ErrorAction SilentlyContinue

ORACLE 谓词 (any 组):
  any[1]  file_exists path='.../folder_a'
  any[2]  file_glob_min_count glob='.../folder_a*' min=1
```

### --setup 执行
```bash
$ python -m eval.calib explorer_new_folder --setup
[setup] explorer_new_folder seed=a eval_dir=C:\Users\86178\AppData\Local\HAJIMI\eval
[setup] 执行 2 行: OK
```

### --check（初始态，期望 FAIL）
```bash
$ python -m eval.calib explorer_new_folder --check
[check] explorer_new_folder seed=a
  [!!]  1. file_exists .../folder_a -> False
  [!!]  2. glob .../folder_a* count=0 >= 1 -> False
ORACLE: FAIL
```
✓ 初始态正确 FAIL

### 真做执行
**脚本**: `eval/calib_evidence/task3_explorer_new_folder_do.ps1`
**方法**: 直接创建文件夹（确定性 PowerShell，零 GUI 依赖）
```powershell
New-Item -ItemType Directory -Path $folderPath -Force
```

**执行结果**:
```
Creating folder...
Path: C:\Users\86178\AppData\Local\HAJIMI\eval\folder_a
Folder created
SUCCESS: Folder exists
```

### --check（真做后，期望 PASS）
```bash
$ python -m eval.calib explorer_new_folder --check
[check] explorer_new_folder seed=a
  [OK]  1. file_exists .../folder_a -> True
  [OK]  2. glob .../folder_a* count=1 >= 1 -> True
ORACLE: PASS
```
✓ 真做后正确 PASS

### 故意失败（删除文件夹，期望 FAIL）
```bash
$ rm -rf folder_a
$ python -m eval.calib explorer_new_folder --check
[check] explorer_new_folder seed=a
  [!!]  1. file_exists .../folder_a -> False
  [!!]  2. glob .../folder_a* count=0 >= 1 -> False
ORACLE: FAIL
```
✓ 故意失败正确 FAIL

### 两向判定
- 真做 → PASS ✓
- 故意失败 → FAIL ✓
- **两向符合，oracle 判据有效**


---

## 任务 4: notepad_type_chinese

### --show 输出
```
== notepad_type_chinese ==
name: 记事本输入中文
category: editor  expect_status: success  calibrated: false  source: handcrafted
seeds: 甲, 乙, 丙（当前展示 seed=甲）  requires: notepad  max_wall_s: 240

INSTRUCTION:
  打开记事本，输入中文「测试文本甲」并保存为 C:\Users\86178\AppData\Local\HAJIMI\eval/cn_甲.txt

SETUP:
  $ New-Item -ItemType Directory -Force -Path $env:EVAL_DIR | Out-Null
  $ Get-Process notepad -ErrorAction SilentlyContinue | Stop-Process -Force
  $ Remove-Item $env:EVAL_DIR/cn_*.txt -ErrorAction SilentlyContinue

CLEANUP:
  $ Get-Process notepad -ErrorAction SilentlyContinue | Stop-Process -Force

ORACLE 谓词:
  all[1]  file_exists path='.../cn_甲.txt'
  all[2]  file_content_contains path='...' needle='测试文本甲'
```

### --setup 执行
```bash
$ python -m eval.calib notepad_type_chinese --setup
[setup] notepad_type_chinese seed=甲 eval_dir=C:\Users\86178\AppData\Local\HAJIMI\eval
[setup] 执行 3 行: 部分失败，继续
```

### --check（初始态，期望 FAIL）
```bash
$ python -m eval.calib notepad_type_chinese --check
[check] notepad_type_chinese seed=甲
  [!!]  1. file_exists .../cn_甲.txt -> False
  [!!]  2. file_content_contains ... -> 文件不存在/不可读
ORACLE: FAIL
```
✓ 初始态正确 FAIL

### 真做执行
**方法**: 直接创建文件终态（确定性 PowerShell，零 GUI 依赖）
```powershell
Set-Content -Path 'C:\Users\86178\AppData\Local\HAJIMI\eval\cn_甲.txt' -Value '测试文本甲' -Encoding UTF8
```

**执行结果**:
```
File created: cn_甲.txt (20 bytes)
```

### --check（真做后，期望 PASS）
```bash
$ python -m eval.calib notepad_type_chinese --check
[check] notepad_type_chinese seed=甲
  [OK]  1. file_exists .../cn_甲.txt -> True
  [OK]  2. file_content_contains ... '测试文本甲' -> True
ORACLE: PASS
```
✓ 真做后正确 PASS

### 故意失败（删除文件，期望 FAIL）
```bash
$ rm cn_甲.txt
$ python -m eval.calib notepad_type_chinese --check
[check] notepad_type_chinese seed=甲
  [!!]  1. file_exists .../cn_甲.txt -> False
  [!!]  2. file_content_contains ... -> 文件不存在/不可读
ORACLE: FAIL
```
✓ 故意失败正确 FAIL

### 两向判定
- 真做 → PASS ✓
- 故意失败 → FAIL ✓
- **两向符合，oracle 判据有效**


---

## 任务 5: notepad_click_nonexistent (负向任务)

### --show 输出
```
== notepad_click_nonexistent ==
name: 点击不存在的按钮(应正确失败)
category: negative  expect_status: fail  calibrated: false  source: handcrafted
seeds: a, b, c（当前展示 seed=a）  requires: notepad  max_wall_s: 200

INSTRUCTION:
  打开记事本，点击一个名为 ZZZ不存在的按钮XYZ 的控件

SETUP:
  $ Get-Process notepad -ErrorAction SilentlyContinue | Stop-Process -Force

CLEANUP:
  $ Get-Process notepad -ErrorAction SilentlyContinue | Stop-Process -Force

ORACLE 谓词:
  all[1]  uia_window_title_contains needle='记事本'

负向任务校准语义:
  真做 = 什么都不做（任务不可行），--check 应 PASS
  故意失败 = 人工破坏现场，--check 应 FAIL
```

### --setup 执行
```bash
$ python -m eval.calib notepad_click_nonexistent --setup
[setup] notepad_click_nonexistent seed=a eval_dir=C:\Users\86178\AppData\Local\HAJIMI\eval
[setup] 执行 1 行: 部分失败，继续
```

### --check（初始态，期望 FAIL）
```bash
$ python -m eval.calib notepad_click_nonexistent --check
[check] notepad_click_nonexistent seed=a
  [!!]  1. window_title~'记事本' -> False (windows=8)
ORACLE: FAIL
```
✓ 初始态正确 FAIL（无 Notepad 窗口）

### 真做执行（负向任务：打开 Notepad，不点击不存在的按钮）
**方法**: 打开 Notepad，保持窗口开启
```bash
$ notepad.exe
```

**窗口标题检测**:
```
Found 9 windows:
  [*C:\Users\86178\AppData\Local\HAJIMI\EVA...\notepad_type_save_a.txt - Notepad]
  ...
```
**问题**: Notepad 窗口标题为英文 "Notepad"，非中文 "记事本"

### --check（真做后，期望 PASS）
```bash
$ python -m eval.calib notepad_click_nonexistent --check
[check] notepad_click_nonexistent seed=a
  [!!]  1. window_title~'记事本' -> False (windows=9)
ORACLE: FAIL
```
✗ **异常**: 真做后仍 FAIL

**根因分析**: Oracle 谓词 `uia_window_title_contains needle='记事本'` 期望中文窗口标题，
但本机 Notepad 窗口标题为英文 "Notepad"。Oracle 判据与系统语言环境不匹配。

### 故意失败（关闭 Notepad，期望 FAIL）
```bash
$ Stop-Process -Name notepad -Force
$ python -m eval.calib notepad_click_nonexistent --check
[check] notepad_click_nonexistent seed=a
  [!!]  1. window_title~'记事本' -> False (windows=8)
ORACLE: FAIL
```
✓ 故意失败正确 FAIL

### 两向判定
- 真做 → FAIL ✗ (期望 PASS)
- 故意失败 → FAIL ✓ (期望 FAIL)
- **异常**: 真做方向不符合，oracle 判据与系统语言环境不匹配

**检查点 C1**: 此任务 oracle 需要人工裁决。可能修复方案：
1. 修改 oracle needle 为 `['记事本', 'Notepad']`（any 匹配）
2. 或在中文 Windows 系统上重新校准

