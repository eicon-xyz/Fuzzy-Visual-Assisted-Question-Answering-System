# HAJIMI 答辩宣讲稿（完整版 · 可直接宣讲）

---

> 对应 PPT：`HAJIMI-答辩PPT-6章版V2.pptx`（共 28 页、6 章）
> A 端源码目录：`server_A/server/`
> B 端源码目录：`HAJIMI_UI/`
> Web 管理后台目录：`web-admin/`

---

## 幻灯片 1 — 封面

各位老师好，我们是第 32 组「大狗嚼组」。今天汇报的项目是 **HAJIMI 自动操作助手**——一个模糊视觉辅助问答系统。

我是答辩人潘振喆，负责 B 端桌面客户端开发。同组成员还有负责 A 端后端与 AI 的杨名，以及负责 C 端集成、语音与管理后台的涂浚稷。接下来由我代表团队，从六个部分展开汇报。

---

## 幻灯片 2 — 目录

（过渡页，一句话带过即可）

---

## 幻灯片 3-4 — 团队概况

我们是 2026 年重庆大学赋能实训第 32 组，项目方向是 AI 驱动的桌面自动化助手。

我们的分工原则是按系统 A/B/C 三端来划分职责。A 端是后端和 AI 服务，B 端是桌面客户端，C 端是集成、语音和 Web 管理。我们采取"接口契约先行"的策略——先约定好三端之间怎么通信、传什么数据，然后各自并行开发，每天站会同步进度。这个模式贯穿了整个开发周期，保证了开发效率。

---

## 幻灯片 5 — 团队成员

团队一共三人，各司其职。

杨名负责 A 端——服务端、LLM 任务规划、安全校验，一共实现了 31 个 HTTP 接口。我负责 B 端——悬浮面板、屏幕覆盖标注、自动执行引擎，还有五套界面主题。涂浚稷负责 C 端——语音交互、审计系统、Web 管理面板，还承担了系统测试工作。

三位成员覆盖了前端、后端、AI 和集成测试的完整链路，互补性很强。

---

## 幻灯片 6 — 协作方式

在协作方式上，我们有四个核心实践。

第一，Git 分支开发加规范提交，所有功能在独立分支上开发，通过 PR 合并。

第二，接口契约先行。我们先约定好 A 和 B 之间的 7 个核心接口和 9 个管理接口、A 和 C 之间复用管理接口、B 和 C 之间通过 Qt 信号通信。三端在契约框架内并行推进，谁也不会阻塞谁。

第三，每日站会，同步进度、暴露阻塞点、持续联调。

第四，文档全程归档。需求文档、设计文档、接口定义、测试用例全部记录留存。

---

## 幻灯片 7-8 — 背景与痛点

我们为什么要做这个项目？因为在日常使用电脑的过程中，我们发现了六个普遍存在的痛点。

第一，操作门槛高。很多软件的菜单层级深、功能繁多，普通用户经常不知道从哪里下手。

第二，找不到入口。用户心里清楚自己要做什么，比如"我想把字体调大一点"，但就是找不到那个设置按钮藏在哪个菜单里面。

第三，教程对不上。网上搜到的教程截图跟用户自己电脑上的软件版本不一致，越看越困惑。

第四，切换繁琐。一个任务需要打开好几个窗口来回操作，费时间还容易出错。

第五，特殊人群更难。老年用户面对密密麻麻的界面完全无从下手，视障用户更是寸步难行。

第六，流程记不住。连续多步操作容易遗漏某一环，一旦中间出错就得从头再来。

这些痛点指向同一个核心矛盾：用户知道要做什么，但不知道怎么操作。于是我们做了 HAJIMI——让用户用一句话描述目标，系统自动帮他分析屏幕、规划步骤、指引操作。

---

## 幻灯片 9 — 目标用户与项目价值

我们的目标用户包括四类人群：新手用户、老年用户、视障用户，以及日常工作繁忙的普通用户。

项目的价值我们总结为四个关键词。

**降门槛**——用户不需要学习软件操作，一句话表达目标就行。

**提效率**——自动化规划与执行，省去手动探索的时间。

**无障碍**——我们做了专门的适老主题，叫"橘猫耄耋"，大字号、高对比度、大按钮；加上语音交互功能，用户说出来就行，不需要盯着屏幕、不需要动鼠标。

**安全可控**——这是我们的底线。每一步操作都要经过风险评分和红线拦截，自动化永远在安全边界内运行，不会越界。

---

## 幻灯片 10-11 — 软件工程实践

在工程实践层面，我们做了四件事。

第一，规范开发流程。按照需求、设计、开发、测试、交付的标准流程推进，每个里程碑做阶段评审。

第二，分层与解耦。系统采用四层架构——表示层、应用层、服务层、数据层——加上 A、B、C 三端分离，接口契约先行，保证各个模块之间低耦合、高内聚。

第三，全面测试。我们做了单元测试、接口测试、集成测试和端到端测试，一共 237 项，覆盖正常流程和异常路径。

第四，文档与版本管理。全套设计文档加上 Git 版本协作，所有产出物统一归档、可追溯。

---

## ═══════════ 幻灯片 12-13 — 核心功能一览 ═══════════

接下来是本次汇报最核心的部分——我们的六大功能模块。这六个功能覆盖了从"让用户看得懂"到"帮用户完成操作"再到"让管理员管得住"的完整链路。

这六大功能分别是：智能指引、自动执行、安全护栏、语音交互、多套主题，以及 Web 管理后台。下面我逐一展开讲解。

---

### ═══ 功能一：智能指引 — 直接画在屏幕上的操作指示 ═══

这是整个系统最核心的用户体验。当用户提出一个任务——比如"帮我把壁纸换成这张图片"——系统不是简单地回复一段文字，而是直接在用户屏幕上画出标注：高亮框框住要点击的按钮、箭头指向要去的位置、每个框上标着步骤序号。用户看到屏幕就知道该点哪里、按什么顺序来。

这个功能的实现，我们把它拆成了五个环节来设计。

**第一个环节是截屏。** B 端客户端收到用户指令后，立即截取用户的全屏画面。这里我们没有用 Python 自带的截图方法，而是选用了 mss 这个高性能 C 语言库，比系统原生方法快三到五倍。截图我们还做了一层优化：900 毫秒内的重复请求会复用同一张截图，避免反复截屏造成 CPU 空转。截图在发送之前会缩放压缩，最长边控制在 720 到 1920 像素，质量调到 82% 到 88%，大幅减少网络传输时间。

**第二个环节是视觉解析。** 截图以 base64 编码发给 A 端服务，A 端再转发给部署在 NVIDIA A800 GPU 上的 OmniParser 视觉检测服务。OmniParser 内部跑着两个模型：YOLO 负责检测屏幕上每个按钮、输入框、菜单在哪里，Florence-2 负责识别这些控件上写着什么文字。检测完成后，它不仅返回每个 UI 元素的坐标和文字，还生成一张"带编号的标注图"——图上每个控件都画了框和编号，一目了然。除此之外，我们还做了一件重要的事：为每个元素计算它跟周围元素的空间关系——谁在它左边、谁在它右边、谁在它上面、谁在它下面。如果没有这一步，LLM 就只知道一堆元素，不知道它们的相对位置，自然也就无法理解"搜索框右边的那个按钮"是什么意思。

**第三个环节是 LLM 规划。** A 端把标注后的截图和元素列表一起发给大语言模型。这里有一条优化的管线：规划 LLM 和 OmniParser 是并行跑的——规划是纯文本任务，不需要等待视觉检测的结果；OmniParser 也不依赖规划的输出。两条线同时进行，等两边都跑完了我们再合在一起，这样整体延迟取的是两者的较慢者，而不是两者之和。LLM 收到所有信息后，把用户的自然语言指令分解成原子操作步骤——打开某个应用、点击某个位置、输入什么内容——每一步都用自然语言描述清楚，并且带上对应的坐标框。

**第四个环节是坐标映射。** 这是容易被忽略但极其关键的一步。LLM 返回的坐标是基于参考分辨率的——我们约定是 1920 乘 1080——但用户的实际屏幕可能是 2560 乘 1440，也可能开了 125% 的 Windows 缩放。如果直接把参考坐标画到屏幕上，所有框都会偏。我们的做法是三步变换：先按比例从参考分辨率映射到截图像素，再除以 DPI 缩放比例得到 Qt 窗口的逻辑坐标。这样无论是 4K 屏还是高分屏、无论 Windows 缩放设成多少，标注框都能精准地打在目标位置上。

**第五个环节是覆盖层渲染。** B 端创建一个全屏透明的 Qt 窗口，设置成无边框、始终置顶、鼠标可穿透。在它上面用 QPainter 画三类东西：高亮框是半透明红色圆角矩形，左上角有一个带步骤编号的小圆标，用户一眼就知道这是第几步、该点哪里；箭头是从一个点指向另一个点的粗线加三角箭头，用来引导用户的视线和鼠标移动方向；检验模式下还会显示蓝绿色的半透明框，把 OmniParser 检测到的所有 UI 元素都标出来，方便调试。每个高亮框上面还叠加了一个独立的热区控件，用户可以点击这个区域——但区域外完全不受影响，不会阻挡用户正常的桌面操作。

---

### ═══ 功能二：自动执行 — 系统替你完成操作 ═══

如果说智能指引是"告诉你怎么做"，那自动执行就是"替你做了"。这个功能的实现核心是一个 LLM 驱动的 Agent——它就像一个虚拟的操作员，能看到屏幕、能操作键盘鼠标、能判断自己做对了没有。

我们在 Agent 里注册了 18 个工具，分成两组。桌面操作组有 11 个工具：启动应用、获取屏幕信息、单击、双击、粘贴文本、输入文字、按键、滚轮、等待、标记完成、标记失败。浏览器操作组有 7 个工具：导航到网页、获取页面快照、点击元素、输入文字、滚动、截图、按键。这些都是以 OpenAI Function Calling 的格式定义的，LLM 可以像调用函数一样选择和执行它们。

每个步骤的执行过程是一个闭环。Agent 先调用"获取屏幕信息"来看当前屏幕上有哪些元素，每个元素有哪些邻居。然后它根据自己的任务目标，选择最合适的工具——比如当前步骤是"打开记事本"，它就直接调用"启动应用"工具，不需要先看屏幕。执行完后，它会验证：输入文字之后窗口内容变了吗？点击之后有新的页面出现吗？验证通过了就标记完成，进入下一步；失败了就重试，最多重试一次，重试还不行就标记失败。整个流程安全、可追溯——每一步的执行记录都被记录下来，前端通过服务器推送事件实时看到进度。

应用启动这个工具值得单独讲一下。因为 Windows 上应用安装方式五花八门——有的在系统 PATH 里、有的是 UWP 商店应用、有的是自定义安装目录——所以我们设计了一个三级回退策略。第一级是映射表匹配：我们维护了一个包含七十多个中英文常用应用名的映射表，"计算器"直接映射到 calc.exe、"微信"映射到 WeChat.exe。如果表里找不到，走第二级——在系统 PATH 里搜索。如果 PATH 里也找不到，走第三级——模拟键盘操作，按下 Win 键打开开始菜单，用剪贴板粘贴应用名搜索，回车启动。三级策略覆盖了绝大多数情况。

浏览器操作是基于 Playwright 的 Chromium 控制。跟桌面操作不一样，浏览器里我们走的是 DOM 级别的精确操作——不再依赖视觉识别坐标，而是直接通过 CSS 选择器定位元素、通过 JavaScript 设置输入框的值。这样操作更精准、更快、也更稳定。

整个执行过程通过服务器推送事件实时展示进度：每一步开始时推一条消息、每个工具调用时推调用名和参数、每次截屏时把标注图推给前端展示、最终任务完成或失败时也发事件通知。这背后是一个线程安全的队列机制——引擎线程往队列里放事件，HTTP 连接从队列里取事件发给前端，如果 30 秒没有新事件就发一个心跳包保活。

---

### ═══ 功能三：安全护栏 — 不让自动化越界 ═══

安全是自动化助手不可逾越的底线。系统设计了三层安全防护。

**第一层，在用户输入进入系统之前，B 端先做一轮本地关键词预检。** 像"自动抢票""自动点击""扫描硬盘""查看聊天记录"这类明显越界的请求，在发送截图之前就被拦截了，不会消耗服务器资源。

**第二层，A 端的输入红线检测。** 我们在 A 端维护了三类红线模式：物理操作类——比如"帮我自动刷票""脚本批量点击"——直接拒绝；隐私侵犯类——比如"扫描所有文件""查看别人的聊天记录"——用引导式的方式拒绝，告诉用户"我做不到这个，但我可以教你怎么操作文件管理器"；还有一种实时动态类——用户如果在看直播画面或者股票行情，屏幕是持续变化的，我们的分析可能滞后，这种情况下会给出降级提示，让用户知道结果的时效局限。

**第三层，也是最细粒度的一层，执行层的安全分类。** Agent 在执行每一步操作之前，都要先做安全检查。我们定义了 23 条红线——批量删文件、窃取密码、自动付款、DDoS 攻击、恶意软件、伪造证件等等——命中任何一条立即阻止。12 条黄线——安装软件、修改系统设置、命令行操作、关机重启等等——命中后会标记警告但仍然允许执行。还有 9 条绿名单豁免——打开应用、查看桌面、调整音量、新建文件夹这些日常操作——先于红黄线检查，确保正常的操作体验不受影响。

这样三层叠加，保证了系统在为用户提供便利的同时，绝不会逾越安全边界。

---

### ═══ 功能四：语音交互 — 说出来就能操作 ═══

语音交互分两个方向：输入和输出。

输入方向，用户点击语音按钮后说话，语音识别模块将音频转成文字，填入输入框并自动触发任务处理。识别失败时会有友好的错误提示。输出方向，系统在执行每一步时，都会自动把步骤描述通过语音合成播报出来——比如"第一步，打开文件资源管理器"——用户不需要盯着屏幕就能跟着走。

语音的开关和参数可以在系统设置面板中自由调节。这个功能跟适老主题配合得特别好：老年用户不用看屏幕、不用敲键盘，说出来就行。

B 端和 C 端的语音模块之间通过 Qt 的信号机制通信——B 端发一个"请播报这段文字"的信号，C 端收到后执行并播报；C 端语音识别出结果后，通过另一个信号回传给 B 端处理。两个方向完全解耦，互不阻塞。

---

### ═══ 功能五：多套主题 — 好看且好用 ═══

系统提供了五套界面外观方案。

**默认蓝**——经典的深蓝风格，沉稳专业。

**典雅黑**——采用了我们称之为 Crystal 玻璃壳的技术，窗口是半透明的毛玻璃效果，边框极细。阴影是通过 Qt 的图形特效动态计算的——不是贴图的假阴影，而是根据用户在设置面板中拖动的强度滑块实时算出来的模糊半径和偏移量。看上去轻盈通透。

**黑金轻奢**——这套主题有自己独立的渲染引擎。不依赖 QSS 样式表，而是用 QPainter 直接绘制背景——可以选毛玻璃质感的暗色背景，也可以选牛皮纸纹理的暖色背景。文字用了金色渐变，标题区换成了书法字体。用户还能在设置里选择金色的渐变方式——水平渐变、对角渐变、或者双层叠加——每种效果都不同。

**牛皮纸**——黑金轻奢的一个变体，换成了暖调的牛皮纸底色，更适合长时间使用，不刺眼。

**橘猫耄耋**——这是专门为老年用户设计的适老主题。大字号、高对比度的配色、大按钮方便触摸。还支持启动音效。这套主题的核心设计理念是"看得清、点得准"。

用户在系统设置里选择主题后，后台会把对应的样式表和渲染器参数即时加载、全局应用。整个切换过程不需要重启，一两秒钟就能完成，所有打开的窗口同步刷新。

---

### ═══ 功能六：Web 管理后台 — 可视化的运营看板 ═══

管理后台有六个页面，基于 Vue 3 加 Element Plus 开发，通过 A 端的管理接口获取数据。

**仪表盘**——页面上方是四个 KPI 卡片：总任务数、成功率、平均步骤数、平均耗时，每个指标旁边标了较昨日的涨跌趋势。下面是两个饼图和两个折线图，分别展示用户反馈的分布、不同处理路径的占比，以及 24 小时内事务量和响应时长的变化趋势。图表用 ECharts 渲染，所有数据实时从数据库拉取。

**失败归因**——这是我们特别看重的一页。左侧是失败类型的柱状图和趋势折线图，点击柱子可以筛选列表。右侧是失败任务列表，点击任意一行会弹出详情抽屉——里面包含了那次任务发给 LLM 的完整 prompt、标注后的截图、LLM 的原始返回。这意味着每一次失败都是可追溯、可分析的，我们能据此持续优化提示词和模型参数。

**数据流监控**——展示请求链路上每一步的耗时和状态，方便定位性能瓶颈。

**用户管理**——管理用户账号、角色和权限。

**系统配置**——在线调整 LLM 参数、OmniParser 地址、安全策略，配置保存后即刻生效，不需要重启服务。

**健康监控**——实时展示三进程的运行状态：B 端客户端、A 端服务、OmniParser 服务。GPU 的状态也在这里——型号、显存用量的进度条、是否就绪。

后端对应的数据库有七张表，分别是用户表、任务事务表、步骤日志表、反馈表、失败记录表、系统配置表和红线拦截日志表。每一张表服务于一个具体的运维需求，形成完整的数据闭环。

---

## ═══════════ 幻灯片 20-24 — 技术架构 ═══════════

### 四层架构 + 三进程流水线

系统的技术架构可以概括为"四层架构、三进程流水线"。

四层分别是：表示层、应用层、服务层、数据层。

表示层负责"给人看"——B 端是 PyQt5 桌面悬浮面板加上全屏透明覆盖层，用来显示标注；C 端是 Vue 3 的 Web 管理面板，用来展示运营数据。

应用层负责"编排控制"——在 B 端，执行引擎管理着任务线程、步骤状态机驱动整个执行流程、审计代理记录每一步操作。

服务层负责"核心智能"——A 端基于 FastAPI 提供 31 个 REST 接口，LLM 规划模块生成操作步骤、安全校验模块守住底线。

数据层负责"持久化与感知"——七张 SQLite 表做结构化存储、OmniParser 在 GPU 上做视觉检测、云端多模态 LLM 做推理和规划。

三进程之间的流水线是：B 端客户端截屏后通过 HTTP 发给 A 端 8010 端口，A 端转发给 OmniParser 的 9800 端口做视觉检测，A 端再调云端 LLM 做规划，最后把结果返回 B 端渲染到覆盖层上。

### 关键技术设计

这里有三个关键设计值得讲。

第一，**接口契约驱动**。A 和 B 之间的接口在开发前就约定好了——请求的 JSON 字段、返回的结构、错误码的格式——然后两端各自实现。这样我们三个人可以同时在三个端上推进，彼此不阻塞。最终交付了 31 个 HTTP 接口，还有 B 端内部 9 个 Qt 信号。

第二，**SSE 实时推送**。用户提交任务后，系统创建一个后台线程执行。执行过程中每发生一个事件——开始执行某一步、调用了某个工具、截了一张图——就放进一个线程安全的队列。前端通过 /stream 端点长连接持续从这个队列中取事件，实时刷新界面。无事件时每 30 秒发一个心跳包保持连接。这个机制让整个执行过程对用户完全透明——用户能看到 Agent 正在干什么、屏幕上的标注在怎么变化。

第三，**两条处理管线**。系统同时运行两条管线。一条是自动执行管线，走完整的流程：红线检测→意图分类→LLM 规划加 OmniParser 视觉检测并行→Agent 循环自动执行，适合需要 GPU 支持的复杂任务。另一条是快速指引管线，只用一次 LLM 调用同时完成规划和定位，不依赖 OmniParser，适合简单任务和对响应速度要求高的场景。两条管线根据部署模式和任务复杂度自动选择。

### 技术栈总览

B 端桌面端用 Python 加 PyQt5 做 GUI、mss 做高性能截屏、pyautogui 做键盘鼠标操作、pyperclip 做剪贴板控制。

A 端服务端用 FastAPI 框架加 SQLAlchemy ORM，内外部通信走 httpx，事件推送走 SSE。

AI 层面，视觉检测用微软的 OmniParser V2——YOLO 图标检测加 Florence-2 图像描述——部署在 NVIDIA A800 GPU 上；大模型推理接云端的多模态 LLM。

浏览器自动化基于 Playwright 的 Chromium CDP，实现了 7 个 DOM 操作工具。

Web 管理后台用 Vue 3 加 Vite 加 Element Plus 加 ECharts 加 Pinia。

---

## ═══════════ 幻灯片 26-27 — 总结与展望 ═══════════

### 项目总结

最后做一个总结。

HAJIMI 实现了一件挺有意义的事：让普通用户用一句话就能完成原本繁琐的桌面操作，把"不会用软件"的门槛降到最低。

我们做到了三端联通——B 端桌面、A 端服务、AI 端视觉检测全链路贯通。六大功能完整可用——智能指引让用户看得懂、自动执行帮用户干完、安全护栏保底不出事、语音交互解放双手、多套主题照顾不同人群、Web 管理让运维有据可查。237 项测试全部通过，覆盖正常流程和异常路径。

更重要的，我们始终在安全边界内运行。每一步操作都有风险评分，每一个越界的请求都会被红线拦截。自动化既好用、又可放心。

### 未来展望

展望未来，我们有两个方向。

**更聪明**——打磨语音的全链路体验，让语音交互更流畅、更自然；同时提升 LLM 对复杂多步任务的规划和执行能力。我们的数据库里已经设计好了用户记忆系统，可以记录用户的成功经验和失败教训，未来可以通过这些记忆来个性化系统的行为，让它越用越懂你。

**更普适**——适配多屏环境和高分屏场景，让系统在更多硬件配置下稳定运行；同时扩展支持的应用和网页操作范围，覆盖更多实际工作场景。

---

## 幻灯片 28 — 感谢页

以上就是我们第 32 组的全部汇报内容。感谢各位老师的聆听，恳请批评指正。谢谢大家！

---

> **宣讲节奏建议**：
> - 幻灯片 1-2（封面+目录）：约 1 分钟
> - 幻灯片 3-6（团队介绍）：约 2 分钟
> - 幻灯片 7-9（背景与价值）：约 3 分钟
> - 幻灯片 10-11（软件工程）：约 1 分钟
> - 幻灯片 12-19（核心功能六大模块）：约 6-8 分钟（这是重点，建议讲详细些）
> - 幻灯片 20-24（技术架构）：约 3-4 分钟
> - 幻灯片 25-28（总结展望+感谢）：约 2 分钟
> - **总计：约 16-20 分钟**

---

# 答辩可能被问的问题（附参考回答）

---

## Q1：用户输入一句话后，系统从开始到结束做了什么？

**这是最核心的问题，建议用"一条命令的旅程"来组织回答。**

> 好的老师，我以用户输入"帮我打开浏览器搜索天气"为例，梳理一下从输入到完成的完整流程。

**第一阶段：B 端本地预处理（约 0.05 秒）**

用户说完话（或者打完字），B 端首先做一个本地红线预检。系统内置了一个敏感词列表，像"自动抢票""查看聊天记录"这些明显越界的请求——在 VScode 的 screen_utils.py 里定义了十来个关键词——在这第一步就会拦截，不会浪费后面任何资源。通过了这一步，B 端开始截屏。这里用的是 mss，一个 C 语言实现的高性能截图库，抓一帧只要几十毫秒。截到的图是物理像素——比如 2560 乘 1440——但为了减少传输，系统会用 LANCZOS 算法把它缩到最长边 1920 像素，转成 JPEG base64，质量 85% 左右。同时系统算一个 SHA-256 的前十六位当屏幕指纹——这是后面判断"屏幕有没有变化"用的。

**第二阶段：网络通信（约 0.1-0.3 秒）**

B 端把用户的指令、截图 base64、屏幕尺寸、窗口标题、屏幕指纹这些信息打包成一个 JSON，POST 到 A 端的 `/api/demo/execute` 接口。请求头里带了一个 X-Demo-Key，防止未授权的调用。

**第三阶段：A 端安全检测加意图分类（约 0.05 秒）**

A 端收到请求后，第一步是执行层的安全检查。这一步比 B 端本地检查更细——不是简单的关键词匹配，而是三段式分类：先走绿名单豁免（像"打开浏览器"这种正常操作直接放行），然后走红名单拦截（像"批量删除文件"这种红区操作直接拒绝），最后走黄名单标记（像"修改系统设置"这种需要用户确认）。通过了安全检测后，系统对用户意图做分类——SetFit 模型加关键词回退——判断这是操作指引类还是错误诊断类、文件管理类还是界面导航类，一共九个类别。意图分类结果会影响后续 LLM 规划的策略。

**第四阶段：LLM 部署加 OmniParser 并行（约 2-5 秒）**

这是耗时最长的阶段。系统做了并行优化：LLM 规划和 OmniParser 视觉检测是同时跑的，互不依赖。规划这一侧——A 端把用户指令发给 LLM，要求它把"帮我打开浏览器搜索天气"拆成原子步骤。LLM 返回的结果是 JSON 格式，包含一个目标描述和步骤列表——比如第一步打开浏览器、第二步在浏览器里搜索天气。视觉这一侧——A 端把截图发到 GPU 上的 OmniParser，YOLO 模型检测按钮和输入框的位置，Florence-2 模型识别上面的文字。OmniParser 返回每个元素的坐标、类型、文字，以及一张标注过的图。两条线都跑完后合在一起：LLM 规划出来的步骤跟 OmniParser 检测到的元素做匹配——比如规划说"点击搜索输入框"，系统就去元素列表里找一个 type 为 input 的元素。

**第五阶段：结果返回（约 0.05 秒）**

A 端把任务 ID、规划出来的步骤列表、标注后的截图，打包返回给 B 端。同时 A 端在后台启动了一个执行线程，把这个任务注册到事件队列里——前端可以通过 `/stream/{task_id}` 实时看到执行进度。

**第六阶段：B 端渲染覆盖层（约 0.1 秒）**

B 端拿到结果后，走坐标映射管道：首先把 LLM 返回的参考坐标（1920×1080 基准）缩放到实际截图像素，再除以 Windows DPI 缩放比例（假如是 1.25 倍，就除以 1.25），得到 Qt 窗口的逻辑坐标。然后 B 端创建一个全屏透明的覆盖窗口，在上面画高亮框、步骤编号和箭头。用户就看到屏幕上直接标注出了"该点哪里"。

**第七阶段（如果用户选择自动执行）：Agent 循环**

A 端的后台线程启动了执行引擎，引擎为每个步骤调用 LLM Agent。Agent 先调"获取屏幕信息"工具——实际上就是再截一次屏、再跑一次 OmniParser——拿到当前的 UI 元素列表。然后它根据当前步骤的目标，选一个工具来执行。比如第一步是"打开浏览器"，它就调 launch_app("Chrome")。这个工具内部走三级回退——先在 70 多个应用的映射表里找、找不到就去系统 PATH 找、还找不到就模拟 Win 键加搜索。工具执行完了，Agent 验证结果：浏览器窗口出来了吗？出来了就标记步骤完成，进入下一步。下一步要"输入搜索词"，Agent 再截一次屏，在元素列表里找到地址栏或搜索框，调 type_text 工具把"天气"粘贴进去，再按回车。整个过程每一步的状态都通过 SSE 实时推给 B 端，用户能看到 Agent 在做什么。所有步骤执行完毕后，任务结束，系统从内存里提取这次操作的成功经验存到数据库里。

**时间总结**：从用户输入到看到覆盖层标注，如果是 GPU 模式、简单任务，大概 3 到 6 秒。如果是 CPU 模式跑 OmniParser，可能要 2 到 4 分钟——这也是我们为什么推荐用 GPU 模式的原因。

---

## Q2：OmniParser 检测不准怎么办？比如屏幕上有个按钮它没认出来。

> 这个问题我们做了好几层防护。

首先，OmniParser 本身是两个模型的组合——YOLO 负责检测"有没有东西"，Florence-2 负责识别"这个东西写了什么"。如果 Florence-2 没认出来文字，元素还是会有一个空的 text 字段，但 bbox 和 type 是有的。LLM 在规划的时候，我们给它的提示词里专门写了：不要只依赖 text 字段来定位元素，也要利用 element_type 和空间关系——比如"搜索框右边的、类型是 button 的元素"。

其次，Agent 执行层面也做了容错。如果 click 一个 element_id 后屏幕没有变化——通过比较前后两次截屏的元素指纹来判断——Agent 会尝试别的方式，比如用 Tab 键切换焦点、或者先等待一秒让界面响应、或者调用重新获取屏幕信息的函数。

还有，我们有一个"步骤重定位"的能力。如果用户在执行过程中手动点到了别的界面，系统可以对当前步骤重新截屏、重新跑 OmniParser，在当前画面上重新找到目标元素。Agent 循环里也有重试机制——每一步如果执行失败，系统会重试一次，重试时清空之前缓存的元素列表、重新截屏解析。

最后，我们的失败归因页面会把每次失败时发给 LLM 的完整 prompt 和 OmniParser 返回的元素列表都记录下来。我们可以通过这些数据来分析是 OmniParser 漏检了、还是 LLM 定位错了，然后针对性地改进 prompt 或者模型参数。

---

## Q3：安全护栏怎么保证不误杀正常请求？绿色操作怎么保证不被拦截？

> 好问题。我们的安全机制有一个明确的匹配优先级：**先绿、后红、再黄**。

具体来说，每次安全检查的时候，系统先跑绿名单。绿名单有 9 条规则，覆盖了最常见的正常操作——打开应用、查看桌面、调整音量、新建文件夹、打字输入、点击按钮等等。如果用户说的是"打开浏览器"，绿名单第一条就命中了，直接返回"放行"，根本不会去匹配红名单。

只有绿名单没命中，我们才去看红名单。红名单的 23 条规则写得比较严格——比如"删除所有文件"这种明确的危险操作才会命中。我们避免使用"删除"这种宽泛的关键词单独作为红线，而是配合上下文——比如"删除所有/整个/全部"这类限定词——来降低误判。

黄名单介于两者之间，12 条规则覆盖了"需要告知但不必禁止"的操作，比如安装软件、修改系统设置。这些操作被标记后，系统会在执行前向用户确认，但不会直接拦截。

另外，B 端在发送请求前也有一道本地预检，但那只是第一关——它拦截的是最明显的违规，不会拦正常请求。真正的精细判断在 A 端，由 44 条正则规则分层把关。我们现在跑了 237 项测试，包括专门的安全测试用例，验证了正常指令不会被误拦、高危指令一定会被拦。

---

## Q4：你们怎么保证标注框的坐标是准的？高分屏、多屏怎么处理？

> 这个问题涉及我们系统里一个关键的设计——坐标映射管道。

坐标在整个系统里经过了三次变换。第一步，LLM 返回的坐标是基于 1920 乘 1080 的参考分辨率——这是我们在 prompt 里约定的统一基准。第二步，这些坐标被等比缩放到实际截图尺寸——比如用户的 2560×1440 屏幕，所有坐标乘以 2560÷1920。第三步也是最关键的一步——处理 DPI 缩放。Windows 系统在 2K、4K 屏上默认会开缩放，比如 125%、150%。mss 截出来的图是物理像素——2560 乘 1440——但 Qt 窗口工作在逻辑坐标下——只有 2048 乘 1152。如果直接把物理坐标画到逻辑坐标系里，所有框都会往右下偏。所以我们在绘制之前做了 DPR 感知转换：判断截图尺寸跟物理分辨率是不是一致，如果一致就除以 DPI 缩放比例，得到逻辑坐标。这套逻辑在 overlay_coords.py 里面就几十行代码，但花了我们不少调试时间——因为不同显示器、不同缩放比例下的表现都不一样。

目前系统主要针对单主屏做了适配，多屏场景和高分屏扩展是我们未来展望里提到的改进方向。

---

## Q5：自动执行失败了怎么办？比如点了一个按钮但软件没反应。

> 系统有几层应对机制。

首先是 Agent 内部的重试。每个步骤默认有最多 15 轮的工具调用机会。Agent 点了一个按钮之后，它会验证——不是盲目相信操作成功了，而是观察屏幕有没有变化。怎么判断有没有变化呢？它比较前后两次截屏的元素列表——如果元素 ID 集合的重叠度超过 80%，说明跟刚才一模一样，操作可能没生效。这时候 LLM 会尝试替代方案：等一等再操作、用 Tab 键换焦点、或者从另一个角度重新定位目标。

如果 15 轮内没搞定，Agent 会标记这个步骤失败。

然后是引擎层的重试。引擎在收到"步骤失败"后，会给这个步骤一次重试机会——重新截屏、清空缓存的元素列表、重新让 Agent 执行。如果第二次还是失败，引擎会标记整个任务失败。

最后是用户体验层。任务失败后，系统把失败记录写入数据库——包括失败类型、发生在第几步、当时的屏幕指纹、完整的 LLM prompt 和响应快照。管理后台的失败归因页面可以查看这些记录，运维人员点开详情就能看到 LLM 当时的思考过程——它看到了什么、它想做什么、为什么没做成。这些数据是持续优化系统的依据。

用户端看到的是一个友好的提示，告诉他在哪一步出了问题，用户可以随时重试整个任务。

---

## Q6：为什么不直接用现有的解决方案，比如微软 Copilot、或者 OCR 加固定脚本？

> 这个问题的核心是——Copilot 和传统 OCR 方案在桌面操作指引这个场景下都有各自的限制。

微软 Copilot 擅长的是对话、搜索、文档生成，但它不具备"理解你的桌面屏幕、告诉你下一步该点哪里"的能力。它不知道你的屏幕上现在显示的是什么。

传统的 OCR 方案——比如 Tesseract——能识别屏幕上的文字，但它面对图标按钮就很无力了。而且 OCR 只给你文字，不给你坐标、不知道按钮和输入框之间的空间关系。OmniParser 不一样——它同时检测 UI 元素的位置、识别文字、给出元素类型、还计算空间关系。LLM 收到的是结构化的"哪有个什么"的数据，不是一张纯图片或者一堆文字坐标。

固定脚本的局限更明显——换一个软件版本、换一种分辨率，脚本就可能失效。我们的方案是视觉加 LLM，它理解语义而不是记住像素，所以对软件版本的差异有天然的容忍度。

简单说，我们的核心思路是"视觉定位 + 语义理解"，而不是"坐标硬编码"。

---

## Q7：三端（A/B/C）怎么通信的？怎么保证各端独立开发不冲突？

> 我们用的是"接口契约先行"的策略。

A 端和 B 端之间通过 HTTP REST 通信。A 端暴露了 7 个核心接口和 9 个管理接口，B 端通过 api_client.py 调用。接口的请求格式和返回格式在开发前就定好了——用 Pydantic 定义了 schema，B 端和 A 端各自对着同一套 schema 开发。所以 B 端不需要知道 A 端内部怎么处理、A 端也不需要知道 B 端怎么渲染——只要双方遵守契约就行。

A 端和 C 端之间复用 A 端的管理接口——Web 管理后台通过 `/api/admin/*` 获取仪表盘数据、失败记录、系统配置等等。C 端的语音模块也通过 HTTP 跟后端的语音识别服务通信。

B 端和 C 端之间不走 HTTP，而是用 Qt 的信号机制。因为 B 端和 C 端的语音、审计模块在同一个进程里，没必要用网络通信。B 端发一个信号说要播报文字，C 端收到就执行；C 端语音识别出结果，发信号通知 B 端处理。两边完全不耦合。

这种方式的好处是，任何人修改自己端的内部实现，只要不违反接口契约，就不会影响其他人的工作。我们整个开发周期里，因为接口不匹配导致的问题不超过三次。

---

## Q8：237 项测试具体覆盖了哪些内容？怎么测试 GUI 相关的功能？

> 237 项测试分布在四个层面。

**单元测试**：核心算法逻辑——比如坐标映射的 adapt_annotation 函数、安全分类器的 check_step 函数、空间关系计算的 _compute_spatial_relations 函数。这些是纯逻辑测试，不依赖外部服务。

**接口测试**：A 端的 31 个 HTTP 接口——用 pytest 加 httpx 写了测试用例，覆盖正常请求、异常参数、未授权请求、超时场景。每个接口至少三到四个测试用例。

**集成测试**：把几个模块串起来测——比如截屏加编码加发送加解析这个链路、或者红线检测加意图分类加 LLM 规划这个链路。覆盖了正常路径和异常路径。

**端到端测试**：完整的用户操作流程——输入指令、等待系统处理、验证覆盖层渲染、模拟点击反馈。这部分在本地 GPU 环境和 Mock 模式下都跑过。

GUI 相关的测试比较特殊——PyQt5 的窗口和覆盖层没法像普通函数那样单元测试。我们的做法是：渲染逻辑单独抽出来验证——比如 annotation_mapper 函数，给它一组固定的输入数据，验证它输出的绘制项列表对不对。覆盖层本身的行为——窗口是否置顶、是否透明、鼠标是否穿透——这些通过手动测试加上脚本验证的组合来覆盖。模拟点击方面我们用了 pyautogui 写自动化脚本，验证按钮点击后信号的发射情况。

---

## Q9：LLM 的响应太慢怎么办？有没有做缓存或者加速？

> 我们做了几个层面的加速。

第一，管道并行。LLM 规划和 OmniParser 视觉检测是同时跑的——两者互不依赖，系统的总延迟取的是两者的较慢者，而不是两者之和。这是省时间最主要的手段。

第二，截屏复用。B 端在 900 毫秒内的重复截图请求会复用同一张截图——如果一个用户在短时间内反复点同一个按钮或者同一个任务被不同模块触发，不会重复截屏。截屏前还会做缩放和 JPEG 压缩来减少网络传输时间。

第三，LLM 的选择上。我们在设置里允许用户选择不同的速度模式——快速模式下用响应快的模型（比如 DeepSeek），平衡和精准模式下用视觉能力更强但稍慢的模型。用户可以根据任务复杂度和自己的耐心来选择。

第四，浏览器操作走 DOM 而不是视觉。在浏览器里我们直接用 Playwright 的 CSS 选择器定位元素——不需要 OmniParser 截屏加检测的完整流程。这比桌面操作用视觉定位快一个数量级。

我们没有做 LLM 结果的缓存——因为每个用户的屏幕内容都不一样，同样的指令在不同屏幕上需要不同的操作步骤。缓存的意义不大。

---

## Q10：部署模式怎么选的？GPU 和 CPU 模式有什么区别？

> 系统支持三种部署模式。

**GPU API 模式**——这是推荐模式。OmniParser 部署在远程的 NVIDIA A800 GPU 服务器上，通过 SSH 隧道映射到本地 9800 端口。A 端和 B 端都跑在本地。GPU 模式下 OmniParser 处理一帧只需要 2 到 5 秒。

**内网模式**——适用于校园网环境。A 端部署在远程服务器上，B 端通过 VPN 连接。OmniParser 和 A 端都在远端，B 端只做界面展示。

**本地 CPU 模式**——全部跑在本地，OmniParser 用 CPU 推理。一帧需要 2 到 4 分钟。仅用于开发调试或者没有 GPU 可用的情况。

部署模式可以在系统设置面板里一键切换。切换后 B 端会自动探测对应端口的服务是否可用，并给出提示。

---

## Q11：如果用户说的是很模糊的话，比如"帮我把这个弄好看一点"，系统怎么处理？

> 这涉及到我们系统的意图分类和澄清机制。

首先，意图分类器会把这句话判断为低置信度。因为"弄好看一点"不属于操作指引、错误诊断、文件管理等九个预定义类别中的任何一个——SetFit 模型的置信度可能低于 0.6，系统会标记为 needs_clarification 等于 true。

然后 LLM 在规划阶段收到这个模糊指令后，我们的 prompt 引导它不要瞎猜，而是反问用户。LLM 可能会在回复中说"您想调整窗口的外观主题？还是想修改文档的排版？"之类的话来澄清。

在智能指引模式下，如果 LLM 规划的步骤置信度不够高，系统不会创建蓝图直接执行，而是先跟用户确认。

不过我们设计上主要还是面向操作性的指令——"打开什么""搜索什么""调整什么"。纯模糊的审美类指令目前不是我们的核心场景，这也是未来可以改进的方向。

---

## Q12：你负责 B 端，你遇到了什么技术难点，怎么解决的？

> 我最大的难点是坐标映射管道的调试。

简单说就是——从 LLM 拿到坐标到最终在屏幕上画出框，中间要经过三次坐标变换。这里面涉及四个不同的坐标空间：LLM 的参考分辨率 1920 乘 1080、截图的物理像素比如 2560 乘 1440、Qt 窗口的逻辑坐标比如 2048 乘 1152、还有 Windows DPI 缩放比例 1.25。

问题出在不同显示器、不同缩放比例下，Qt 的 geometry 和 mss 的截图尺寸对不上。我一开始的映射逻辑比较简单——以为 mss 截图和 Qt 逻辑坐标之间就是线性的。结果在同事的 4K 屏上标注框全偏了，在 125% 缩放下框的位置也不对。

后来我发现关键在于 DPR。mss 截的是物理像素，Qt 画的是逻辑像素，两者之间差了一个 devicePixelRatio。我重新设计了 overlay_coords.py 的转换逻辑：先判断截图尺寸跟物理分辨率是不是一致，一致就除以 DPR；不一致就按比例缩放——三段分支覆盖了三种情况。

调试过程主要在同事的不同电脑上验证——2K 屏 125%、2K 屏 100%、4K 屏 150%、1080p 100%。每次换一台电脑坐标就出问题，改一次修一台。最后写了统一的 get_screen_metrics 函数，在系统启动时从 QScreen 对象读取当前显示器的逻辑分辨率、物理分辨率和 DPR，后续所有坐标换算都基于这三个参数，这才稳定下来。

另外覆盖层的透明窗口也花了不少时间。Qt 的 WA_TranslucentBackground 和 WA_TransparentForInput 配合 FramelessWindowHint 和 WindowStaysOnTopHint——这些标志位的组合有坑。比如单独用 TransparentForInput，鼠标穿透了但窗口收不到任何事件；加上 Tool 标志不显示在任务栏但在某些 Windows 版本上窗口会自动失去置顶。这些行为在 Win10 和 Win11 上还不太一样，最后还是读了 Qt 的源码注释才搞清楚各个标志位的精确语义。
