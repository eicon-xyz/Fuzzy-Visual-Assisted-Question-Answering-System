# HAJIMI 部署文档

> 适用对象：接收交付包后首次安装部署 | 版本：v1.0 | 2026-07-09

---

## 一、系统要求

| 项目 | 最低要求 | 建议 |
|------|----------|------|
| 操作系统 | Windows 10 x64 | Windows 11 x64 |
| Python | 3.12+（安装时勾选「Add to PATH」） | 3.12 |
| 内存 | 8 GB | 16 GB+（L5 Sidecar 含 PyTorch 约 2 GB） |
| 磁盘 | 5 GB 空闲（含三 venv） | 10 GB+ |
| 网络 | 首次 pip 安装需联网 | 后续可离线 |
| 浏览器 | Edge 或 Chrome（L5 网页自动化可选） | 最新版 Edge |
| LLM API Key | Vision 多模态模型（如 SiliconCloud Qwen-VL 或 GPT-4o） | — |
| GPU（可选） | OmniParser 远程 GPU 或本地 conda 环境 | 校园 GPU :9800 隧道 |

---

## 二、快速部署（5 步）

### 第 1 步：解压

将 `HAJIMI-release-YYYYMMDD.zip` 解压到**不含中文和空格**的路径，例如：

```
E:\HAJIMI\
```

确认目录结构：

```
E:\HAJIMI\
├── 安装全栈.bat
├── 启动全栈.bat
├── 验收.bat
├── 打包说明.md
├── HAJIMI_UI/          # B 端 + A 端 :8010
├── server_A/           # L5 Sidecar :8011
├── client/             # C 端 语音/审计
├── web-admin/          # 管理面板
├── models/             # Vosk 中文语音模型
└── launchers/          # 启动快捷方式
```

### 第 2 步：安装依赖

双击 **`安装全栈.bat`**，等待完成（首次 10–30 分钟）。

脚本自动执行：

1. B 端 UI venv（`HAJIMI_UI\.venv`）
2. A 端 :8010 venv（`HAJIMI_UI\server\.venv`）
3. L5 Sidecar :8011 venv（`server_A\server\.venv`，含 PyTorch，最慢）
4. 初始化 `.env` 并同步关键配置到 8011

### 第 3 步：配置 LLM API Key

编辑 **`HAJIMI_UI\server\.env`**，至少填写：

```ini
LLM_API_KEY=sk-your-key-here
LLM_BASE_URL=https://api.siliconflow.cn/v1
LLM_MODEL=Qwen/Qwen2.5-VL-72B-Instruct
```

> 模型必须支持 Vision（多模态），否则 L4 无法分析屏幕截图。

安装脚本已自动将关键配置同步到 `server_A\server\.env`（端口改为 8011），无需手动编辑第二份。

### 第 4 步：启动服务

双击 **`启动全栈.bat`**，将依次打开控制台窗口：

| 窗口标题 | 端口 | 作用 |
|----------|------|------|
| HAJIMI-A-end | 8010 | L3/L4 规划、inspect |
| HAJIMI-L5-Sidecar | 8011 | L5 自动执行（pyautogui） |
| HAJIMI-B-end | — | PyQt5 桌面 UI |

等待每个窗口稳定输出日志后，B 端 UI 窗口弹出。

### 第 5 步：验收

在 B 端 UI 启动后（保持 8010 + 8011 运行），双击 **`验收.bat`**。

通过标准：

```
verify_l4      PASS
verify_l5      PASS
verify_integration  PASS
```

---

## 三、OmniParser 配置（三选一）

OmniParser 用于屏幕元素检测（L5 部分功能 + precision 模式），L4 Vision 模式可不依赖它。

### 方案 A：GPU 隧道（推荐）

适用：校园 GPU 或可 SSH 的远程 GPU 机器。

1. 建立 SSH 隧道：

   ```bash
   ssh -L 9800:127.0.0.1:9800 student@10.246.2.7 -p 12202
   ```

2. 编辑 `HAJIMI_UI\server\.env`：

   ```ini
   OMNIPARSER_URL=http://127.0.0.1:9800
   OMNIPARSER_TIMEOUT=120
   ```

3. 重启 A 端和 B 端。

### 方案 B：本地 CPU

适用：无 GPU，愿意接受较慢的检测速度（2–4 分钟/帧）。

1. 安装 OmniParser：

   ```bat
   cd HAJIMI_UI
   scripts\setup_omniparser.bat
   ```

   需要 conda `omni` 环境和 ~4 GB 模型权重。

2. 编辑 `server\.env`：

   ```ini
   OMNIPARSER_URL=http://127.0.0.1:8002
   OMNIPARSER_TIMEOUT=360
   ```

### 方案 C：纯 L4 Vision（零依赖）

适用：只要 L4 指引功能，不需要 precision 和元素检测。

编辑 `server\.env`：

```ini
DETECTOR_BACKEND=vision_llm
```

L4 完全可用；L5 部分功能受限（无元素定位时退化为文本指令）。

---

## 四、C 端组件（可选）

### 语音识别/合成

```bash
pip install -r client/requirements.txt
python client/voice_setup.py    # 验证 ASR/TTS 模块
```

Vosk 中文模型已在 `models/vosk-model-small-cn-0.22/`。

### B-C 信号联调

需 B 端 UI 已启动：

```bash
python client/bc_adapter.py
```

### 审计队列

C 端 `AuditAgent` 使用本地 SQLite 队列（`%LOCALAPPDATA%/HAJIMI/audit_queue.db`），离线缓存、恢复网络后批量上报到 A 端 :8010。

---

## 五、Web 管理面板（可选）

```bash
cd web-admin
npm install
npm run dev          # http://localhost:5173
```

或生产构建：

```bash
npm run build        # 输出 web-admin/dist/
```

---

## 六、数据存储位置

| 数据 | 路径 | 说明 |
|------|------|------|
| 主数据库（7 表） | `HAJIMI_UI/data/hajimi.db` | A 端首次启动自动建表 |
| L5 数据库 | `server_A/data/hajimi.db` | 独立实例 |
| C 端审计队列 | `%LOCALAPPDATA%/HAJIMI/audit_queue.db` | C 端首次运行创建 |
| 用户设置 | `%LOCALAPPDATA%/HAJIMI/user_settings.json` | B 端启动时读写 |

---

## 七、端口一览

| 端口 | 进程 | 作用 |
|------|------|------|
| 8010 | A 端 FastAPI | L3/L4 规划、inspect、admin |
| 8011 | L5 Sidecar | L5 自动执行 Agent |
| 9800 | OmniParser GPU 隧道 | 屏幕元素检测 |
| 8002 | OmniParser 本地 CPU | 备选 |
| 5173 | web-admin dev | 管理面板开发模式 |

---

## 八、常见部署问题

| 现象 | 处理 |
|------|------|
| `找不到 server_A` | 确认 `server_A` 与 `HAJIMI_UI` 同级目录 |
| L5 venv 验证失败 | `cd server_A` → `scripts\repair_l5_venv.bat` |
| `fastapi._compat` 报错 | 同上 |
| pip 安装失败 | 关闭代理/VPN → 重试 |
| `A-end not ready` | 检查 8010 窗口日志；确认 `server\.env` 已配置 |
| 端口被占用 | `HAJIMI_UI\scripts\stop_all.bat` |
| 无 LLM API Key | 编辑 `server\.env` 后重启 A 端 |
| 中文输出乱码 | 正常 — 控制台编码问题；功能不受影响 |
