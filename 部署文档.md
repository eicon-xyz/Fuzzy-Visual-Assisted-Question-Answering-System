# HAJIMI 部署文档

> 适用对象：接收交付包后首次安装部署 | 版本：v2.0 | 2026-09（L5 自动执行，唯一模式）

---

## 一、系统要求

| 项目 | 最低要求 | 建议 |
|------|----------|------|
| 操作系统 | Windows 10 x64 | Windows 11 x64（L5 依赖 Windows UIA） |
| Python | 3.12+（安装时勾选「Add to PATH」） | 3.12 |
| 内存 | 8 GB | 16 GB+（L5 Sidecar 含 PyTorch 约 2 GB） |
| 磁盘 | 4 GB 空闲（含两个 venv） | 10 GB+ |
| 网络 | 首次 pip 安装需联网 | 后续可离线 |
| 浏览器 | Edge 或 Chrome（L5 网页自动化） | 最新版 Edge |
| LLM API Key | 文本对话模型（默认 DeepSeek `deepseek-chat`，OpenAI 兼容接口亦可） | — |

> L4 指引模式与 OmniParser（含 :9800 GPU 隧道、本地 :8002、校园 GPU、内网联调）已整体移除，无需任何视觉模型或远程后端。

---

## 二、快速部署（单机 L5，5 步）

### 第 1 步：解压

将 `HAJIMI-release-YYYYMMDD.zip` 解压到**不含中文和空格**的路径，例如：

```
E:\HAJIMI\
```

确认目录结构：

```
E:\HAJIMI\
├── 安装全栈.bat
├── 启动全栈.bat
├── 验收.bat
├── stop_all.bat
├── 打包说明.md
├── HAJIMI_UI/          # B 端 PyQt5 桌面应用
├── server_A/           # L5 Sidecar :8011（唯一后端）
├── client/             # C 端 语音/审计
├── web-admin/          # 管理面板（API → :8011）
├── models/             # Vosk 中文语音模型
└── launchers/          # 启动快捷方式
```

### 第 2 步：安装依赖

双击 **`安装全栈.bat`**，等待完成（首次 10–30 分钟）。

脚本自动执行：

1. B 端 UI venv（`HAJIMI_UI\.venv`，依赖仅 PyQt5）
2. L5 Sidecar :8011 venv（`server_A\server\.venv`，含 PyTorch，最慢）
3. 初始化 `server_A\server\.env`（从 `.env.example` 复制）并写入 L5 设置（`OMNIPARSER_ENABLED=false`、`ROUTING_MODE=l5`）

### 第 3 步：配置模型 API Key

模型 key **唯一存放处**为 **`server_A\server\.env`**，至少填写：

```ini
DEEPSEEK_API_KEY=sk-your-deepseek-key
DEEPSEEK_MODEL=deepseek-chat
```

或改用任意 OpenAI 兼容文本模型：

```ini
LLM_API_KEY=sk-your-key-here
LLM_BASE_URL=https://api.siliconflow.cn/v1
LLM_MODEL=Qwen/Qwen2.5-72B-Instruct
```

也可以先跳过此步：启动后在 B 端「系统设置」页填入，保存时会自动同步写入 `server_A\server\.env` 并重启 Sidecar。

### 第 4 步：启动服务

双击 **`启动全栈.bat`**（内部 `HAJIMI_UI\scripts\start_release_fullstack.bat`），将依次打开控制台窗口：

| 窗口标题 | 端口 | 作用 |
|----------|------|------|
| HAJIMI-L5-Sidecar | 8011 | L5 规划 + 自动执行（UIA 绑定 + Playwright DOM） |
| HAJIMI-B-end | — | PyQt5 桌面 UI |

等待 Sidecar 健康检查通过后，B 端 UI 窗口弹出。

### 第 5 步：验收

在 B 端 UI 启动后（保持 Sidecar :8011 运行），双击 **`验收.bat`**（内部 `verify_all.py --require-l5`）。

通过标准（示例）：

```
check_ui_env           PASS
verify_bc_signals      PASS
verify_settings_fragment / verify_theme_apply  PASS
verify_l5              PASS
```

停止服务：双击 `stop_all.bat`（只停 :8011）。

---

## 三、C 端组件（可选）

### 语音识别/合成

```bash
pip install -r client/requirements.txt
python client/voice_setup.py    # 验证 ASR/TTS 模块
```

Vosk 中文模型已在 `models/vosk-model-small-cn-0.22/`。C 端 `server_url` 默认已指向 Sidecar `http://127.0.0.1:8011`。

### B-C 信号联调

需 B 端 UI 已启动：

```bash
python client/bc_adapter.py
```

### 审计队列

C 端 `AuditAgent` 使用本地 SQLite 队列（`%LOCALAPPDATA%/HAJIMI/audit_queue.db`），离线缓存、恢复网络后批量上报到 Sidecar :8011。

---

## 四、Web 管理面板（可选）

```bash
cd web-admin
npm install
npm run dev          # http://localhost:5173（vite proxy → 127.0.0.1:8011）
```

或生产构建：

```bash
npm run build        # 输出 web-admin/dist/
```

面板由 Sidecar（:8011）挂载的 admin/audit/auth/flow/monitor/users 路由提供数据。

---

## 五、数据存储位置

| 数据 | 路径 | 说明 |
|------|------|------|
| 主数据库（7 表） | `server_A/data/hajimi.db` | Sidecar 首次启动自动建表 |
| C 端审计队列 | `%LOCALAPPDATA%/HAJIMI/audit_queue.db` | C 端首次运行创建 |
| 用户设置 | `%LOCALAPPDATA%/HAJIMI/user_settings.json` | B 端启动时读写 |
| 模型 key | `server_A/server/.env` | 唯一存放处 |

---

## 六、端口一览

| 端口 | 进程 | 作用 |
|------|------|------|
| 8011 | L5 Sidecar（server_A FastAPI） | 唯一后端：L5 执行、admin、auth、audit、config-pull |
| 5173 | web-admin dev | 管理面板开发模式（proxy → 8011） |

> 旧 A端 `:8010`、OmniParser `:9800`（GPU 隧道）/ `:8002`（本地 CPU）已随 L4 指引模式移除，不再使用。

---

## 七、常见部署问题

| 现象 | 处理 |
|------|------|
| `找不到 server_A` | 确认 `server_A` 与 `HAJIMI_UI` 同级目录 |
| L5 venv 验证失败 | `cd server_A` → `scripts\repair_l5_venv.bat` |
| `fastapi._compat` 报错 | 同上 |
| pip 安装失败 | 关闭代理/VPN → 重试 |
| `L5 Sidecar not ready` | 检查 HAJIMI-L5-Sidecar 窗口日志；确认 `server_A\server\.env` 已配置 |
| 端口被占用 | `HAJIMI_UI\scripts\stop_all.bat`（只停 :8011） |
| 无 LLM API Key | 编辑 `server_A\server\.env` 或 B 端设置页保存（自动同步并重启 Sidecar） |
| 中文输出乱码 | 正常 — 控制台编码问题；功能不受影响 |
