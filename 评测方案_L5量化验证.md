# HAJIMI L5 量化验证评测方案

> 版本：v1.0 · 2026-09 · 关联：《调研报告》§四（P0/P0.5 实施状态）、《审查台账》E4（"改进不可归因"）、`server_A/eval/HOWTO_WINDOWS.md`（操作手册）
> 一句话：**P0/P0.5 改完了但没有一个真数。本方案用一台 Windows 机器、半天时间，产出回答四个决策问题的数据表。**

---

## 1. 评测要回答的四个问题（不是"跑个分"）

| # | 问题 | 决策含义 |
|---|---|---|
| Q1 | **P0/P0.5 净贡献是多少？**（现行 vs 改前，同任务同 seed） | 继续投 P1？还是某几项"更贵不更对"该回滚？ |
| Q2 | 失败集中在哪一类？（四类打标） | P1 该先做 E1 跨步重规划，还是先补感知盲区（A2/A3 真机修正）？ |
| Q3 | 新机制有多少真的在用？（expect 使用率、gate 拦截、stale 触发、批量命中） | 回答台账 C1/C2/D1 的空口之争——用分布数据定语义 |
| Q4 | 成本曲线如何？（token/轮次/耗时/LLM 调用数） | 台账 E3 上下文治理的紧迫度定量 |

## 2. 指标体系（全部由现成管线自动产出，零新增代码）

**主口径**：`All-Pass@4`（同任务 3 seeds × 4 repeats **全过**才计通过——RoTS 生产稳定性口径）
**辅口径**：`pass@1`、逐任务 seed 明细
**行为指标**（runs.jsonl 遥测，`report.py` 自动聚合）：

| 指标 | telemetry 字段 | 健康解读 |
|---|---|---|
| expect 使用率/命中率 | `expect.used / hit` | used/actions <30% → C2 判定"验证链空转"，expect 需强制化 |
| done gate 拒收/豁免 | `gates.done_refused / unverified_done` | refused 高而成功率不升 → D1"仪式化"实锤，改证据语言 |
| 卡死越界 | `loop_events.repeat5/8/12 · stagnation · replan` | 越界后任务仍死 → **E1 插队实锤**（检测会说话不会拔电） |
| actionability 拒发 | `not_actionable` | 高且随后成功 → 谓词有效；高且任务死 → 阈值过敏 |
| 批量 | `tool_calls.perform_batch` + partial 率 | 验证 B1 价值（UFO² 口径应见 llm_calls 降） |
| 稳定 id 真实表现 | `errors.element_stale`（新码，A4 真机首验） | stale 高频 = RuntimeId 假设失效，回退策略要调 |
| 观察成本 | `snapshots.count/latency_ms/nodes` | 回答 WAA 的"UIA 秒级查询"诅咒是否命中我们 |
| LLM 经济性 | `llm_calls · tokens_prompt/completion · rounds · wall_ms` | Q4 主数据 |

## 3. 任务集与校准（诚实铁律）

- 现有 `server_A/eval/tasks/seed.json`：**20 任务（16 正向 + 4 负向），8 项 P0 每项 ≥2 暴露任务，全部 `calibrated:false`**。
- **未校准不得计 KPI**（README 双向校准协议）：每条任务先真做 → oracle 必须 PASS；再故意失败（不做/做错）→ oracle 必须 FAIL。两向皆过才标 `calibrated:true`。**先校准 5 条覆盖 P0 核心面的任务**（建议 `notepad_type_save` + 菜单路径 + 弹窗干扰 + 1 负向红线 + 1 浏览器），当天可跑。
- 扩充：把 Windows 回测第一轮（任务提示词·其一）暴露的真实失败指令以 `source:user-failure:*` 入集——**评测集必须长在自己的失败分布上**（RoTS：自分布数据 +5.3 vs 人工 +0.6）。
- **公开集移植（2026-09 调研定案，详见根目录《调研报告_公开评测集选型.md》）**：主来源 = **WindowsAgentArena**（154 条 Windows 真任务、MIT、原生 UIA 观测 `--som-origin a11y --a11y-backend uia`、含 13 条 `infeasible` 负向、任务初始文件公开可镜像）——写 `waa2seed.py` 抽 60 条机器判据可映射任务转中文 seed.json（≈6–8 人日）；OSWorld `examples_windows`(49)+`test_infeasible`(9) 同格式可批量并入；**WindowsWorld**(181，唯一自带中文指令)只借任务文本与 L4「应拒绝」设计、判据是 VLM 截图判分触红线不借；浏览器通道借 **WebArena-Verified**(ServiceNow) 的确定性 comparator/后端状态校验/95%CI 协议升级 `oracle_eval.py`+`report.py`。**结论：中文+Windows+机器判据 公开集为空白，自研集即生态位。**

## 4. 实验设计：A/B 基线回测

```
B 组(现行)  = master HEAD                    ← 今天
A 组(基线)  = git worktree add ../l5-pre-P0 267ba932   ← P0 开工前最后一 commit
同一台机器 · 同一 .env(同模型同温度) · 同一清理协议(每实例 setup/cleanup PS1 幂等)
参数：--seeds 3 --repeats 4 --order-seed 固定（可复现乱序，防顺序偏置）
```
- A/B 各 20 任务 × 3 seeds × 4 repeats = **240 实例/组**；按 60–120s/实例估 **4–8h/组**——拆两天或先砍到校准过的 5 任务 × 全参数（40 实例/组）拿第一个信号。
- 每批次自动打 `git_sha` 标签 → `report.py 结果A 结果B` 直接出对比 markdown。
- 环境控制：Windows 测试账号 + 关通知/锁屏；有条件上 VM 快照（cua 教训：**环境不可复现则数字不可信**）。

## 5. 数据呈现形态（`report.py` 实拍：以下为**合成输入**跑出的真实管线输出——数字虚构，格式即成品）

```markdown
# HAJIMI L5 评测报告
## 对比 · 基线(demo_base) → 现行(demo_new)

| 指标 | 基线 | 现行 | Δ |
|---|---|---|---|
| All-Pass 率(主) | 33.3% | 83.3% | +50.0% |
| pass@1 | 79.2% | 87.5% | +8.3% |
| 均 LLM 调用/实例 | 19.8 | 8.8 | -11.0 (-56%) |
| 均 tokens/实例 | 33725 | 8263 | -25462 (-75%) |
| 均轮次/实例 | 13.0 | 13.3 | +0.3 (+3%) |
| 均耗时 s/实例 | 85.1 | 101.0 | +15.9 (+19%) |

### 现行批次行为指标（P0 机制是否在干活，基线组此栏天然为空）
- P0 行为：expect 34用/25中 · gates {'done_refused': 10, 'unverified_done': 1}
  · loops {'repeat5': 7, 'stagnation': 4} · not_actionable 10 · 快照 113
  · errors {'element_stale': 3}
- 失败四类：{'grounding': 1, 'perceptual': 1, 'recovery(执行偏航)': 1}

## 逐任务
| 任务 | 基线 allpass / pass@1 | 现行 allpass / pass@1 |
|---|---|---|
| calc_add | 50% / 88% | 100% / 100% |
| edge_search | 0% / 75% | 50% / 62% |
| notepad_type_save | 50% / 75% | 100% / 100% |
```

**读法三例**：①expect 34 用 → 使用率若 <30% 坐实台账 C2"验证链空转"；②loops.repeat5=7 而任务仍死 → "检测会说话不会拔电"，E1 插队实锤；③gates.done_refused=10 且二次带证据成功率高 → D1 的 gate 不是仪式，保留并谓词化。

## 6. 判读与行动映射（开跑前定死，防"跑完再找话说"）

| 观察 | 行动 |
|---|---|
| All-Pass Δ ≥ +10% 且 tokens 不涨 | P0.5 定案 → 进 P1（1.1/1.2 按归因表定序） |
| Δ ∈ [−5%, +10%] | 按 5.2 逐机制去留：空转机制简化或强制化，不整体回滚 |
| Δ < −5% 或 tokens +50% 且 pass 不动 | **回滚候选**（台账协议）：逐包二分（B1/A3 最可疑），worktree 半程回测 |
| 归因 recovery 占比 >35% | P1-1.1 双账本升为第一优先 |
| expect 使用率 <30% | 台账 C2 有数：下批强制 expect 或降级其返回权重 |
| element_stale >10% | A4 假设复检（RuntimeId 在该应用不稳定 → 路径哈希回退） |

## 7. 执行 SOP（详见 HOWTO_WINDOWS.md，此处为决策节奏）

```
Day0(30min)  环境清场 + 校准 5 条（双向）
Day1(半天)   现行 master 批跑（5 任务 ×3 seeds ×4 repeats，40 实例）
Day2(半天)   基线 worktree 同参数批跑
Day2 晚      report.py 出表 → 按 §6 判读 → 台账落对应行的"答案+数据"
之后          日常 B 端使用自动积累 runs.jsonl，攒 20+ 行跑趋势
```

## 8. 红线与局限声明

- 红线不破：评测只是消费者——不改引擎、不碰红线判定、`--require-l5` 冒烟仍是唯一接线检查；oracle 禁 llm_judge/截图判分（T2 刻意设计）。
- 已知局限：单机单模型（跨 harness 数字不可比，cua 90%→9% 教训）；20 任务仍偏小（结论报"方向+置信区间"不报"精确百分点"）；Linux 侧只能验管线本身，**所有成功率数字只从 Windows 出**。
