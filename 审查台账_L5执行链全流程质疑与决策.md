# L5 执行链审查台账（感知→操作→验证→证据→收口）

> 审查窗口：2026-09-03 · 对象：master @ 049acc8a（P0 八项落地后）· 关联：《调研报告_L5-Agent架构对标开源.md》v3.2 §四
> **性质：活文档。** 一条问题一行状态，逐条解决、逐条勾销；决策变更必须记"为什么"。全部关闭后结论回填调研报告 §四。
> 状态图例：✅ 已决策待实施 · 🗓️ 讨论中（等回答"为什么"）· 🔧 实施中 · ✔ 已解决（附 commit）· ❌ 不做（附理由）

---

## 0. 全流程实况快照（以代码为准，行号基于 049acc8a）

```
指令 → B端红线归一化 → :8011 红线复检 → planner.plan_steps() 一次性静态分解
  → engine for 循环逐步执行：
      execute_step ≤50 轮：deepseek-chat(512 tok, T=0.2) → 单工具调用 → dispatch_tool
      感知 = UIABridge.snapshot()：GetForegroundControl → DFS depth6/120
             → 10类白名单 → 可交互优先 top-40 投影 → 附全屏 JPEG base64
      操作 = _check_actionable(轮询≤3s) → Invoke/Select/Toggle/Expand 逐个try → 坐标回退
      验证 = verify()(目标控件 enabled+onscreen 轮询) + 目标控件 props diff
             + expect→wait_for_text(全树 Name 子串轮询≤4s)；失败→自动重观察
      证据 = 每步 _action_evidence 账本 → mark_step_done gate（无证据拒收一次，二次放行打标）
      收口 = done→previous_steps 只带一句 action_summary；
             failed→同指令盲重试1次→整任务终止；infeasible/ask_user→跳过重试终止
```

---

## 1. 感知层 A

### A1 截图 base64 进 LLM 上下文 ✔ 36b57820
- **证据**：`agent.py:1034` 写入 `result["annotated_image"]`；`:1831` 仅 get 转发 SSE、**从不 pop**；`:1897` `json.dumps(result)` 整包进 tool message。全屏 JPEG ≈150–400KB ≈ 数万 token/次；P0 的自动重观察把触发频率进一步放大。
- **误解澄清（审查者）**：移除它与完成判定**零关系**——deepseek-chat 是文本模型，base64 在它眼里是随机 token，不携带屏幕语义；它实际依赖的是元素投影、verified/state_changed/expect_ok、证据账本。图的两个消费者里只有 SSE→B 端渲染是有用的。若要"看一眼画面再判"，那是换模型的 P2-2.3 范畴，不属本条。
- **决策**：进 messages 前 pop（SSE 路径不动）；回归测试：tool message 不得含 `annotated_image` 键。
- **行动**：一行修复 + 断言（批次 0）。

### A2 单前台窗口感知 ✅（与 A3/A4 绑定）
- **现象**：只 `GetForegroundControl()`；弹窗（独立 HWND：另存为/UAC/Office 提示）、多窗口任务不可见；OSWorld 错误链"弹窗遮挡"无对策。
- **回答（项目方）**："这点没有想到，就应该按全部可见顶层窗口 + 激活窗口优先。"
- **决策**：采纳。**依赖序警告（审查者）**：多窗口全树扫描=观察成本翻倍，必须与 A3 浅探同批落地，不得单开。
- **行动**：快照改为"每个可见顶层窗口 depth-2 大纲 + 激活窗口默认展开"（批次 2，随 A3）。

### A3 全树重扫、无浅探/按需下钻/语义过滤 ✅
- **现象**：每节点 5 次 `Get*Pattern` 探测，depth6×120 = 数百次跨进程调用/次观察（WAA 实测 UIA 查询可达秒级）；目标在 depth7+ 或 120 节点外永远不可见；被截断对 LLM 不可见（连"被折叠"这个事实都没暴露）。
- **回答（项目方）**："使用 uiacli 的方案，并解释按需下钻的'按需'怎么实现；做语义过滤。"
- **设计定稿（审查者已给出，此处固化）**：
  `get_screen_info(scope, filter, depth)` 三参数：
  1. **浅树大纲**：默认 depth-2，容器带计数摘要（如 `u12 pane "文件列表" (142 items, 可展开)`）——让"被折叠"对 LLM 可见；
  2. **按需 = LLM 用 scope 参数指定下钻**（服务端只确定性执行，不做聪明猜测）；依赖 A4 稳定 id，scope 才指得准；
  3. **语义过滤两层**：第一层词法（当前步 instruction 切词 × name/class/type 重叠打分 top-k，零依赖可测）；第二层 embedding 余弦（`context/embedding_matcher.py` all-MiniLM-L6-v2 本地 CPU 已有，词法 0 命中才回退，做开关）。
  **护栏**：filter 可能漏目标（"保存" vs 软盘图标），故大纲计数恒在，LLM 可 `filter=""` 重取；UFO soft filter 同样不隐藏折叠量。
- **行动**：批次 2（A3→A2→A4 同批）。

### A4 id 重编号制度 ✅（与 A3/A4 绑定）
- **现象**：`u1..uN` 按遍历序现编、每次快照全失效 → 验证失败/菜单展开/找不到重试都连锁触发昂贵的重观察。
- **回答（项目方）**："当时只考虑最简实现，现在采取考虑连锁成本的方案。"
- **决策**：改稳定 id（runtime-id 或 窗口句柄+树路径）。**配套协议**：控件销毁后 `act(id)` 必须报 `element_stale` 而非解析到重建的同名控件（抄 Playwright detached handle 的报错语义）。
- **行动**：批次 2。

---

## 2. 操作层 B

### B1 每轮单动作（自我设限）✅
- **证据**：system prompt"禁止在一次响应中调用多个工具"；填 5 字段表单 ≥10 轮往返。
- **回答（项目方）**："当时没考虑到，现在采取投机批量的方式。"
- **决策**：实施 UFO² 式 `perform_batch([...])`（报告 P1-1.4 提前）。**接口冲突预警（审查者）**：批量会击穿 `_LoopDetector` 动作滑窗——契约定为：子动作逐个记账进滑窗；**首败即停**返回 `partial`；未执行子动作显式回报 `"Not executed: earlier action failed"`（Anthropic 语义）。
- **行动**：批次 3（依赖 B2 决策表先行——批量子动作要先能确定性选对 pattern）。

### B2 pattern"选第一个能用"而非"选最对" ✔ d0a7eb85
- **证据**：`_act_click` 链 invoke→select→toggle→expand→coord 逐个 `except: pass`——"不支持该 pattern"与"执行失败(控件死亡)"不可区分；死亡控件落到坐标点击（OSWorld 2.0"执行时刻坐标已失效"原病）；Invoke 已生效但超时→坐标补刀=**双触发**（不可逆动作重复执行）。
- **回答（项目方）**："现在的方案是选择第一个能用的而不是最对的，有哪些方案能做出正确的选择？"（反问，方案清单如下，**待项目方拍板**）
  1. ControlType→pattern 决策表（button→Invoke / checkbox→Toggle / tabitem·listitem→Select / menuitem 有子→Expand 叶子→Invoke / combobox→Expand / edit→Value），类型投影已有；
  2. 动词上移给 LLM：拆 `click/expand/toggle/select/set_value`（patterns 字段已暴露），click 保留=按表映射的语法糖，表判不出返回 `action_ambiguous`；
  3. **fail-closed**：选定 pattern 执行失败→报 `pattern_failed` 停止，禁自动降级坐标；
  4. 坐标回退显式化：LLM 传 `via="coordinate"`+理由才允许；
  5. probe 与 exec 分离：支持性用快照缓存的 patterns，执行期不再 `Get*Pattern()` 试探。
- **审查者立场**：1+3+5 立即做（纯正确性）；2 随 R4 prompt 重写一并改契约；4 可与 2 同批。
- **行动**：批次 1。
- **实施注记（P0.5-B2 d0a7eb85）**：实施 1+3+5+4最小逃生舱；方案2（动词拆分）按台账约定并入 R4 prompt 重写。

### B3 paste_text/press_key 无焦点校验 ✅
- **现象**："粘贴到当前焦点"焦点归属不可见；前一步点错窗口则整段文本灌进错误应用。type_text 有元素断言，paste_text 零断言。
- **回答（项目方）**："应该在感知做。"
- **决策**：感知层补 `GetFocusedControl()` → 观察结果头部加 `focused: {name,type,class}` 字段；paste_text 增加可选 `expect_focus` 前置断言（不满足→拒发，错误码 `focus_mismatch`）。
- **行动**：批次 4。

### B4 动作空间缺口（右键/拖拽/窗口动作）✅（先算覆盖账）
- **回答（项目方）**："应该添加，前提是有哪些操作能被其他操作覆盖。"
- **覆盖账（已裁定，固化）**：
  | 拟新增 | 已覆盖方 | 结论 |
  |---|---|---|
  | 滑杆拖动 | RangeValuePattern.SetValue | 加 `set_range`，**不加 drag** |
  | 滚动条拖动 | scroll + press_key(PageDown/Space) | 不加 |
  | 文本区间选择 | Shift+方向键（press_key 可表达） | 不加 |
  | 右键菜单 | 多数 Win32 支持 Shift+F10/Apps 键；Electron/UWP 部分不支持 | **加最小 right_click**（clicker.right_click_at 现成，只差暴露），低优先 |
  | hover 飞窗 | ExpandCollapse + 自动重观察已覆盖 | 不加通用 hover |
  | 文件拖拽 | 正解=P2-2.2 语义函数/COM/剪贴板键 | **不做，写进反模式** |
  | 窗口切换/置前/最小化 | Alt+Tab 只覆盖一半；不抢焦点的后台 SetFocus 无键鼠等价 | **加 WindowPattern 组**，高优先（A2 多窗口的必然配套） |
  | 多级菜单 | 逐级 click 可行但 N 次展开+重观察 | 加 `select_menu_path(items[])` |
- **真缺口四件**：WindowPattern 组、select_menu_path、set_range、最小 right_click。
- **行动**：批次 4。

---

## 3. 验证层 C —— 🗓️ 全部待答（原回答在"C"处截断）

### C1 state_changed 语义错位（假阴性源）
- **现象**：`state_changed`=目标控件**自身**属性变化；点"新建标签页"/"登录"，变的不是按钮本身 → 正常动作被判"无变化"，每个假阴性=全树重扫+base64+一轮 LLM。UFO 原型是"预期有界面变化的动作→对照动作前后**全局**快照"。
- **待答问题**：接受局部 diff 当通用验证代理，是有意识的两步走，还是沿用旧 `verify()` 的路径依赖？若同意，改为**全局投影 diff**（动作前后各一次大纲级指纹对比，成本≈一次浅扫，依赖 A3）。

### C2 expect 可选且断言语言太弱
- **现象**：仅"界面出现某子串"；无否定/value/文件谓词；不强制→模型大多不写→0.2 验证链对多数动作空转。
- **待答问题**：expect 实际使用率打算测吗（依赖 E4 遥测）？覆盖率<50% 时 0.2 的有效成分只剩可疑 diff——接受这个现状到什么程度？

### C3 每轮无条件 `time.sleep(1.5) # throttle OmniParser load` ✔ 51ccf8ff
- **证据**：`agent.py:1721`。OmniParser 已退役，理由不存在；50 轮一步纯睡 ~73s。
- **状态**：✔ 51ccf8ff（P0.5-R2 落地）。

### C4 3s+4s 同步轮询压在关键路径
- **现象**：actability/verify/expect 全同步 sleep-poll；UIA 本有 StructureChanged 事件可零成本等待。轮询×全树重扫=每采样都贵。
- **待答问题**：事件驱动是能力圈外还是没排期？若维持轮询，A3 浅扫落地后采样成本降一个量级是否即可接受？

---

## 4. 证据层 D —— 🗓️ 全部待答

### D1 done 证据化目前是"仪式"非"判据"
- **现象**：三条放行路径无一可机器独立核查：强证据依赖 C1；`evidence≥8字`自由文本=幻觉换个长点的说法；`launch_app/browser_navigate 成功→放行`=对静默失败的 tier 链开绿灯。真正的独立判据（complete_criterion、首末帧 a11y diff、env-check oracle）一个未接。
- **待答问题**：自由文本 evidence 是防假 done 还是防"太快 done"？若是前者，为什么不把每步 complete_criterion 定义成 expect 同款谓词（planner 产出步骤时顺手生成）？gate 当前唯一确定生效的行为=每次 done 多烧一轮 LLM，这个代价换来的是什么？

### D2 证据不被下游消费
- **现象**：evidence 只进 SSE 与 ExecutedStep；下一步 previous_steps 只有一句 action_summary——"假 done 传染"病灶（报告 §1.3-2）未真正闭合。
- **待答问题**：是遗漏还是刻意省 token？若补，注入格式建议"step N: 摘要｜evidence: …（unverified_done 时显式警示）"。

### D3 mark_step_failed 无条件拦一次
- **现象**：红区拦截/应用不存在等确定死路也白烧一轮；缺"哪些 error_code 可直接失败"白名单（如 blocked_redline）。
- **待答问题**：过早放弃群体收益 vs 确定失败单次税，有没有阈值化折中（如同一 error_code 连续出现即免拦）？

---

## 5. 收口/骨架层 E —— 🗓️ 全部待答（审查者插队立场：E1/E4 优先级高于 P1 原序）

### E1 跨步无重规划（当前成功率第一限制）
- **现象**：步内 nudge 已做；步间仍是 for 循环 + 盲重试 1 次 + 整任务终止。一次环境偏差仍杀死全任务。
- **待答问题**：P0 收益在这副骨架上能兑现吗？1.1/1.2（stall 账本+proactive replan）该不该插队到一切新毛刺修复之前？

### E2 账本只说话不行动
- **现象**：⛔ 阈值后仍带着同一份膨胀消息继续跑；Magentic 原语义=清历史保账本重规划。
- **待答问题**：清历史重建（保留账本+最近观察）在 L5 里缺哪些前置（账本不存在→先做 E1 的账本？）

### E3 上下文无治理
- **现象**：messages 线性膨胀（R1 炸弹+每次重观察 top-40+验证字段 JSON），512 tok 思考上限；P0 是往桶里多倒东西。
- **待答问题**：1.3 压缩是否提前到与 R1 同批（R1 已去掉大头，先量一把再决定压缩策略？）

### E4 零遥测（八项改造不可归因）
- **现象**：snapshot 耗时/节点数、expect 出现率与命中率、gate 拒收率、每步 token、not_actionable 分布——全无。
- **待答问题**：接受"先改后测"到什么程度？最低成本的埋点方案（execute_step 内一个 dict 收集 + 任务结束一条 log）要不要作为批次 0 的伴随项？

---

## 6. 冗余清理 R（删，不辩护）

| # | 冗余项 | 状态 | 处置 |
|---|---|---|---|
| R1 | annotated_image 进 LLM 上下文（A1） | ✔ 36b57820 | messages 前剥离 + 回归断言（P0.5-R1 落地） |
| R2 | `sleep(1.5)` OmniParser throttle 化石（C3） | ✔ 51ccf8ff | 删除；限流交 API 层退避（P0.5-R2 落地） |
| R3 | omniparser 兜底分支整套 + `OMNIPARSER_ENABLED` 开关 | 🗓️ 待确认 | 红线已禁，代码还活着（agent.py:1051 起，含 1054 处 core.screen_capture 引用）；建议删分支、感知失败直接报错 |
| R4 | prompt 互相矛盾条款堆积（"≤1 次 get_screen_info"vs 自动重观察；"找不到先 wait(2)"vs 停滞检测；效率约束段 vs _LoopDetector） | ✅ 需重写 | 与 B2 动词契约、C1 结论同批一次性重写（四段式），禁止再钉补丁 |
| R5 | server_A 顶层死 fork（core/、ui/、main.py） | 🗓️ 待确认 | **卫生风险实锤**：fork 内 service_manager/api_client/deployment_resolver/defaults/ui_parser/settings_widgets/medium_panel 等仍含 :8010/OmniParser 字符串（grep 已证）——留着会污染红线审计；agent 对 core.screen_capture 的引用仅两处（:1032 R1 删、:1054 R3 删），R1+R3 落地后即可整删 |

---

## 7. 实施批次草案（P0.5 修订批次，依赖序）

| 批次 | 内容 | 依赖/理由 |
|---|---|---|
| **0 立即** | R1、R2（+可选 E4 最小埋点：每步 token/耗时/expect 使用 dict→log） | 纯损失回收，一行级，不依赖任何讨论结论 |
| **1** | B2 决策表方案 1+3+5（正确性优先：不再自动降级坐标、双触发风险归零） | ✔ d0a7eb85（P0.5-B2 落地，另含方案 4 最小逃生舱 via='coordinate'）；B1 的前置（批量子动作需确定性 pattern） |
| **2** | 观察成本三连：A3 浅探+scope/filter → A2 多顶层窗口 → A4 稳定 id+element_stale 协议 | 三者同批，否则 A2 会把观察成本翻倍到失控；A1 已先行降本 |
| **3** | B1 `perform_batch`（含滑窗记账/首败即停/未执行显式回报契约） | 依赖批次 1 |
| **4** | B3 焦点感知（focused 字段+expect_focus）；B4 四件套（WindowPattern 组/select_menu_path/set_range/最小 right_click） | 独立 |
| **5** | R4 prompt 四段式重写（吸收 B2 动词契约、C1/D1 最终语义）；R3+R5 死代码清理 | 等 C/D 层结论，避免两轮重写 |
| **插队 P1** | E1 步间账本+proactive replan、E4 评测台最小集（≥3 seeds+All-Pass@4+四类打标） | 审查者立场：成功率第一限制与归因能力，排在一切新毛刺之前 |

---

## 8. 决议协议

1. 每条状态变更在本文件更新对应小节（决策/行动/commit 号），不另开文件；
2. 实施批次=独立 commit，message 前缀 `P0.5-x.y`，引用本文件问题号；验证门沿用：py_compile + HAJIMI_UI tests（35/6 基线）+ Linux sidecar verify_l5 冒烟 + test_executor_p0 扩展；
3. C/D/E 的回答补进对应小节"回答（项目方）"栏后再动批次 5 与插队项；
4. ✔ 条目保留在文件内（含证据与理由），作为设计决策史；全部关闭后浓缩回填调研报告。

---

## 附：待项目方回答的问题清单（下次窗口从这里继续）

- C1 局部 diff → 全局 diff？；C2 expect 使用率测量与强制策略；C4 轮询→事件驱动排期？
- D1 自由文本证据定位（防假 done 还是防太快 done）；D2 证据回灌下一步；D3 失败拦截白名单
- E1 重规划插队确认；E2 清历史重规划前置；E3 压缩提前量；E4 埋点方案与评测台启动时点
- R3/R5 死代码删除确认（含 server_A/core、server_A/ui、server_A/main.py 全部引用扫描）
